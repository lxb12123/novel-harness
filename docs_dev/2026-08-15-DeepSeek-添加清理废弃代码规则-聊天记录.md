# 添加清理废弃代码规则

> 整理说明：本文件从 Codex 原始 JSONL 会话中提取，只保留用户与 DeepSeek 的可见消息；系统提示、环境注入、内部推理、工具调用和工具原始输出已省略。对话正文未改写。

- 日期：2026-08-15
- 模型：deepseek-v4-flash
- Codex 任务 ID：01a007b2-ff33-7f43-a1f4-770d6668a64b
- 工作目录：`/Users/lixibin/Desktop/novel-harness`
- 可见消息：用户 5 条，DeepSeek 10 条
- 原始记录：[JSONL](/Users/lixibin/.codex/sessions/2026/08/15/rollout-2026-08-15T19-12-46-01a007b2-ff33-7f43-a1f4-770d6668a64b.jsonl)

---

## 用户 · 2026-08-15 19:13:56 EDT

帮我在claude.md和agent.md里边加话，我这个项目有agent.md吗就是加一个就是代码清洁的话，那些废弃的没有用的代码就是要删掉，不要保留在这里面。

---

## DeepSeek（过程更新） · 2026-08-15 19:14:05 EDT

项目里目前只有 `CLAUDE.md`，没有 `AGENTS.md`。我先看一下 `CLAUDE.md` 的结构，再把代码清洁的要求加进去，并新建一个 `AGENTS.md`。

---

## DeepSeek · 2026-08-15 19:14:55 EDT

搞定。两点说明：

1. **CLAUDE.md**：在「约定」一节加了一条代码清洁规则——废弃/没用的代码一律删除，不留备用（包括被注释掉的代码、死分支、砍掉功能的残留），并同步清理引用它们的测试、导入和文档；git 历史就是备用，需要时从历史捞。

2. **AGENTS.md**：项目里原本没有 `agent.md`，我新建了标准命名的 [AGENTS.md](/Users/lixibin/Desktop/novel-harness/AGENTS.md)（Codex 等 agent 工具读的就是这个名字）。考虑到这个仓库很忌讳「同一份内容多处拷贝」的问题，我没有把 CLAUDE.md 整份复制过来，只放了一条指向 CLAUDE.md 的入口说明 + 代码清洁规则本身。

如果你更希望叫 `agent.md`（小写）或者把 AGENTS.md 写得和 CLAUDE.md 一样全，跟我说一声就改。

---

## 用户 · 2026-08-15 19:23:42 EDT

那意思是说我原本这个项目不存在这个文件的含义吗？

---

## DeepSeek（过程更新） · 2026-08-15 19:23:44 EDT

对，就是这个意思。我确认一下：

---

## DeepSeek · 2026-08-15 19:23:52 EDT

是的，你项目里原本**没有**这个文件。

git 记录也证实了：`git ls-files` 里搜不到任何 `agents.md`（包括历史记录都没有），仓库里本来只有 `CLAUDE.md`。

所以这次我做的事是：

- **CLAUDE.md** —— 原本就有，我往里面加了一条代码清洁规则（改动了它）；
- **AGENTS.md** —— 原本不存在，是我新建的（git 里显示为 `??`，即新文件、还没提交）。

顺带一提：如果你之前听别人说「项目里应该有 agent.md」，那通常是搞混了——这个文件不像 `CLAUDE.md` 那样是 Claude 的默认配置文件，它一般是给 Codex 等其它 agent 工具用的，很多项目并不需要它。现在你有了，以后 Codex 类的工具进项目也会先读到这条代码清洁的规矩。

---

## 用户 · 2026-08-15 19:24:25 EDT

AGENTS.md —— 原本不存在，是我新建的（git 里显示为 ??，即新文件、还没提交）。 相同的替代文件作用有没有？就是我原来。

---

## DeepSeek（过程更新） · 2026-08-15 19:24:27 EDT

好问题，我去查一下项目里有没有别的「给 agent 看的说明文件」：

---

## DeepSeek · 2026-08-15 19:24:39 EDT

查完了：**没有**。项目里没有其它跟 `AGENTS.md` 同等作用的文件。

具体来说：

- **CLAUDE.md** 就是原来那个「给 agent 看」的文件——它是 Claude 读的约定文件，你的项目里它一直都在。
- 其它名字的类似物（`CODEX.md`、`.cursorrules`、`.codex/` 配置等）在项目里**都不存在**，我搜过了。
- `.claude/` 目录里只有一个空的 `worktrees` 文件夹，不是说明文件。
- 项目里虽然有几份文件名带 "agent" 的文档（比如 `docs/adr/0019-agent-loop-not-graph.md`），但那些讲的是**小说引擎产品里**的写作 agent 逻辑，不是给 AI 编码助手看的工程约定，作用完全不一样。

所以总结：你原来只有一份「给 agent 看的文件」= `CLAUDE.md`；`AGENTS.md` 是我新加的、跟它同类的文件，作用是让 **Codex 这类不读 CLAUDE.md 的工具**也能看到同样的约定。两份文件本质上是「同一用途的替代」，而不是项目里原来就有个我没发现的文件。

---

## 用户 · 2026-08-15 19:25:53 EDT

那你把这个文件的内容跟cloud。md对齐一下。

---

## 用户 · 2026-08-15 19:26:10 EDT

那你把这个文件的内容跟cloud.md对齐一下。

---

## DeepSeek（过程更新） · 2026-08-15 19:26:22 EDT

好，我把 `AGENTS.md` 的内容对齐成和 `CLAUDE.md` 一致。先读一下当前 `CLAUDE.md` 的完整内容：

---

## DeepSeek（过程更新） · 2026-08-15 19:26:44 EDT

内容拿到了。现在把 `AGENTS.md` 替换成和 `CLAUDE.md` 完全一致的内容（顶部加一行镜像说明，提醒以后两边要同步改，避免这份仓库最忌讳的「拷贝漂移」）：

---

## DeepSeek（过程更新） · 2026-08-15 19:27:11 EDT

改完了，现在验证一下两边内容是否一致：

---

## DeepSeek · 2026-08-15 19:27:15 EDT

对齐好了。[AGENTS.md](/Users/lixibin/Desktop/novel-harness/AGENTS.md) 现在和 [CLAUDE.md](/Users/lixibin/Desktop/novel-harness/CLAUDE.md) 的内容**逐字一致**（diff 验证过），包括：

- 动手前必读的文档清单（ARCHITECTURE、ADR、UI、EVAL_PROTOCOL、PLAN）
- 这个项目最容易犯的 7 个错
- 常用命令（L1/L2/L3 三层验证）
- 全部工程约定（含你要的代码清洁规则）
- 「现在做到哪」的进度指针

唯一多出来的，是文件最顶上的一行镜像说明：「本文件是 CLAUDE.md 的镜像，内容必须与它保持一致；改动任一份时，另一份必须同步改。」这行是特意加的——因为这个仓库自己最忌讳「同一份内容多份拷贝然后慢慢漂移」的问题，加了这行提示，以后谁改其中一份都会知道要同步另一份。

如果你觉得这行提示多余、想要两个文件完全一模一样（一个字节都不差），跟我说一声，我把它删掉。

