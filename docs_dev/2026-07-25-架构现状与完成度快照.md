# 2026-07-25 架构现状与完成度快照

> **这份文件有两半，读法不一样：**
>
> - **下面的「待办看板」是活的** —— CLAUDE.md 有一条规则要求每做完一件事就回来打勾。
>   它是这份文件里唯一会被持续改动的部分。
> - **其余全是 2026-07-25 那天的快照** —— 数字、判断、当时怎么读这些数字的。**不要改它**，
>   过期了就整份重写一份新日期的（一份快照一个文件，时间戳才有意义）。
>
> **两半都不是权威。** 架构事实的唯一副本在
> [`docs/ARCHITECTURE.md` 的「当前状态」](../docs/ARCHITECTURE.md#当前状态)，冲突以那边为准。
>
> 配套的方向决策见 [2026-07-25-分发形态定为桌面应用走C方向.md](2026-07-25-分发形态定为桌面应用走C方向.md)。

---

## 待办看板（活的 —— 做完一件回来打勾）

**全部打完 = 这份看板的使命结束**，那时要做三件事：把 CLAUDE.md 里那条「更新看板」的规则删掉、
在本节记一句「YYYY-MM-DD 全部完成，规则已移除」、然后整份文件重写成新日期的快照。

### 挡路的（做完才谈别的）

- [x] **`docs/EVAL_PROTOCOL.md` 提交进 git** —— **2026-07-25 完成**：单独提交一个文件
      （`0393088`，`2026-07-25T16:19:00-04:00`），不夹带任何代码改动，让这条 commit 的用途
      没有第二种解释。提交时 `runs/` 不存在、一次生成都没跑过 → **预注册成立**。
      ⚠️ 此后再改协议 = 改卷子，真要改就开新的一份并说明改了什么。
      **注意还欠一半**：`src/novel_harness/eval/`（判分器本体）和它的两个测试文件仍是 untracked。
      协议的效力不受影响，但「判分器晚于结果被写出来」同样是可质疑的——见下方新增项。
- [x] **判分器进 git** —— **2026-07-25 完成**（`8190882`，紧跟协议那条）。带的是判分器
      **跑得起来的最小闭包**：`eval/{leak,score}.py` + `draft/provider.py` + 三个测试，
      外加两个不能分开提交的东西——`panel/constraints.py` 的 `secret_surfaces`
      （`leak.py` 直接 import 它）和 `openai` 依赖 + lock（`provider.py` 走 OpenAI 兼容协议）。
      提交时 `runs/` 仍不存在 → 「判分器先于被判者」从**自述**变成**可查证**。

- [x] **补空花名册洞** —— **2026-07-25 完成**：左栏花名册旁的 ＋ 打开新的
      `frontend/src/components/RosterDrawer.tsx`（建 6 类节点 + 加称呼），
      `LABEL_ZH` 从 `LeftRail` 提进 `api/types.ts` 免得抄第二份，
      新增 `useCreateNode` / `useCreateAlias` 两个 hook（成功后走 `invalidatePanels`）。
      空花名册的文案从中性的「空」改成「导入只切章、不认人 —— 建第一个」。
      端到端验过（纯 HTTP）：导入后花名册 `[]` → 建人物/秘密/别名 → 用别名「魔尊」声明 →
      `valid_from = ch1` 由引语算出 → 矩阵 `KNOWS (ch1)`。
      ⚠️ **只过了 `tsc` + 构建 + 后端 HTTP，没有人在浏览器里点过**——前端零运行时测试
      （见下方「其余」里那一项），渲染层的错会在作者手里第一次出现。

### 决定（写代码之前得先想清楚）

- [x] 作者的库默认放哪 —— **2026-08-02 完成**（ADR 0012）：**库跟着书走**，没有
      全局默认位置。一本小说 = 一个文件夹，`book.db` 住在文件夹里；
      `novel-harness` 不带参数 = 向上找最近的 `book.db`（找不到给人话）。
      `__main__.py` 已接上 `nh serve`。
- [ ] LLM key 走 BYOK 还是自建中转 → 堵着桌面版的起草
- [ ] 包名 → 2026-07-25 有意推迟，定了才谈 PyPI 注册
- [ ] CLI 要不要正式降级为「引擎的调试出口」，写进 README 一句话

### 真书（同一个前置：弄到一本中文小说 TXT）

- [ ] 切章数 = 目录数
- [x] 说话人标签覆盖率实测 —— **2026-08-02 完成，R5 已砍**：真书样本 3 章
      引语 97 条、显式标签 8 条 = **8.2% < 10%**。口径先由 ADR 0014 统一为
      「引语条数」，ADR 0005 的「实测结果」节已填。M3 规则集 = R2/R3/R4。
- [ ] 花名册 90% 提及（M1）
- [x] 误报 < 1 条/章（M3）—— **2026-08-03 完成（维护者判定）**：`111935.txt`
      第 1–20 章 R2/R3/R4 全跑 = 0 误报（干净文本，性质如实标注），
      合成考卷 25/25 证明「该响时响」。**M3 双边门槛通过，M4 解锁。**

### M2 kill-gate（两侧都已落地，等第一轮真模型）

- [x] `draft/assemble.py`（三臂 `PromptForm` 本体）—— **2026-07-30 完成**（272 行 / 17 条测试）。
      三臂**不是三套构造器**，是同一个函数的三个 `form` 取值：`_base()` 只有一份，
      X1/X2 只在它算出的那段用户消息**尾部**追加图谱段。于是「X0 是 X1/X2 的严格前缀」
      不是一句自觉，是 `x1[:-1] == x0[:-1]` 加一条尾部拼接断言。
      两个当时定下的取舍：**① 图谱段追加在同一条 user 消息里**而不是另发一条——
      连续同角色消息在部分 OpenAI 兼容端点上直接 400；
      **② `DEFAULT_HOUSE_STYLE` 里不许出现「秘密/不知道/泄露/剧透/伏笔/设定」**，
      因为它三臂共用，混进任何一个这类词都等于给 X0 也上了半条约束，Δ 会被压小
      （有一条关键词集合测试守着这件事）。
- [x] `draft/context.py` —— **2026-07-27 完成**（`c292da3`，205 行 / 12 条测试；
      **勾是 2026-07-30 才补的**，那几天只改代码没回看板，于是看板骗了三天人）。
      **先把这一条从 `assemble.py` 那行拆出来**：一条项捆两个文件，做完一半既打不了勾
      也没法记账。做法是把两条原本靠纪律维持的事编码进 `ResolvedConstraints` 类型——
      「cast 已解析」（不调 `require_resolved_cast()` 就拿不到这个类型，退化态在类型里
      表示不出来）和「X1/X2 吃同一份认知矩阵」（矩阵挂在这个类型上，反混淆铁律不再
      依赖 runner 记得只算一次）。
- [x] `confound_lint` —— **2026-07-30 完成**（234 行 / 25 条测试）。X1 与 X2 除「清单 vs 散文」
      外不许有第二处差异：人名集合差（纯 `in` 判断，**不上分词器**——分词器要认「这段话在说谁」，
      那是语义判断，ADR 0005 禁止）+ 可见字符长度比。
      长度那条的分母选了**更严的对称读法** `max/min ≤ 1.15`，不是以 X1 为基准的非对称读法——
      非对称读法会让「X2 比 X1 长 15%」放行而「X1 比 X2 长 15%」拦下，
      同一件事换个臂名结论就变，跟修正案 3 修掉的 `max(Δ1,Δ2)` 平局 bug 是同一个形状。
      空态（任一臂为空、`known_names` 为空）一律判 `ok=False`：检查没跑成不算通过。
- [x] `synth/`（合成小册子 + 自检）—— **2026-07-30 完成**。12 章正文（851–1114 字/章）+
      `booklet.toml`（6 人 / 5 秘密共 8 条边界 / 3 未来 / **25 陷阱 = 15 KNOWS + 10 FUTURE**）+
      `build.py`（走**真实写入链**落库再派生 `ground_truth.json`）+ `leak_selfcheck.py`。
      **旋钮只有一个，就是 `prior` 的显隐程度**（修正案 4 裁定 A 给的自由度）。
      15 条 KNOWS 统一三句式：上场收尾（无 tell）→ tell 出现一次，视角是叙述层或另一个
      已知情的人 → 收在「目标角色不在场 / 没听见」。两个刻意取舍：
      **tell 不放最后一句**（紧挨续写点的那句最粘手，会把 X0 顶过 0.90 天花板）、
      **末句点明目标角色的无知**（既是裁定 A 的原文要求，又把强度往回收半档）。
      FUTURE 那 10 条照裁定 C 从严：prior / goal / reference 里一个 tell 都没有。
      **归因不许糊**：`must_not_reveal` 在多数场次是全部 5 条秘密（多数角色对多数秘密
      没有边 → UNKNOWN，这是引擎的正确答案不是退化值），而 `knows_violation` 是**整场**判定，
      所以逐条核过「每条 KNOWS 的 prior 恰好含且只含 target 的那一个 tell」（15 条全部 `{own:1}`）。
      验收：`tests/test_synth_artifact.py` 5 条——协议形状 / `tell_aliases == secrets` /
      自检全绿 / 没有一条陷阱派生出空禁忌集 / **25 × 3 臂 × 3 次整轮桩跑通且 `confound_ok=True`**。
      接线烟测（桩客户端，**未落盘，`runs/` 至今不存在**）：让 X0 逐字节回抄 prior 时
      KNOWS 泄漏 1.00 / X1 0.00 —— 仪器通电了，两个极端都表达得出来。
      ⚠️ `leak_selfcheck.py` 要用 `uv run python -m synth.leak_selfcheck` 跑，
      直接 `python synth/leak_selfcheck.py` 会 ImportError（它和 `build.py` 共用一份 schema，
      刻意不加 `sys.path` hack）。
      **2026-08-01 补证据守卫**：TOML 与 ground truth 的 7 个源字段逐项一致才放行，
      不再只比 id/kind/chapter/target；`tests/test_synth.py` 现有 28 条。
- [x] runner（传入 `out_path` 的产出者；`nh gate` 默认写 `runs/`）——
      **2026-08-01 收口**（418 行 / 40 条测试 + `nh gate`）。
      四条硬约束都编码进代码而不是注释：**① 一条陷阱只调一次 `scene_view()`**，
      `.constraints` 喂判分、整个 view 喂起草，于是「prompt 里的事实」和「判分用的禁忌集」
      是同一个对象算出来的，不是两次碰巧一致的查询；
      **② `config=None` 显式判死**——它会走 `from_env()` 现读环境变量，
      中途谁 `export` 一下 `NH_LLM_TEMPERATURE` 三臂就不再是同一次调用的三个取值，而没有东西会红；
      **③ 完整 `messages` 原样进 jsonl（`ensure_ascii=False`）**——「tell 漏进 prompt」是这套
      仪器最贵的错误，而发现它的唯一办法是人去读存下来的 prompt 原文。
      **④ 结果文件独占新建**——已有 `runs/*.jsonl` 在第一次模型调用前拒绝且一字节不改；
      没有 `--force`、append 或续跑语义，避免两轮证据混在一起。
      落点也是被钉死的：`tests/test_draft_boundary.py` 写明 runner 必须住在 `eval/`，
      落到 `cli.py` 或顶层 `synth/` 就绕过整堵墙——它是**同时碰两侧的唯一一段代码**。
- [x] `score.decide()`（FLOOR/CEILING 门 + 符号稳定 + `MIN_DISCORDANT` 裁决表）——
      **2026-07-27 完成**（`df99cc1`，`eval/score.py:349`，+18 条测试，620→638；同样是
      2026-07-30 才补的勾）。裁决表**按序判**，而顺序本身是预注册的一部分：地板门排最前，
      因为「陷阱没咬住」和 KILL 长得一模一样（三臂泄漏率都低、Δ≈0、p 不显著），
      顺序反过来会让一个陷阱造得太软的实验直接砍掉一个本来对的项目。
- [x] `tests/test_draft_boundary.py` —— **2026-07-27 完成**（`9a84eb2`，11 条测试，609→620）。
      做法比原计划宽一格：不只钉 `eval/` 不自建禁忌集（EVAL_PROTOCOL §3 点名的那条），
      还钉了**墙的另一面——`draft/` 永不拿 tell**，而那一面原来没人写过，后果却更重：
      `assemble.py` 顺手调一次 `secret_surfaces()` → X1/X2 命中自己写进 prompt 的词 →
      Δ 翻负 → 裁决表读出**一个假的 KILL**，全程没有任何东西会红。
      两侧规则**故意不对称**（draft 能看矩阵不能看 tell，eval 反之）——那正是
      「检测器命中的 tell」与「prompt 里的标签」两集合天然不相交的机械保证。
      实弹验过：往真目录种三个违规文件，三条守卫分别红并指到行，删掉回绿。
      判据选择都为了不误报：AST 不用 grep（leak.py 的 docstring 里写满了被禁的名字）、
      `.resolve(` 只在带位置参数时算（`Path(...).resolve()` 满仓都是）、`props` 只认属性访问。
- [x] ADR 0010 —— **2026-07-30 完成**（Writer 边界 D1–D6，**先于 `assemble.py` 定形**）。
      非写不可的理由在第 4 道守卫自己的 docstring 里：它明说拦不住「完整 PLANNED 进 prompt」
      （那要语义判断），那一格只有这份 ADR 和 review 守得住。
      六条里 **D6（`cast` 三臂都有）是唯一影响实验结果的构造裁定**，
      所以 ADR 里连「这条裁定往哪边错」一起写了——改它要重造小册子并重跑整轮。
- [ ] **ADR 0009 不在这份清单上，而且现在不该写。** 协议 §8 定死它「跑完写」，
      在有 `runs/*.jsonl` 之前写裁决正是预注册要防的那件事。`docs/adr/README.md` 里
      那一行已经这么标了。**别把它当成一个漏掉的待办。**
- [x] **额外多出来一份：[EVAL_PROTOCOL 修正案 4](../docs/EVAL_PROTOCOL_AMENDMENT_4.md)**
      —— **2026-07-30 完成（2026-08-01 补勾）**，先于 `synth/` 与首个 run 定形。
      写 `booklet.toml` 之前推演「X0 到底怎么可能
      泄漏」，推不出来：tell 是唯一生造的专名，X0 的 prompt 里只有 house-style + `prior` + `goal`、
      零图谱事实，若 `prior` 一个字不许提 tell，X0 物理上够不着那个字符串 → 泄漏率恒 ≈ 0 →
      撞 §6 第一行地板 → **永远 INVALID**。裁定改读成「不许让**目标角色**已经知道它」。
      它是四份里**第一份往对本项目有利的方向裁的**，所以 ADR 0009 引用它时要连这一点一起写。
- [ ] 跑 kill-gate，出结果 —— **唯一剩下的一件事**，要一个 endpoint 和一笔预算

### 其余（不挡路，但欠着）

- [x] 前端 smoke test —— **2026-07-25 完成**，但做法比「smoke test」重：光渲染不报错堵不住
      这条缝（真问题是后端出参改了前端静默坏，而 `tsc` 看不见后端、`types.ts` 是手写的）。
      所以**fixture 不手写**：`tests/test_frontend_contract.py` 从真 app dump 21 个端点的真出参，
      规范化掉 ULID/时间戳/路径后冻在 `frontend/src/__fixtures__/api.json`，组件测试吃同一份。
      两头各有守卫，**实测两侧都会红**（把 `since_chapter` 改名一试：pytest 报「出参对不上」+
      指路 `NH_UPDATE_FIXTURES=1` 和 git diff；vitest 报组件渲不出那一格）。
      vitest 18 条 + pytest 契约 1 条，CI 加了 `frontend` job（`npm run build` + `npm test`）。
      **剩下的**：只有 3 个组件有测试，`CenterEditor` / `LocalGraph` / `DeclareDrawer` 等仍是零
      —— 契约层钉住了，组件自身的渲染错还是作者先撞上。
- [ ] README 加截图 / GIF —— 全仓零张图，GUI 项目最大的传播减分项
- [x] `start.command`（macOS）—— **2026-07-25 完成**。放仓库根，因为双击的人是刚解压完
      在找「怎么开」的那个人，不会去翻 `scripts/`。做的四件事：`cd` 回脚本所在处
      （双击时 cwd 是家目录，这条不写全脚本就废）、`uv sync`、首次运行构建前端、`nh serve`。
      三个实测过的细节：**① uv 的 PATH 兜底**——双击起的 shell 常常没读到把 `~/.local/bin`
      加进 PATH 的那份配置，脚本会自己去三个常见位置找（实测：清空 PATH 后仍然跑起来了）；
      **② 报错停在窗口里等回车**——`.command` 窗口一 exit 就关，报错会一闪而过；
      **③ 没有 uv 时退出码 1**，且给的是一行可以直接粘的安装命令。
      顺带把 `*.db` 和 `books/` 加进 `.gitignore`——**作者的库里是他没发表的小说**，
      而这个脚本会在仓库根建 `book.db`，clone 了仓库来写书的人很容易连稿子一起 commit。
      **Windows 的 `.bat` 没做**：手上没有 Windows 机器，一个没跑过的启动脚本比没有更糟
      （它会在第一次双击时失败，而那正是唯一一次机会）。
- [x] `text/mentions.py` —— **2026-08-02 完成**（`95dac6f`）：正则 alternation 最长优先，
      `Located` 锚复用；R2/R3 已进 `ALL_CHECKS`，M3 合成门槛 25/25 通过。
      剩 R5 的前置是「覆盖率实测」，不是代码。

---

## 一句话

**引擎基本完工，产品还没接上，而且一行代码没在真书上跑过。**

这三段是**独立的**，别用一个百分比概括——按引擎算完成度很高，按产品算卡在两个洞上，
按验证算接近零。混在一起看会得出一个假的进度感。

## 分层现状

| 层 | 规模 | 状态 |
|---|---|---|
| `graph/` 时态图 | 3236 行 | ✅ 完工。`state_at` / supersede / 闭开区间全系统只实现一次 |
| `migrations/` | 545 行 / 13 张表 | ✅ 完工 |
| `text/` 切章·锚·场景 | 908 行 | ✅ 代码完工 ⚠️ 真书没验 |
| `declare.py` 声明层 | 522 行 | ✅ 完工。引语→章号那条链是全部差异化的地基 |
| `panel/` 认知边界 | 532 行 | ✅ 完工。**头牌** |
| `checks/` 一致性 | 294 行 | 🚧 **只有 R4 一条**——不是落后，是 ADR 0005 划完铁律后剩下的 |
| `cli.py` | 1168 行 / 15 命令 | ✅ 完工（今天新增 `nh serve`） |
| `api/` FastAPI | 975 行 / 27 路由 | ✅ 完工 |
| `frontend/` React | 2204 行手写 / 23 文件 | 🚧 主结构在，缺一个关键入口（见「挡路」） |
| `draft/` 起草 | 159 行 | 🔴 **只有 provider，三臂本体一个字符没有** |
| `eval/` 判分 | 202 行 | 🟡 判分器齐了，但**没有被判的对象** |
| `extract/` 抽取 | 0 | ⬜ M4，完全不存在 |

## 按里程碑

```
M0   骨架 + 打包链路 + 数据层        ✅ 代码全绿  ·  ⏳ 真书验收 0/3
M1   声明层 + 认知边界面板            ✅ 已落地（终端）
M1.5 FastAPI + React 工作台           🚧 ~85%，缺一个洞
M2   合成小册子 + 起草 + kill-gate    🚧 ~30%（判分器先于被判者）
M3   规则 R2 / R3 / R5                ⬜ 三条共同缺 `text/mentions.py`
M4   增量抽取                         ⬜ 零
M5   v1.0                             ⬜
```

---

## 「还没有」要分三类，混在一起会误判进度

这是这次盘点里最有用的一条区分。

### ① 代码真的没写 —— 可以直接开工

- `draft/assemble.py`（三臂 `PromptForm` 本体）、`draft/context.py`
- `confound_lint`、整个 `synth/`（合成小册子 + 自检）、runner（`runs/*.jsonl` 的产出者）
- `score.decide()`（FLOOR/CEILING 门 + 符号稳定 + `MIN_DISCORDANT` 的完整裁决表）
- 第 4 道 arch-guard `tests/test_draft_boundary.py`
- ADR 0009 / 0010

**最后那条守卫特别值得点名**：`eval/leak.py:11` **已经在宣称它钉死了边界，但它不存在**——
按这个仓库自己的判据，一个不存在的守卫比一个永远绿的守卫更糟。

### ② 代码写了但没验 —— 不是写代码能解决的

**三条真书门槛全部卡在同一件东西上：手上没有一本真实中文小说 TXT。**

- 切章数 = 目录数（现在只有手写的 46 行 5 章 fixture）
- 显式说话人标签覆盖率 ≥10%？—— **决定 R5 的生死**，ADR 0005 的「实测结果」一节还是空的
- 花名册 90% 提及（M1）、误报 < 1 条/章（M3）

**合成小册子不能冒充这三条中的任何一条**：它有强制说话人标签、强制唯一 tell，
测的是注入机制不是真书行为。

### ③ 决定没做 —— 写代码之前得先想清楚

- **作者的库默认放哪** → 堵着 `uvx novel-harness`（那个默认位置发出去就很难改）
- **LLM key 走 BYOK 还是我的中转** → 堵着桌面版的起草功能
- **包名** → 今天决定推迟（见另一份）
- **CLI 要不要正式降级**为「引擎的调试出口」，不再承诺和 GUI 功能对等

---

## 挡在路上的只有两件事，其余都是加法

### 1. 空花名册洞（今天就能做，立刻见效）

后端 `POST /api/projects/{pid}/nodes`（`api/app.py:708`）与 `.../aliases`（`:744`）**都在**，
前端**零调用**；而导入 TXT 只切章、不抽实体（ADR 0004 是有意的）。

于是首次导入一本书之后花名册是空的 → declare 写闭环里的「填称呼」必然 `UnknownName` 404
→ **认知矩阵 / 约束 / declare 三样头牌全部起不来**。作者今天仍得回终端 `nh declare character` 起头。

这是「非程序员能用」这条标准上最硬的洞，也是唯一一个**今天就能做、且立刻见效**的。

### 2. M2 kill-gate 没跑（决定项目定位）

整个项目押的那个宝——**「提前告诉 AI『这个角色现在还不该知道 X』，AI 就不会写漏」**——
**到今天为止没有人验证过它是不是真的**。

协议已经冻在 `docs/EVAL_PROTOCOL.md`，但**它还是 untracked**。
而那份文件第 11 行自称「先 commit 的 git 时间戳是预注册成立的**唯一**证据」——
**在它进 git 之前，之后任何 `runs/*.jsonl` 都不具备预注册效力。**

**这是全项目最便宜、且最紧急的一步**：一次 `git add` + `commit`，几秒钟，
但它决定后面所有实验算不算数。拖着不提交等于把未来所有实验的效力挂在空中。

---

## 数字（2026-07-25 实测）

```
引擎          8981 行 Python
前端          2204 行手写 TS/TSX（另有 1189 行生成的 schema.ts，不入库）
测试          608 个，全绿，2.2 秒跑完
ADR           8 份
心跳          demo.sh 绿
分发          nh serve 可用 · wheel 带工作台 · PyPI 有意推迟
```

**测试分布值得看一眼**，它暴露了一个梯度：

```
test_store_conformance.py   78     ← 数据层/图层最厚
test_migrate.py             64
test_scenes.py              60
test_canon_writer.py        36
test_api.py                 32     ← 到 HTTP 边界就薄了
test_serve.py                7     ← 今天新增
frontend/                    0     ← 越靠近作者越薄
```

**「错了最贵的地方测得最厚」是对的**（图层错了会静默给出错的认知矩阵，那是产品核心主张破功）。
但**前端那个 0 是当前唯一真正的双份维护成本**：引擎 → CLI/HTTP 两个薄壳是加法
（`api/app.py` 第一行的纪律：「只把引擎函数包成 HTTP，不装业务」），
而前端 ↔ 后端契约是乘法。CI 现在会跑 `tsc -b` 所以类型错会红，但语义改了照样静默坏。

---

## 我当时的判断

1. **别按「完成度百分比」安排工作。** 引擎那 8981 行已经过了最难的部分（时态图 + 声明层），
   剩下的大头不是代码量，是**两个洞和三条没验的门槛**。再写 3000 行引擎代码，
   离「作者能用」也不会更近。
2. **最短路径是先做那两件挡路的**，且顺序建议是先 commit `EVAL_PROTOCOL.md`（几秒），
   再补空花名册洞（前端调两个已有路由，工作量小、见效立刻）。
3. **真书这件事被低估了。** 三条门槛卡在同一个前置上，而那不是技术问题——
   是「去弄一本 TXT」。它现在挡着 R5 的生死判断，也挡着切章器的唯一一次真实验收。
4. **M2 判负是被允许的结局。** README 里写着「砍掉 AI 起草线，定位改成作者的记忆外挂」。
   所以在 kill-gate 出结果之前，别把桌面壳、签名、API key 入口这些**只对起草线有意义**的东西做掉。

## 这份快照什么时候作废

任何一条发生就该重写，别在旧数字上打补丁：

- 空花名册洞补上（M1.5 就真完工了）
- `EVAL_PROTOCOL.md` 进 git（预注册成立，M2 才真的开始）
- 拿到真书 TXT（三条门槛同时解锁，ADR 0005 的空白节会被填上）
- M2 kill-gate 出结果（判负的话上面一半内容都要改写）
