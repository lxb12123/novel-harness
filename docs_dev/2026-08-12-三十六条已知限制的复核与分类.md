# 2026-08-12 三十六条已知限制：逐条拿代码核实 + 分类

> **这份不是权威。** 同 `docs_dev/` 里其它文件：它记的是维护者 2026-08-12 这一天
> 拿代码核对出来的结论，**不是工程约束**。架构事实的唯一副本在
> [`docs/ARCHITECTURE.md` 的「当前状态」](../docs/ARCHITECTURE.md#当前状态)，
> 冲突以那边为准；要把下面任何一条变成约束，得落进 `docs/` 或开一份 ADR。
>
> **它也不改任何东西。** 这是一次勘察：一行产品代码没动、一条测试没加、
> [进度板](2026-08-10-模式二与事后可查可改的进度板.md)一个字没改
> （那份 630 行的板子等着整份重写，现在动它会撞）。

## 这次核的是什么、怎么核的

进度板「已知限制（不假装解决了）」那一节今天有 **36 条**，是过去三天陆续记下来的。
**其中有几条已经被悄悄修掉而没人回来划掉**——就在今天，「按停中断不了正在跑的起草」
被发现时，它描述的东西已经修好了。

所以这不是整理，是核实：**每一条都去代码里找证据，不许凭板子上的措辞判断。**

### 快照点（重要）

- 核实基准 = **`2bcc3e1`**（`feat: 用量汇总不再把「不知道」显示成 0 + ADR 0024`）。
- 核实期间**有另一个 workflow 正在改 `src/novel_harness/agent/{loop,tools}.py`**
  （工作区脏，`+494 / -2`，正在加第 10 条工具 `ask_author`；中途一度 import 都坏着，
  `tools.py` 的 `model_validator` 未定义 ⇒ 那会儿 24 个测试文件收集失败）。
  **凡引用这两个文件的行号，一律以 `git show HEAD:…` 为准**，且下文逐条标了
  「（HEAD）」。**没有把在途改动记成「已过期」。**
- `uv run pytest -q tests/test_doc_numbers.py` → **12 passed**（本次改动没弄红它；
  `docs_dev/` 本来就被那条守卫显式排除，见 `tests/test_doc_numbers.py:509`）。

---

## 四类计数

| 类 | 条数 | 判据 |
|---|---:|---|
| **① 已过期** | **9** | 已经修了，或前提没了。**板子在说假话** |
| **② 仍然成立，该修** | **10** | 带粗排序 |
| **③ 仍然成立，有意不做** | **9** | 理由今天仍站得住（逐条重新验过，不是抄当初那句） |
| **④ 说轻了** | **8** | 实际比板子写的更严重，或范围更大 |
| 合计 | **36** | |

**分类是「主导性的那一条修正」**，不是唯一的一条：几条 ① 有活着的残留、
几条 ④ 同时也「该修」。逐条里都写清楚了。

---

## ① 已过期（9 条）——**板子在说假话，照它排期会做重复的活**

### 1. 「底栏的花销汇总系统性偏低，`record_call` 全仓只有两个调用方，起草在 `model_call` 里一行都没有」

**假的。** `/draft` 2026-08-12（`6b13abd`）已经记账：
`src/novel_harness/api/app.py:1276-1295`（`bill()` → `record_receipt`，`on_call` 逐次落、
不等整稿拼完），实现在 `src/novel_harness/extract/call_audit.py:145-197`。
`record_call` 今天有 4 个调用点（`call_audit.py:172` / `:207`、
`draft/rolling_summary.py:265`、`api/chat.py:460`）。

> ⚠️ **这一条和板子上的第 10、12、20 条互相矛盾**（那三条都以「起草有账」为前提）。见文末。

### 3. 「一次工具返回的天花板和整段对话的天花板是同一个数」

**不再是同一个数。** 默认预算今天是 `floor + context.return_units`
（HEAD `agent/loop.py:1166-1177`，注释明写「两个数不是同一种量」），
实测 5,571 + 17,000 = **22,571**；`return_units` 仍是 `memory_units_available()`
（`agent/ports.py:334`）。「一次满额返回就把对话顶满、第二步开始必剪」被一条断言反过来钉死：
`tests/test_context_policy.py:869`（自守卫在 `:924-925`：按被推翻的旧口径喂
`budget_units=context.return_units`，那条返回当场被剪成占位）。

**残留仍成立**：`api/chat.py:802-819` 的 `run_turn(...)` 确实**不传 `budget_units`**。

### 4. 「按停那一档回执写 `done`，界面上要留意」

**界面那一半 3.5 已经做了。** loop 侧确实还写 `done`（HEAD `agent/loop.py:1375-1380`），
但前端不按 `reason` 一个字来说话：`frontend/src/chat.ts:157 stopFootnote()`
（`if (!stopped || receipt.reason === "author_stopped") return null`）→
`ChatPanel.tsx:374` 传 `Receipt` → `:126` 渲染，`ChatPanel.test.tsx:271` 钉着
（「按了停、回执却说『说完了』—— 补一句，别让按钮看起来是坏的」）。
**没有加那个「你按过停」的位**，走的是「前端自己知道信号送达了」那条路——
出参形状一个字没改，正合板子那句「后者要改出参形状，别顺手加」。

### 8. 「`_call_detail` 把 `params_json` 原样 `str()` 出去」

**已经不出接口了，连 SELECT 都不取。** `src/novel_harness/activity.py:1020-1031`
（`_CALL_SELECT` 只选 12 列，没有 `params_json`）、`:1237-1280`（`_call_detail` 的
8 行 `DetailRow` 里没有它），`:1240-1249` 就是拆除记录。
写入方仍只有一个、仍只写两个键（`extract/call_audit.py:110-114`）；
008/009 加的两批列不进 `params_json`。**「那一列变宽的那一天要补收窄」这条前提也没了**——
它已经不在出参路径上。

### 17. 「按停中断不了正在跑的起草」（板子已划掉，划得对）

`6b13abd` 已修，`ChapterDesk.write` 是唯一设值点
（`agent/drafting.py:274-283`，`interruptible=self._cancel is not None`）。

**残留原样成立，且写在代码里**：`agent/model.py:243-251` 的 `agent_call_plan()`
调 `plan_call(AGENT_REPLY_LENGTH, AGENT_REASONING, capability)`，**不传 `interruptible`**
⇒ 对话回复那一档仍然不流式 ⇒ 聊天里按停的粒度还是「这一次调用跑完」。
`agent/model.py:28-38` 把「不许为了让它流式去抬输出预算」的理由写死了。
**ADR 0024（2026-08-12 已接受）不解这一条**：它发的是编排事件，不是模型 token 流。

### 20. 「起草那一行账在日志页上答不出『为哪一章』」

**产品档起草已经答得出。** 009 加了 `model_call.chapter_number`，
`/draft` 那条路传 `chapter_number=ctx.chapter`（`api/app.py:1291`），
读端 `activity.py:801-816 _call_chapter()` 先读列、答不出再反查。

**剩下的那一半就是第 11 条**（写作助手那一档没填）—— 两条现在说的是同一个洞。

### 23. 「预算只量对话不量工具声明（18%）；`stop_wording(CONTEXT_FULL)` 里那对 `**` 会上屏」

**两半都过期了。**
- 预算今天**量工具声明**：HEAD `agent/loop.py:363 payload_units()` 吃的是
  `{"messages": …, "tools": …}` 整份 payload，`run_turn` 投影时 `tools=declarations`。
- 那对 `**` 还在（HEAD `agent/loop.py:789`），但**前端现在渲染它**：
  `ChatPanel.tsx:61-68 <Wording>` → `frontend/src/chat.ts:201 emphasize()`
  （只认成对 `**`，落单的原样留着），测试 `frontend/src/chat.test.ts:140-142`。

> 那个「18%」的数**说轻了**，见 ④ 区。

### 29. 「`CostTotals` 的 `tokens_in/out/ms` 是 `COALESCE(…,0)` 的非空 int」

**2026-08-12（`2bcc3e1`）修掉了。** `activity.py:1133-1135`：
「**一个 `COALESCE` 都没有，那是有意的**」；`tokens_in/out/ms` 改成可空
（`activity.py:317-338`），并配了 `metered_calls`（`:308`）说清「这是不是全部」。
拆除记录写在 `CostTotals` 的 docstring 里（`activity.py:286-289`）。

### 32. 「开着的 dev server 会把新迁移应用到作者的真书上」

**机械保护已落地，且在野外真的接住过一次；那个进程也已经不在了。**
- `db.py::BACKUP_LABEL`（`src/novel_harness/db.py:56`）+ `VACUUM INTO`（`:183`）+
  先临时名再原子改名（`:187-190`）+ 拷不成就拒绝迁移（`:192-197`）。
- 实测：`/Users/lixibin/Desktop/111935/book.db.升级前备份-2026-08-12-第11版.db`
  在磁盘上（12,238,848 字节，13:50）。
- 实测：`ps aux | grep uvicorn` **零命中**——板子上那句「PID 29620，此刻还活着」不成立了。
- 实测：真书 `PRAGMA user_version = 11` == 最新迁移（`011_rule_revocation.sql`）。

**保留价值**：这一条作为「事故史」值得留在新板子上，但**不该再留在「已知限制」里**——
它已经不是一个限制了。

---

## ④ 说轻了（8 条）——**这次最该看的一区**

### 9. 「『摆出来、能取消』只做了一半：读端有了，撤销没有」

**实际是三件里只做了一件，而且做的那一件不是『读端』。**

| 动作 | 板子说 | 实际（HEAD） |
|---|---|---|
| **存**（把一条规矩写进 canonical） | 没提 | `rules.py:159 rule_message()` —— **`src/` 里一个调用方都没有**，只有 `tests/test_agent_rules.py` / `tests/test_context_policy.py` 调 |
| **升级**（同一句说第二遍 ⇒ 批级升章级，ADR 0023 决策二） | 没提 | `rules.py:331 promoted()` —— 同样**零生产调用方** |
| **按章号过期**（投影侧） | —— | ✅ 真落地了：HEAD `loop.py:620/638-647` 用 `surviving_rule_indices` / `is_rule` / `expired_rule_count` |
| **撤销** | 「没有」 | 确实没有，**但障碍已经不在了**：`011_rule_revocation.sql` 已经把 `chat_message.revokes_seq` 加上了（`b4c249d`），而板子写的是「要么加列，要么复用 `tool_call_id` 那个空串字段（脏）」 |

**实测**：`grep -rn "revokes_seq" src/ tests/ frontend/src/`（排除 `.sql`）**零命中**。
工具表 9 条里也没有任何一条能建规矩（`scene_constraints / character_state /
draft_chapter / book_index / character_chapters / chapter_summaries / chapter_text /
save_draft / read_draft`）。

**结论：今天生产里一条作者规矩都进不去**，所以「读端有了」这句话在生产上是空的。
ADR 0023 的退路押在「看得见 + 能取消」上，**今天两半都没兑现**，只兑现了「进来了的话会正确过期」。
⚠️ **在途的那个 workflow 正在改 `loop.py`/`tools.py`，接线窗口就是现在**——这条要先看一眼再排期。

### 11. 「`api/chat.py::_ledger` 的章号仍然是空的」

事实成立（`api/chat.py:460-476` 的 `record_call(...)` 没有 `chapter_number`），
**但 009 落地之后后果变了**：四个写入方里**三个已经在填**（抽取 / 章节总结 / 产品档起草），
只剩写作助手这一档不填。于是同一张日志页上**一半的行有章号、一半没有**——
「未记录」在一片有章号的行里读起来像「这一次不为任何一章花的」，而不是「我们没记」。
板子写这条时另外三个也都没填，那时它是齐的。

### 13. 「`_estimate_tokens` 量 `prompt_bytes`，含整张工具声明——**这条是预存的**」

**不是预存的，已经生效并且已经改过一次测试了。**
- 工具表**已经从 7 条走到 9 条**（HEAD `agent/tools.py`，`ADR 0022` 加了
  `save_draft` / `read_draft`）——板子描述的那个实验（「把工具表从 7 加到 9」）**就是今天的产线**。
- 那条测试**红过并被抬了阈值**：`tests/test_agent_loop.py:182`，
  `:186-189` 的注释是拆除记录（「表里每多一条工具，这儿就更容易假红一次
  （ADR 0022 加了两条之后 10,000 当场不够）」），`max_tokens` 从 10,000 抬到 **60,000**。
- 判据本身没变：HEAD `loop.py:1229-1232` 仍走 `_estimate_tokens(receipt.prompt_bytes…)`，
  而 `prompt_bytes` 来自 `loop.py:1015-1029 _prompt_digest(messages, declarations)`——
  **声明一起进字节**。
- ⚠️ 在途那次改动要加第 10 条（`ask_author`），**这一档会再紧一次**。

### 16. 「起草留在对话里的那份正文没有过期检查（`DraftResult` 验不过 `ChapterFullText`）」

**旧的那个洞缩掉了大半，而且仓库自己知道**：ADR 0022 之后
`DraftResult`（HEAD `agent/tools.py:257`）**没有 `text` 字段**了，进对话的只剩
id + 定长预览 + 自述；`tests/test_draft_landing.py:862-880` 就是钉这个新形状的
（标题直译：「起草留在对话里的那一份不被检查」，正文写着「2026-08-12（ADR 0022）
这个洞缩掉了大半……钉的是今天的形状，不是想要的形状」）。

**但同一次改动开了一条新的全文通路，板子和那条测试都没提**：
`read_draft` 的出参 `DraftFullText`（HEAD `agent/tools.py:340-366`）带**整章正文**，
一样进 `chat_message`、一样每一轮重发（`tests/test_draft_candidates.py:295-306` 实测
`whole_chapter in json.loads(full.content)["text"]`）。
而 `outdated_manuscript`（HEAD `agent/tools.py:871-917`）的判据是
「验不验得过 `ChapterFullText`（`extra="forbid"`）」——`DraftFullText` 的字段集不同，
**同样认不出**。

诚实说清楚这条比旧的那条**轻在哪**：候选表里那一稿是不可变的，它不会像磁盘正文那样
「变了」；真正会错的是它随身带的 `landed` 那一位（稿子落盘之后作者又改了那一章，
模型手里那份仍写着 `landed=true`）。**范围变大是真的，严重度不同也是真的。**
边界五那一半反倒是补上的：`_answered_chapter`（HEAD `agent/tools.py:785-797`）
专门为这两条只带稿号的工具取出参上的 `chapter`，`tests/test_draft_candidates.py:309` 钉着。

### 26. 「`GET /runs` 不走 `_with_jump`，今天无害只因为 `BottomBar.tsx` 不渲染 jump」

**两处都比板子写的重。**

1. **「无害的理由」挂在了一个根本不消费这个端点的组件上。**
   `frontend/src/components/BottomBar.tsx` 全文件 grep `jump|runs|RunsPanel` **零命中**
   （它只用 `useScenes` / `useCharacterState` / `useRoster`，`:1-2, 91-107`）。
   `/runs` 唯一的生产消费者是 `ActivityLog.tsx:151` 的 `UsageStrip`，
   它今天只读 `data.totals` 和 `data.run_count`，**从不碰 `data.entries`**。
   所以板子那句「谁哪天让底栏渲染 jump，它就当场骗人」是一个**永远不会触发的守卫**——
   真正的引信在 `UsageStrip`。
2. **那句假话在一档上是真的假话，不是「本来就空」。**
   `_run_jump`（`activity.py:727-748`）在**恰好一条待审提案**时给的是
   `JumpTarget.PROPOSAL` + `proposal_id`，而 `_endpoints`（`api/activity.py:47-73`）
   对这一档**本来会给三条真路由**（accept / reject / edit）。
   `/runs` 跳过 `_with_jump`（`api/activity.py:192-215` 直接 `return read_runs(...)`）
   ⇒ 那一档的 `endpoints` 是空的 ⇒ 按本仓契约就是在断言「今天没有任何路由能改这个」。
   （`JumpTarget.CHAPTER` 那一档确实本来就空，`_endpoints` 的 docstring 写死了理由。）

### 27. 「屏幕守卫罩不住小写裸枚举值」

洞和自认测试都在（判据文件 `frontend/src/test/screenGuard.ts:22-32`；
自认那条断言在 **`frontend/src/components/DevTerms.guard.test.tsx:338-347`**，
不在 `screenGuard.test.ts` 里——找的时候会扑空）。

**说轻在**：`alive`（`HealthValue`）**既不在 `screenGuard.ts:24-25` 那份自述清单里，
也不在那条测试的六个探针里**（探针是 `author / system / failed / extraction /
accept / extractor`）。也就是这个洞**连自己承认的范围都比实际窄**——
而板子那句「有一条测试钉住『它必须继续自己承认』」会让人以为清单是齐的。

### 28. 「`/matrix` 的 unresolved 横幅措辞对歧义称呼是错的」

**不是一处，是三处；而且有两处根本不是 `/matrix`。**
同一句措辞在 `KnowledgeMatrix.tsx:301`（`/matrix`）、
`RightPanel.tsx:67`（走 `useConstraints`，`:37`）、
`ChapterPrepPage.tsx:113`（同样是 `/constraints`）各躺一份。
后端确认歧义**进** `unresolved`：`src/novel_harness/panel/constraints.py:57`
（「**歧义（「师兄」→ 8 个人）和查无此人都进 `unresolved`**」）。
另：`KnowledgeMatrix.test.tsx:109` 把这句措辞钉住了，改的时候要一起改。
**这正是本仓反复栽的那个病（一份措辞三处拷贝），只是这次躺在前端。**

### 35. 「R2 / R3 在作者手里仍然是死的」

事实成立且证据齐（`first_appears_chapter` 在 `src/` 里只被读：
`panel/constraints.py:306,319`、`checks/future_leak.py:34`、`checks/dead_speaks.py:82`、
`agent/index.py:371,388`、`api/app.py:163`；`DeclareNodeBody`（`api/app.py:918-926`）
没有这个字段，`app.py:983` 调 `declare_node` 时不传 `props`；
`value_key` 的 6 处 `EdgeProps(` 构造无一传它，抽取器写 `HAS_STATE` 时只给 `value`，
`extract/service.py:356`；前端 `DeclareDrawer.tsx:36-42` 只发 where/believes/knows）。

**说轻在**：板子写这条时模式二还没有工具表。**今天工具表 9 条，一条都写不了节点或边的 props**
（全是读 + 起草）。也就是**模式二整个落地之后，这个 capability gap 一毫米都没动**——
而「让作者能声明『血枭盟第 200 章才出现』」正是 R2 唯一的开火条件。

---

## ② 仍然成立，该修（10 条）——粗排序：谁挡着谁 / 谁最伤作者

> ④ 区里的第 9、11、13、16、26、28、35 条同样「该修」，只是那几条的主导修正是「说轻了」。
> 真排期时应该和下面这些放在同一张表上。

**先做（挡着别人 / 改动最小 / 作者马上撞得到）**

1. **第 21 条 · 会话的 `house_style` 到不了起草那一稿。**
   `agent/drafting.py:297-301` 的 `ChapterDraftRequest(...)` 只传 `goal / length /
   previous_tail`，**没有 `house_style`**；而它在 `chat_session` 上是有的
   （`agent/store.py:116-125` → `loop.py:318-322` 进 prefix）。
   **对话面板 2026-08-12 才落地，作者第一次用就会撞上「agent 起的稿不跟我的文风」。**
2. **第 15 条 · 版本历史按 actor 过滤不成立。**
   `graph/models.py:1026-1041` 的 `ChapterSnapshot` 上没有 actor 列。
   关联键**是存在的**：落盘那一行写了 `payload["text_sha256"]`
   （`agent/drafting.py:661-667`），和快照的 `text_sha256` 对得上。
   ⚠️ **板子给的紧迫性理由已经过期**：「一轮最多 6 稿」是 ADR 0021 的形态；
   ADR 0022 之后落盘是模型的一个**显式动作**（`save_draft`，而且是并发屏障），
   不再是起草的副作用。**洞还在，被淹的速度慢下来了。**
3. **第 10 条 · 可中断换掉了起草的 token 计数。**
   实测注册表 8 条路由，`supports_stream_usage is True` 的只有 OpenAI 那 4 条
   （DeepSeek 两条 / Anthropic 兼容 / OpenRouter 都是 `None`）；
   理由写死在 `draft/capabilities.py:747-753`。
   **板子给的三条路里，③（先修底栏那个 `COALESCE`）已经执行了**（见 ① 第 29 条），
   所以「一个诚实的空在屏幕上变成一个撒谎的零」这句已经不成立、
   「它有了第二个受害者」这个加速理由也没了。**剩下的是 ① / ② 之间还没裁的那一次。**
4. **第 19 条 · agent 落盘会静默抹掉那一章里作者标的场景块。**
   `agent/drafting.py:588` `candidate = importer.chapter_text(head.raw_heading, text)`
   ——只保住章标题那一行，`## 场景 N` + `<!-- nh: cast=… -->` 一律不留。
   方向仍然安全（退回从正文推 = 超集 = 多禁），进了快照、退得回来；
   但落盘回话（`:645`）只说「章标题保持原样」，**一个字不提场景块**。

**再做**

5. **第 24 条 · 滚动摘要会过期，而 `coverage()` 只问「有没有」。**
   `ChapterSummaryStatus` 只有四个字段（`draft/rolling_summary.py:63-66`），
   `coverage()`（`:115-163`）全程不碰 `prompt_hash`。
   mtime 兜底那一层确实在（`agent/index.py:230-242 _rewritten_since` +
   `may_be_stale:836` / `stale_checked:883` / `stale_total:890`）。
   彻底做法（`coverage()` 里算 `stale` 纯量）会一路动到前端契约。
6. **第 18 条 · `TurnLimits.max_tokens = 300_000` 该重裁一次。**
   HEAD `agent/loop.py:810 max_steps=8` / `:813 max_tokens=300_000` /
   `:823 max_calls_per_step=6` ⇒ 上界 **8×6 = 48 次工具调用/轮**，
   最贵那一档全是 `draft_chapter`（每次一稿正文）。那个 300,000 确实没被重裁过。
7. **第 2 / 22 条 · `ProviderError` 那一路没有账，`steps` 也不计。**
   HEAD `agent/loop.py:1332-1337`：`except ProviderError` 直接 `return finish(...)`，
   **走在 `:1339 steps += 1` 和记账那一段之前**。方向偏低，和 `/draft` 那个洞同向。
   ⚠️ **这两条是同一件事，22 是 2 的超集**（见文末）。
8. **第 25 条 · `FakeEvents` 比真库宽。**
   `tests/test_agent_index.py:387-401`：`chapter` 只被记进 `self.calls`（`:394`），
   过滤条件（`:396-401`）只有 `wanted & (participants | knowers)`，**没有章号比较**；
   真库那边 `chapter` 会进 `TEMPORAL_WHERE`（`graph/queries.py:254/258`）。
   断言（`:447-448`）只核对坐标，没核对过滤效果。
9. **第 31 条 · 只改 `believed_value` 不翻类型撞同一个幂等键。**
   幂等键 = `(project_id, src, dst, type, valid_from_chapter, information_scope)`
   （`graph/queries.py:739-761`）——**props 不在键里，且那条 SELECT 不过滤 `status`**，
   刚 RETRACTED 的同一身份边也会被认成「重跑」，走
   `graph/sqlite_store.py:443-448`（`update_edge_facets`，`created=False`）。
   **今天走不到那儿只因为入口先拒**：`corrections.py:320-323`
   （`if not wrong: raise CorrectionRefused(…)`）。所以这是一条**被入口挡着的**结构问题。
10. **第 30 条 · 改 CANON 事件的 `summary` 还做不到**（schema 裁决，见 ③ 也可）。
    `UNIQUE (project_id, evidence_id, information_scope)` 还在
    （`migrations/002_m4_events.sql:38`，`grep story_event migrations/*.sql` 除 002 外零命中）；
    `corrections.py:564-570` 是同一条限制的第二处记录。
    队列路径（还没升 CANON 的）照旧能改（`api/review.py:104,381` 的 `edited_summary`）。

---

## ③ 仍然成立，有意不做（9 条）——理由今天重新验过

### 5. 「占位必须发生在写库之前」
**这不是一条限制，是一条已兑现的纪律。** 顺序还是「占位 → 读 → 写」：
`api/chat.py:730-735`（注释「**先占位，再读、再写。**」）→ `:735 LIVE.begin(...)`
→ `:746 _load(...)` → `:754 _append(...)`；断言两边都在
（`tests/test_chat_api.py:532`，`:559 chat_busy` / `:563` 200 / `:564-567` 历史必须是 `["甲","好"]`）。
**建议在新板子上把它挪出「已知限制」**——它是一条纪律，不是一个洞。

### 6. 「`_Running` 是进程内的」
`api/chat.py:129`（`self._live: dict` + `Lock`）、`:194 LIVE = _Running()`。
**理由没被自己推翻**：迁移 010 只加 `draft_candidate.stopped_reason`、
011 只加 `chat_message.revokes_seq`，`006_chat_session.sql:5` 反而明写「这里没有 status」。
2026-08-12 每轮多了个 `run_id`（`chat.py:435`，`:425` 明写「**它不进任何一行数据**」），
仍然只在进程内。

### 7. 「`model_call.cost` 至今没有写入方」
`extract/call_audit.py:119-123` 的 `INSERT INTO model_call (...)` 列表里**没有 `cost`**。
BYOK 之下引擎不知道作者签的什么单价，`priced_calls`（`activity.py:301`）
把这个「零」的理由一起发出去。理由今天更硬了，因为 29 条修完之后
`CostTotals` 上「计数 + 可空合计」已经是一对成型的形状。

### 12. 「`/draft` 记账失败会把 200 变成 500」
`extract/call_audit.py:169-197`：`record_receipt` 里 `except BaseException: conn.rollback(); raise`，
docstring 明写「**失败不吞**：账记不上的那一次调用已经花过钱了」。
改动点仍然只有那一个 `try`。

### 14. 「`messages` 里的裸 SYSTEM 消息从此有了含义」
**比板子说的还更「只有一个写入方」**：全仓 `Role.SYSTEM` 只有 4 个写入点
（`loop.py:320` / `:322` / `rules.py:356 promoted()` 三处都进 **prefix**；
`rules.py:176 rule_message()` 是唯一进 `messages` 的），
`loop.py:266-271` 的 validator 还拦着 prefix 里放非 SYSTEM。
判据是**结构**不是内容（`rules.py:144-157 is_rule()`），静默消失在 `loop.py:640-647`。
（⚠️ 参见 ④ 第 9 条：那个唯一的写入方今天在 `src/` 里连一个调用方都没有。）

### 27 / 28 / 35 见 ④ 区。以下是剩下的四条：

### 33. 「卷级摘要没有」
`grep -r "卷级|volume_summary|VolumeSummary|arc_summary"` 全仓（含 sql/ts/tsx/md，
排除 `node_modules`）唯一命中是进度板自己那一行。摘要粒度只有一章
（`chapter_summary` 表 + `SummaryStore`）。722 章今天装得下，理由成立。

### 34. 「`knowers` 是推断不是文本事实」
**风险陈述仍然成立，而它指名的退路今天已经全兑现到浏览器里了**（板子写这条时还没有）：
后端 `corrections.py:424 correct_event_cast`（`knowers_added/removed` 在 `:161-162`）→
路由 `POST …/canon/events/{event_id}/cast`（`api/review.py:209-235`）→
hook `frontend/src/api/hooks.ts:648-664 useCorrectEventCast` →
组件 `CanonEventCast.tsx:99` → 挂在 `ProposalReviewTab.tsx:319`。
**「不做事前批准」这个决定今天比当初更站得住**，因为退路是真的通的。

### 36. 「MCP 不做」
理由今天更硬：工具表就是权限边界，而那张表已经 9 条并且还在长
（在途在加第 10 条）；边界一「不可回收」刚被 ADR 0024 的「代价」一节再点了一次名
（`docs/adr/0024-…md:102-110`）。反方向（把 novel-harness 做成 MCP server）仍撞
`2026-08-04-正文真相源在磁盘与外部审计的边界.md`。

### 30 也可以读成 ③（schema 裁决不是一下午的活），已列在 ② 第 10 位，不重复计数。

---

## 顺手发现的、板子上一条都没记的

### A. 🔴 ADR 0024 的两条「代价」板子上一条都没有，而那一刀**正在落地**

`docs/adr/0024-a-turn-is-a-conversation-not-a-black-box.md`（**状态：已接受，2026-08-12**）
决定了「编排边跑边发事件 + 模型能停下来问作者」。

**核实起点（HEAD `2bcc3e1`）上它一个字都没实现**：
`grep -rn "EventSource|StreamingResponse|text/event-stream" src/novel_harness/api/ frontend/src/`
零命中。**但核实过程中它就在落地**——在途那个 workflow 除了 `loop.py`/`tools.py`
（第 10 条工具 `ask_author`）之外，`agent/model.py` 也已经长出
`EventFn` / `TurnEvent` / `safe_emitter` / `REPLY_DELTA` / `DRAFT_DELTA`。
**所以下面这两条不是「以后的事」，是这一两天的事。**

ADR 0024 的「代价」一节（`docs/adr/0024-…md:102-118`）里有两条**新的**已知限制，
板子上一条都没有——
① 「每一条事件都是一个新的输出面，而边界一不可回收」（中间过程里就有工具原返回）；
② 「契约夹具那道网在事件流上没有现成对应物」（`api.json` 冻不住一串按时间来的消息）。
**整份重写板子的时候要一起进去。**

**顺带**：第 17 条那个残留（对话回复不流式）现在是 ADR 0024 的承重墙了——
在途的 `agent/model.py:40-46` 自己写着「对话回复走非流式的一次往返，里面没有
『写到一半』这个时刻可以插进去，所以 **`REPLY_DELTA` 在产品上今天基本不响，
响的是 `DRAFT_DELTA`**」。也就是「边写边看」今天只在**起草**那一档是真的。
那份 ADR §2 举的例子（「读到第三行发现语气不对 ⇒ 第 5 秒按停」）读的正是稿子，
所以不算落空——**但板子上「打断粒度」那句话从一条余债变成了一条产品事实**。

### B. `_endpoints` 的 `JumpTarget.CHAPTER → ()` 是 ADR 0020 的观测点，它现在被第 26 条挡着

`api/activity.py:47-61` 写死了「`CHAPTER` → 空：明说『只能定位，今天没有编辑入口』」，
docstring 直接说这是 ADR 0020「什么条件下推翻本 ADR」里那条的观测口。
而 `/runs` 不走 `_with_jump` ⇒ 底栏这条路上**所有** target 的 `endpoints` 都是空的
⇒ **「真的没入口」和「这条路没算」在出参上长得一模一样**。
修第 26 条的时候要顺手确认这个观测点没被污染。

### C. `agent/drafting.py` 落盘那一行的 `decision_log` 用的是 `SYSTEM_ACTOR`，而它是模型的显式动作

`agent/drafting.py:669-671`：`actor=decisions.SYSTEM_ACTOR`，注释写「没有任何人点过这一次保存」。
ADR 0021 时这是对的（落盘是起草的副作用）。**ADR 0022 之后落盘是模型的一个显式工具调用**
（`save_draft`），而 `actor` 这一列今天只有 `author` / `system` 两个取值
（`activity.py` 的 actor 过滤就按它分）。
**不是 bug**（模型不是作者，归到 system 没错），但「作者点的 vs 系统自己做的」这条分界线
现在把「模型自己决定写进书」也算进了 system 那一侧——ADR 0020/0021 的整条退路
（「作者能把系统自己做的和自己做的分开看」）在这个粒度上分不出「后台整理」和「写作助手改了我的正文」。
**值得在重写板子时记一条。**

### D. `read_draft` / `DraftFullText` 的全文没进任何一条边界三的测试

见 ④ 第 16 条。`tests/` 里 `outdated_manuscript` 的全部断言
（`tests/test_chat_boundary.py:965-1023`）只喂 `ChapterFullText` 形状和非 JSON，
**一条都没喂 `DraftFullText`**。

---

## 36 条里互相矛盾 / 说着同一件事的

**这个仓库栽过好几次「照过期那份排期」，所以单列一节。**

### 🔴 真矛盾（一条断言另一条的前提不成立）

**第 1 条 ⟂ 第 10 / 12 / 20 条。**

- 第 1 条：「`record_call` 全仓只有两个调用方——**作者花钱最多的动作『起草』在 `model_call` 里一行都没有**」。
- 第 10 条：「一开可中断就没有 `stream_options` ⇒ **每一稿的 token 数退成 `NULL`**」——
  这句话预设每一稿**都写了一行** `model_call`。
- 第 12 条：「**`/draft` 记账失败**会把 200 变成 500」——预设 `/draft` 在记账。
- 第 20 条：「**起草那一行账**在日志页上答不出『为哪一章』」——预设那一行存在。

**第 1 条是旧的那份，10/12/20 是新的。** 照第 1 条排期的人会去做一件
`6b13abd` 已经做完的事（给 `/draft` 补记账），并且会得出「底栏的数不该被当真」
这个今天已经不对的结论。

### 同一件事的两个版本（不矛盾，但会被数成两条）

| | 关系 |
|---|---|
| **第 2 条 ⊂ 第 22 条** | 都是「`ProviderError` 那一路没有账」；22 多一句「`steps` 也不计那一次」。**22 是 2 的严格超集**，2 可以直接删。 |
| **第 11 条 ≡ 第 20 条（今天）** | 20 的产品档那一半已被 009 修掉，剩下的正好就是 11。**今天它们是同一个洞的两种说法。** |
| **第 3 条 · 第 23 条 · 第 13 条** | 三条都在说「预算 / 估算和工具声明的关系」，但**方向相反**：13 说「声明进 `prompt_bytes` ⇒ 闸提前咬住（偏紧）」，23 说「预算不量声明 ⇒ 系统性低估（偏松）」，3 说「两个天花板同一个数」。它们量的是三个不同的计数器（成本闸的估算 / 投影的字数预算 / 一次工具返回的上限），**但摆在同一张清单上读起来像自相矛盾**。今天 3 和 23 都已过期、13 已生效——**只剩 13 一条，正好把这个混乱一次清掉。** |
| **第 1 条 · 第 10 条 · 第 29 条** | 三条构成同一个「底栏在说什么」的簇：1 = 缺行、10 = 缺数、29 = 把缺的显示成 0。**29 已修，1 已过期，只剩 10 的那次裁决。** |
| **第 16 条 · 板子「3.5 的两条余债」** | 都在说「对话里躺着的东西会和真相源对不上」，一条在正文侧、一条在前端重取侧。不冲突，但重写时该放一起。 |

---

## 给整份重写板子的人的三条

1. **① 那 9 条直接删，别改成「已修」再留着**——留着就还会被当成待办读一遍。
   其中第 32 条建议挪去一个「事故史」小节（它有价值，但不是限制了）。
2. **④ 那 8 条要连措辞一起重写**，尤其第 9 条（不是「做了一半」，是「三件里做了一件，
   而且做的不是板子说的那一件」）和第 26 条（「无害的理由」挂错了组件）。
3. **合并第 2/22、11/20，删掉第 1 条**，再把 3/13/23 收成一条——36 条会掉到 30 上下，
   而且不再有两条互相打架的。
