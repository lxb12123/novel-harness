// 工作台的图标。**这一份是这套图标的语法**，后面每加一个都照它来——
// 一套图标里最刺眼的从来不是哪一个画得不好，而是**它们互相不像**（这个粗那个细、
// 这个 24 视框那个 20、这个跟着文字颜色走那个写死了黑色）。所以规矩先立在这儿：
//
// 1. **24×24 视框，中心 (12,12)**。图标自己不写宽高——大小由 CSS 给（`.icon` = 16px），
//    所以同一个图标在按钮里和在空状态里是同一份，不是两份。
// 2. **描边式**：`fill="none"` + `stroke="currentColor"`，`stroke-width` 1.5，
//    两端和拐角都圆。跟着 `currentColor` 走这一条是关键：按钮悬浮变强调色、禁用变淡，
//    图标自己跟着变，**一行额外的 CSS 都不用写**。
//    1.5 是配着这个界面挑的：13px 正文 + 1px 描边，图标再粗就比它旁边的字还重。
//    **例外只有作者亲手给图的那两颗**：`WingIcon`（2026-09-07 的那对翅膀）和
//    `NibIcon`（2026-09-12 的笔尖），都是实心剪影（`fill="currentColor"`），见各自的
//    注释。`currentColor` 那一条它们照守。
// 3. **`aria-hidden` + `focusable="false"`**：图标不进无障碍树，名字由**按钮的
//    `aria-label`** 给。一颗只有图标的按钮少了 aria-label = 读屏念不出名字的按钮，
//    而这件事在屏幕上完全看不出来。
// 4. 几何**算出来，不手画**：齿距差一度、圆角差半像素，放大到 48px 一眼就看得见。

/**
 * 活动记录：三条「时间线上的一件事」。
 *
 * 它**故意画得平**——这一栏里只有写作助手那一个是角色，别的都是工具。
 * 两个都卖萌，就没有一个是主角了。
 */
export function LogIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <circle cx="5.6" cy="6.4" r="1.5" />
      <path d="M10 6.4h9" />
      <circle cx="5.6" cy="12" r="1.5" />
      <path d="M10 12h9" />
      <circle cx="5.6" cy="17.6" r="1.5" />
      <path d="M10 17.6h5.4" />
    </svg>
  );
}

/**
 * 写作助手 —— **一个脑袋是书的小机器人**（作者的原话：头是一个书的机器人，卡通 Q 版可爱）。
 *
 * 它是助手那张脸，两处在用：顶栏那颗模式开关**开着的时候**（novel-agent 模式）画它，
 * 写作助手面板还一句话都没有时的那一屏也画它。
 *
 * **2026-09-12 之前它有两态**（书合着 = 待命、书摊开 = 上场），合着那版给协助模式，
 * 摊开那版给 novel-agent 模式。作者这一天把顶栏那颗改成了「协助模式 = 笔尖
 * （`NibIcon`），novel-agent 模式 = 这个合着书的小机器人」——摊开的书从此没人画，
 * 那一支就删了（废弃即删；真要回来 git 里有）。
 *
 * ── 为什么可爱只能可爱到这个程度 ────────────────────────────────────────
 *
 * Q 版靠的是「圆头 + 大眼睛」的比例，而这一栏的图标是 16–20px。
 * 手脚、笔、表情在这个尺寸下全都糊成噪点（试过：加了笔的那一版在 16px 上只剩一团斜杠，
 * 加了嘴的那一版在 20px 上看不见）。所以只留三样撑得住的：
 * **书的轮廓、两只实心大眼睛、一根天线**——实心眼睛是关键，空心圈在 20px 下会变成小方块。
 */
export function BotIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M12 5.6V3.4" />
      <circle cx="12" cy="2.6" r="1.05" />
      <rect x="4.8" y="6.4" width="14.4" height="12.6" rx="2.6" />
      <path d="M8 6.4v12.6" />
      <circle cx="12.2" cy="11.6" r="1.3" fill="currentColor" stroke="none" />
      <circle cx="16" cy="11.6" r="1.3" fill="currentColor" stroke="none" />
    </svg>
  );
}

/**
 * 笔尖 + 星芒 —— 顶栏那颗模式开关**关着的时候**（协助模式，作者自己写、引擎在旁边
 * 续写）画的那张图。作者 2026-09-12 给的（原图 270×247 视框、`fill-rule="evenodd"`），
 * 坐标**一个字没改**——同 `WingIcon` 那条规矩：要换图就整份换 path 加对应那条
 * transform，别手调单个点。
 *
 * - `fillRule="evenodd"` **不能丢**：三段子路径里第二段画在第一段里面，靠奇偶规则
 *   把笔尖掏空成线稿；丢了它笔尖就是一坨实心。三段也因此必须待在**同一个** `<path>`
 *   里（奇偶是按一条 path 算的），别学 `WingIcon` 拆成数组。
 * - transform 是量出来的（Chrome `getBBox`：x 39、y 40.140、宽 173.837、高 170.860），
 *   把 bbox 的长边缩成 20 个单位、摆进 24 视框正中——跟 `WingIcon` 同一档高度，
 *   也跟它换班的那颗小机器人（描边算上约 19 高）看着一般大，来回切不会忽大忽小。
 * - 线比这一排别的图标细：它的「线」是两条轮廓之间那道实心带，折到 24 视框只有
 *   0.6–0.9 个单位（别处描边是 1.5）。**那是图本身的事，别在这儿加粗**——
 *   同 `WingIcon` 走过的弯路：要粗就回去重生成图。
 *
 * ⚠️ 同 `WingIcon`：它是 `fill="currentColor"` 的实心剪影，**破了本文件顶上那条
 * 「描边式」语法，是作者给的图不是疏忽**——别「顺手统一成描边」。
 */
const NIB =
  "M43.5,206.9 C41,204.6 39,201.9 39,200.9 C39,199.8 41.5,195.3 44.6,190.7 " +
  "C55.4,174.8 61.9,159 69.4,130.1 C71.3,122.8 73.7,115.9 74.7,114.8 " +
  "C75.6,113.7 81.6,109.7 88,105.8 C94.3,101.9 106.9,94.1 116,88.5 " +
  "C139.4,74.1 139.5,74 141.9,74 C144.6,74 168.7,96.4 169.6,99.7 " +
  "C170,101.3 168.3,105.6 163.7,114.2 C160.2,121 152.2,136.4 146,148.5 " +
  "C139.8,160.6 133.6,171.7 132.1,173.1 C130.3,174.7 126.8,176.3 121.9,177.4 " +
  "C101.8,182.3 79.6,192.1 62.4,203.5 C56.2,207.6 50.4,211 49.5,211 " +
  "C48.7,211 45.9,209.1 43.5,206.9 Z " +
  "M61.8,192.6 C76.3,183.2 101.9,172.6 120.7,168.1 C123.6,167.4 126.3,166.3 126.8,165.7 " +
  "C127.3,165 134.8,150.4 143.6,133.3 L159.5,102 156.7,99.3 L153.9,96.5 136.4,114.3 " +
  "C120.1,130.8 118.9,132.3 119.5,134.9 C122.8,149.9 101.9,156.2 96,142 " +
  "C92.9,134.6 99.4,126 107.9,126 L112.6,126 130,107.9 L147.3,89.8 144.4,87 " +
  "L141.5,84.1 122,95.9 C111.3,102.4 97.8,110.6 92.1,114.1 C80.4,121.3 81.8,119.1 76.5,138.5 " +
  "C70.1,161.8 63.2,178.9 54.6,192.5 C49.1,201 49.3,200.7 52,198.9 " +
  "C53.4,198 57.8,195.2 61.8,192.6 Z " +
  "M186,81.8 C184.3,74.6 182.5,71.3 179.2,69.7 C177.7,68.9 174.1,67.7 171.2,67 " +
  "C166.7,66 165.9,65.5 166.2,63.6 C166.4,62 167.6,61.2 171,60.4 " +
  "C181.5,57.7 183.7,55.5 185.9,45.4 C187,40.6 187.5,39.9 189.3,40.2 " +
  "C191,40.4 191.7,41.6 192.6,45.5 C194.9,55.6 198.2,58.9 207.7,60.5 " +
  "C211.6,61.1 213.7,63.1 212.5,65 C212.2,65.5 209.4,66.4 206.3,67.1 " +
  "C198.4,68.9 195.3,72 193.1,80 C191.7,85.1 190.9,86.6 189.3,86.8 " +
  "C187.6,87.1 187,86.3 186,81.8 Z";

// bbox → 24 视框正中，长边 20 个单位。数字是量出来的（Chrome `getBBox`），不是手调的。
const NIB_FIT = "translate(-2.487 -2.447) scale(0.115050)";

export function NibIcon() {
  return (
    <svg className="icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false" fill="currentColor">
      <g transform={NIB_FIT}>
        <path fillRule="evenodd" d={NIB} />
      </g>
    </svg>
  );
}

/** 「续写正在生成」的小圈——挂在顶栏模式开关角上的徽标，不是这份文件开头那套
 *  「描边线稿」的语法：那套是给正文里独立站着的图标定的，这个是**实心刻度放射状**
 *  旋转（Safari 标签页转圈那种），八根短刻度依次淡出淡入，营造转动的错觉——
 *  跟着 `currentColor` 走这条继续成立，底下那颗图标是什么颜色它就是什么颜色。 */
export function ThinkingSpinner() {
  return (
    <svg className="thinking-spinner" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
      {Array.from({ length: 8 }, (_, i) => (
        <rect
          key={i}
          x="11"
          y="1.5"
          width="2"
          height="7"
          rx="1"
          fill="currentColor"
          transform={`rotate(${i * 45} 12 12)`}
          // 亮点走的是延迟从大到小（角度从大到小先亮），不是刻度顺序本身——
          // `(7 - i)` 才是「亮的那一下沿顺时针跑」，`i` 顺序试过是反的（逆时针）。
          style={{ animationDelay: `${((7 - i) * -0.1).toFixed(2)}s` }}
        />
      ))}
    </svg>
  );
}

/** 「保存」按钮上的图标：手上这份还没落盘 = 一滴水（还没「定形」）；
 *  已经存进磁盘、跟远端对齐了 = 一片雪花（定了）。
 *
 *  ── 2026-09-09：**两个都按同一个倍率放大，去填满 24 视框** ────────────────
 *  这一天「保存」的文字去掉了，它们从「贴在字边上的小徽标」变成了**这颗按钮本身**，
 *  于是要跟隔壁那颗怀表读起来是一对。作者原话「这个水滴大小对齐左边那个钟表」。
 *
 *  病根不在 CSS 那个 17px 的框——两颗图标的框一直一样大，差的是**画在框里的墨**：
 *  水滴原来只占 11.6 个单位高（怀表 21.6），因为它当初是照着 13px 的中文字高画的。
 *  现在两个都拉到 19 个单位、居中在 (12,12)：比怀表矮一点点是**故意的**，
 *  水滴是实心块面、怀表主体是个空心圈，等高的话水滴看着会比怀表大一号。
 *
 *  ⚠️ **雪花必须跟着一起改**（作者只提了水滴）：两个图标共用按钮上同一个位置，
 *  一大一小的话「按下保存」那一瞬间图标会跳一下尺寸——那是个没人会去查的小毛病。
 *  倍率各自算（水滴 ×1.638、雪花 ×1.284），因为它们原来就不一样大。
 *
 *  **描边线稿，不实心**——回到这份文件开头那套语法（`fill="none"` +
 *  `stroke="currentColor"`）：填色渐变那版作者说「太胖」「一坨」，机器人图标
 *  是素描风，这两个挂在别的控件上的徽标也该是同一种笔触，不能自成一派。
 *  杏仁形（对称双曲线，不是尖顶+圆肚那种写实水滴）：**2026-08-30 从「更像真水珠」的
 *  不对称版改过来**——那版在 14px 描边下尖端和肚子的曲率差太不明显，看着像个模糊的点；
 *  对称杏仁形两端都是硬转折，小尺寸下轮廓反而更清楚。 */
export function DropletIcon() {
  return (
    <svg
      className="save-badge-icon droplet"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinejoin="round"
    >
      <path d="M12 2.5c-3.93 4.75-6.06 9.17-6.06 12.94a6.06 6.06 0 0 0 12.12 0c0-3.77-2.13-8.19-6.06-12.94z" />
    </svg>
  );
}

/** 六芒雪花：一条主枝 + 一对侧枝，转 6 次拼成一整片（同 `ThinkingSpinner` 那套
 *  「画一份、转出六份」的做法，几何算出来不是手描的）。**2026-08-30 从「主枝+三对
 *  侧枝+枝尖分叉」的繁版简化过来**——那版线太密，14px 下枝杈糊在一起分不清，
 *  这版只留主枝根部一对侧枝，形状更清爽。
 *  `strokeWidth` 跟水滴一致——线细一档，同样的框里「墨量」比水滴薄，看着就比水滴小
 *  一圈。**2026-09-09 两个一起从 1.6 并到 1.5**：那 0.1 是这两颗当年单独定的，
 *  而它们现在跟怀表并排站着，全套统一的 1.5 才是这一排的粗细（`icons.tsx` 开头第 2 条）。 */
export function SnowflakeIcon() {
  const arm = (
    <>
      <line x1="12" y1="12" x2="12" y2="2.5" />
      {/* 分枝落在主枝 72% 处（原来 59%）：往里挪一分，六条臂的内段就连成一片实心，
          那正是它在 17px 下比怀表重一档的原因（作者 2026-09-09：「感觉都很怪」）。
          往外推之后中间那圈留白回来了，轮廓才读得出「雪花」而不是「一个黑星」。 */}
      <line x1="12" y1="5.3" x2="9.95" y2="3.85" />
      <line x1="12" y1="5.3" x2="14.05" y2="3.85" />
    </>
  );
  return (
    <svg
      className="save-badge-icon snowflake"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
    >
      {[0, 60, 120, 180, 240, 300].map((deg) => (
        <g key={deg} transform={`rotate(${deg} 12 12)`}>
          {arm}
        </g>
      ))}
    </svg>
  );
}

/**
 * 齿轮（AI 设置）。
 *
 * 8 齿，齿顶半径 9.2、齿根 6.3、齿顶张角 19°、齿侧 6°、中心孔 r=3.4；
 * 拐角不写圆角，靠 `stroke-linejoin="round"` 自动圆掉。
 * **这几个数是调出来的**：先在 15/16/18/20/64px 上并排渲染过五档
 * （少齿的像花、10 齿的在 16px 上糊成一团），最后按「16px 下齿还分得开」定的。
 */
export function GearIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M10.48 2.93A9.20 9.20 0 0 1 13.52 2.93L13.68 5.93A6.30 6.30 0 0 1 15.10 6.52L17.34 4.51A9.20 9.20 0 0 1 19.49 6.66L17.48 8.90A6.30 6.30 0 0 1 18.07 10.32L21.07 10.48A9.20 9.20 0 0 1 21.07 13.52L18.07 13.68A6.30 6.30 0 0 1 17.48 15.10L19.49 17.34A9.20 9.20 0 0 1 17.34 19.49L15.10 17.48A6.30 6.30 0 0 1 13.68 18.07L13.52 21.07A9.20 9.20 0 0 1 10.48 21.07L10.32 18.07A6.30 6.30 0 0 1 8.90 17.48L6.66 19.49A9.20 9.20 0 0 1 4.51 17.34L6.52 15.10A6.30 6.30 0 0 1 5.93 13.68L2.93 13.52A9.20 9.20 0 0 1 2.93 10.48L5.93 10.32A6.30 6.30 0 0 1 6.52 8.90L4.51 6.66A9.20 9.20 0 0 1 6.66 4.51L8.90 6.52A6.30 6.30 0 0 1 10.32 5.93L10.48 2.93Z" />
      <circle cx="12" cy="12" r="3.4" />
    </svg>
  );
}

/**
 * 看不看得见密钥。`off` = 明文正露着（点一下藏回去），所以那时才划一道。
 *
 * 眼形是**四段对称的三次贝塞尔**：左右两个尖角落在 (3,12)/(21,12)，上下顶点 5.7/18.3，
 * 瞳孔 r=3。上一版是两段二次曲线（控制点 (12,4.2)/(12,19.8)）——二次曲线的顶点只走到
 * 控制点的一半，实际只鼓到 3.75，于是在 17px 下看着是**一道横缝里卡了颗小黑点**，
 * 而不是一只眼睛（作者：「这个眼睛太丑了」）。三次曲线才画得出「尖角 + 圆顶」这个形状，
 * 瞳孔也跟着放到 r=3——眼白和瞳孔的比例是「像不像眼睛」的全部。
 *
 * 那道杠是 45° 的整条对角线，**压在眼睛上面画**：短一截的斜杠在这个尺寸下会看成睫毛。
 */
export function EyeIcon({ off }: { off: boolean }) {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M3 12c0 0 3.4-6.3 9-6.3s9 6.3 9 6.3-3.4 6.3-9 6.3-9-6.3-9-6.3z" />
      {/* **划掉的那只不画瞳孔。** 一条 45° 的杠正好从瞳孔正中穿过去，三笔挤在
          中间那 6px 里，17px 下糊成一个黑团（实测比过：带瞳孔的那版认不出是眼睛）。
          去掉之后剩「一只眼 + 一道杠」，两笔各说各的。 */}
      {off ? <path d="M4.2 4.2 19.8 19.8" /> : <circle cx="12" cy="12" r="3" />}
    </svg>
  );
}

/**
 * 发送（写作助手输入框里那颗）。
 *
 * 一支向上的箭头，不是纸飞机：这颗按钮**贴在输入框右下角**、只有 28px，
 * 纸飞机那种斜的轮廓在这个尺寸下糊成一团，而箭头在 12px 都认得出。
 * 竖杆穿 5.5–18.5（同 `PlusIcon` 那一笔，两颗按钮的视觉重量才一致），
 * 两撇从顶点各斜下 5.5。
 */
export function SendIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M12 18.5v-13" />
      <path d="M6.5 11 12 5.5 17.5 11" />
    </svg>
  );
}

/** 停：发送那颗圆钮在一轮跑着、框里没字时换成它（作者 2026-09-12）。**实心的方块**——
 *  「停」在播放器上的形状。11 个单位见方、居中在 (12,12)：比那支箭的 13 窄一圈，
 *  因为实心块面比 1.5 的描边重，等宽的话方块会比箭头大一号（同 `DropletIcon`
 *  比怀表矮一点那条道理）；头一版 10 见方又小了一档，在 28px 的圆钮里像一粒。 */
export function StopIcon() {
  return (
    <svg className="icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
      <rect x="6.5" y="6.5" width="11" height="11" rx="2" fill="currentColor" />
    </svg>
  );
}

/**
 * 关掉这扇窗。
 *
 * **是图标不是那个 `×` 字符**：字形的两笔在不同字体里粗细、倾角、在方格里的位置都不一样
 * （上一版就是它，作者：「右上角的打叉是不是有点问题」），而且它跟着字号走、跟不了
 * `currentColor` 之外的那套图标粗细。画成图标之后，它和这扇窗里的眼睛是同一档描边。
 */
/**
 * 改（规则行右端那一颗）。一支笔：笔杆 45° 斜过来，笔尖收在左下，尾端一道短横是
 * 笔帽——**不画纸、不画方框**，那两样在 16px 下只会变成一团噪点（同这份文件开头
 * 那条：几何算出来，不手画）。
 */
export function PencilIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M4.8 19.2l1-4 10-10 3 3-10 10-4 1z" />
      <path d="M14.3 5.7l3 3" />
    </svg>
  );
}

/**
 * 闪电（「检验本章」）。**一笔闭合折线**，不是两个三角拼的：小尺寸下两块之间那道缝
 * 会糊成一个缺口。顶点收在 (13,2)→(4,13.5)→(11,13.5)→(10,22)→(20,10)→(13,10)，
 * 上下两截的斜率不同——完全对称的闪电看着像一个 Z。
 */
export function BoltIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      {/* 未检验那一档是**渐变**（作者给的样张：左下偏蓝的淡紫 → 右上亮紫）。
          渐变只能定义在 SVG 里，`color` 给不了——所以这颗图标自带一份 `defs`，
          由 CSS 决定用不用它（`.rules-run.idle .bolt-path`）。
          id 全局唯一且写死：屏幕上同时只有一颗闪电，重复 id 的老问题在这儿不成立。 */}
      <defs>
        <linearGradient id="bolt-idle" x1="0" y1="1" x2="1" y2="0">
          <stop offset="0%" stopColor="#8ea2f5" />
          <stop offset="55%" stopColor="#9b7bf2" />
          <stop offset="100%" stopColor="#a855f7" />
        </linearGradient>
      </defs>
      <path className="bolt-path" d="M13 2 4 13.5h7L10 22l10-12h-7z" />
    </svg>
  );
}

/** 放大镜（按章号找情节）。圈 + 一道 45° 的柄，柄从圈的切点出发**不插进圈里**——
 *  插进去在 16px 下会看成一个带尾巴的圆点。 */
export function SearchIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <circle cx="10.5" cy="10.5" r="6.5" />
      <path d="M15.4 15.4 20.5 20.5" />
    </svg>
  );
}

export function CloseIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M6.5 6.5 17.5 17.5" />
      <path d="M17.5 6.5 6.5 17.5" />
    </svg>
  );
}

/**
 * 加号（「新起一章」）。
 *
 * 两笔都**穿到 5.5–18.5**、在 12 处正交——不是「一个 ＋ 字符」：那个字形自带很宽的
 * 两侧留白，塞进一颗 26px 的圆里会显得又小又偏，而它旁边就是一列 13px 的汉字。
 * 画成图标之后大小由 `.icon` 给，和这套里别的图标是同一档。
 */
export function PlusIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M12 5.5v13" />
      <path d="M5.5 12h13" />
    </svg>
  );
}

/**
 * 排序方向（角色册那颗「按出场章数排」开关，`order` 是 `"desc" | "asc"`）。
 *
 * 三道长短不一的横杠代表**出场章数**这个量，箭头指它现在往哪边排：长杠在上、
 * 箭头朝下 = 多→少；反过来 = 少→多。杠的长短排列和箭头方向说的是同一件事，
 * 两个信号一起看错的概率比只画一个低。
 *
 * 上一版是文字按钮「写得多 → 少」+ 原生 `title`：四个字的中文和右边「＋」
 * 那颗一个字符的按钮宽窄不一挤在同一行（作者原话：「这个升序降序的切换
 * 按钮也很丑」）。换成这份文件开头那套图标语法之后宽度固定成一个按钮的大小；
 * 说明也从原生 `title`（要等约一秒，作者另一次的原话是「以为没有」）换成
 * `data-tip`（`.icon-btn::after`，.12s 就出来）。
 */
// **云纹试过、撤掉了**（2026-08-31，角色册每一行折叠态右侧那点装饰，
// 作者原话：「补点花纹」「用 svg 画云纹」）。半径递减的两段圆弧 + 一撇尾巴，
// 在 26×17px 的实际尺寸下糊成一团看不出是云——headless Chrome 截图自查
// 时肉眼判的，放大 6 倍看也只像一团缠绕的线，不像云纹惯用的螺旋收尾。
// 这一批先不留一个不好看的装饰凑数，路径数字没有留档（不是「先留着改天调」，
// 是这版几何本身就不成立，改天真要做得从头设计）。

/**
 * 收起（角色册那一行展开着的时候，右端那颗按钮）。
 *
 * 展开态下它**占的正是「⋯」那个位置**（作者 2026-09-04：「他这个三个悬浮的点…
 * 换成一个收起的符号好看一点，然后点击它就会收起」），所以尺寸和「⋯」一样是
 * 一颗 28×28 的 `.roster-act`，只是里面从三个点换成这道人字尖。
 */
export function CollapseIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M6 14.5l6-6 6 6" />
    </svg>
  );
}

export function SortIcon({ order }: { order: "desc" | "asc" }) {
  const bars = order === "desc" ? [17, 12, 7.5] : [7.5, 12, 17];
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <line x1="3.5" y1="6.4" x2={bars[0]} y2="6.4" />
      <line x1="3.5" y1="12" x2={bars[1]} y2="12" />
      <line x1="3.5" y1="17.6" x2={bars[2]} y2="17.6" />
      <path d="M20 5.5v13" />
      {order === "desc" ? (
        <path d="M17.8 15.3 20 18.5 22.2 15.3" />
      ) : (
        <path d="M17.8 8.7 20 5.5 22.2 8.7" />
      )}
    </svg>
  );
}

/** 「分析本章」——一支笔尖上带火花的**放大式扫描**：镜片扫过正文，扫出情节。
 *
 *  和同一条工具栏上的放大镜（`SearchIcon`）**故意不同形**：那一颗是纯圆镜 + 斜柄，
 *  这一颗镜片里有一道横线（读到的那一行）、外面三颗星（读出来的东西）。
 *  两颗都用放大镜的话，作者得靠位置去记哪颗是哪颗。 */
export function ScanIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      {/* 一页正文的两行字 */}
      <path d="M4.2 6.6h9" />
      <path d="M4.2 10.4h5.6" />
      {/* 扫过去的那道镜 */}
      <circle cx="13.6" cy="14.2" r="4.4" />
      <path d="M16.8 17.4 20.4 21" />
      {/* 扫出来的三点 */}
      <path d="M18.4 4.6v2.6M17.1 5.9h2.6" />
      <path d="M21.2 9.4v1.8M20.3 10.3h1.8" />
    </svg>
  );
}

// ══════════════════════════════════════════════════════════════════════════
// 右栏那一排栏目的图标（作者 2026-09-06）
//
// **这五个是一组，得先互相分得开，再各自好看。** 一排 15px 的图标并列时，
// 作者是靠**轮廓**认它们的，不是靠细节——所以五个的外形刻意选了五种不同的
// 大轮廓：人形 / 盾 / 旗 / 页 / 钟。两个都用「一页纸 + 几条线」的话，
// 在这个尺寸上就是同一个图标出现了两次。
// ══════════════════════════════════════════════════════════════════════════

/**
 * 角色册 —— **两个人**（一前一后）。
 *
 * 不画「一个人 + 几条线」：那和 `LogIcon` 在 15px 上是同一个轮廓（左边一竖排点、
 * 右边一竖排线），而它们就在同一块屏幕上。两个人形没有这个问题。
 *
 * 后面那位只画**右半边**（一段头弧 + 一段肩弧）：整颗头叠上去，两个圆在 15px 上
 * 会糊成一个花生。
 */
export function RosterIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      {/* 前面那位：整颗头 + 整条肩 */}
      <circle cx="9.6" cy="8.4" r="3.4" />
      <path d="M3.6 19.6v-.7a6 6 0 0 1 12 0v.7" />
      {/* 后面那位：只有右半边露出来 */}
      <path d="M16.4 5.4a3.2 3.2 0 0 1 0 6" />
      <path d="M18.2 13.8a5 5 0 0 1 3.4 4.7v1.1" />
    </svg>
  );
}

/**
 * 检验规则 —— **盾牌里一个勾**。
 *
 * 为什么不是「一页纸 + 勾」：那一页纸会和「章节总结」撞轮廓。盾是这一排里
 * 唯一一个尖底的外形，扫一眼就分得出来。
 *
 * 勾**不顶到盾的边**（左右各留 2.6）：顶到边的话，15px 上勾的两端会和盾壁并成
 * 一条线，看着像盾里有一道裂缝。
 */
export function RulesIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M12 3.2 19.2 6v5.4c0 4.3-3 7.5-7.2 9.4-4.2-1.9-7.2-5.1-7.2-9.4V6z" />
      <path d="m8.9 11.9 2.4 2.4 4.2-4.6" />
    </svg>
  );
}

/**
 * 事件 —— **一面旗**。
 *
 * 旗是「这儿发生了一件事」的通用标记（里程碑），而这一格装的正是这一章
 * 读出来的那些情节。
 *
 * 旗面开了个**燕尾缺口**而不是画成整块矩形：矩形旗在 15px 上和「章节总结」
 * 那一页纸只差一根杆子，缺口一开就分得开了。
 */
export function EventsIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      {/* 杆子：上下都留 3.2，不顶到框 */}
      <path d="M6.4 3.4v17.2" />
      {/* 旗面：右缘的燕尾缺口是它和「一页纸」唯一的分辨点 */}
      <path d="M6.4 4.8h11l-2.4 3.6 2.4 3.6h-11z" />
    </svg>
  );
}

/**
 * 章节总结 —— **一页纸，三行字，末行短**。
 *
 * 末行**故意短一截**：三行等长读起来是「一页纸」，末行收短才读得出「一段话」，
 * 而这一格装的是一段话不是一页正文。
 *
 * 行距 3.4 是配着 15px 挑的：再密两行会并成一道灰；再疏第三行就压到页脚上了
 * （页底在 20.6，末行 19.2 已经只剩 1.4 的余量）。
 */
export function SummaryIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      {/* 页 + 折角。折角是「一页纸」的判据，去掉它就成了一个圆角方框 */}
      <path d="M14 3.2H6.8a1.6 1.6 0 0 0-1.6 1.6v14.2a1.6 1.6 0 0 0 1.6 1.6h10.4a1.6 1.6 0 0 0 1.6-1.6V7.8z" />
      <path d="M14 3.2v4.6h4.8" />
      <path d="M8.6 12.4h6.8" />
      <path d="M8.6 15.8h6.8" />
      <path d="M8.6 19.2h3.6" />
    </svg>
  );
}

/**
 * 通知 —— **一口钟**。
 *
 * 钟舌（底下那道小弧）**不能省**：只剩钟身的话，15px 上它和一个倒扣的碗
 * 分不出来。加上那道弧，一眼就是钟。
 *
 * 这一格的标签后面还跟着一个数（「通知 956」），所以图标本身**不带小红点**——
 * 同一件事在一颗按钮上说两遍，第二遍只会让人以为那是另一件事。
 */
export function NotificationsIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M17.4 9a5.4 5.4 0 0 0-10.8 0c0 6.2-2.6 7.8-2.6 7.8h16s-2.6-1.6-2.6-7.8z" />
      <path d="M13.6 20.2a1.9 1.9 0 0 1-3.2 0" />
    </svg>
  );
}

/**
 * 收起 / 展开侧栏 —— **一对翅膀，一颗按钮拿一半**（作者 2026-09-07 定的）。
 *
 * - `side` 说它管哪一栏。原图那一半画的就是**左**翅，所以左栏那颗直接用，
 *   右栏那颗是它的镜像——靠 `scale(-1 1)` 翻，**不写第二份坐标**：手写两份迟早差
 *   半个像素，而这一对就是拿来并排比对称的。
 * - `collapsed` 说这对翅膀是**展开还是收拢**：展开 = 羽毛向外上方铺开，
 *   收起 = 羽尖向下收拢。**状态由形状本身说**，不靠「尖朝哪一边」。
 *
 * 图是作者用 Codex 生成的，两份都在 `~/Documents/Codex/2026-09-07/new-chat/outputs/`：
 * - 展开 = `wings-reference.svg` 的 `#reference-wing-shape`（960×640 视框）
 * - 收起 = `wings-folded.svg` 里那半只（256 视框）
 *
 * 坐标**一个字没改**。两份视框不一样大，所以缩放系数也不一样——下面那两条 transform
 * 各自把自己那份的 bbox 缩成「20 单位高、摆进 24 视框正中」，**两个状态因此一样高**，
 * 来回切不会忽大忽小。bbox 是量出来的（Chrome `getBBox`），不是手调的。
 * 要换图就整份换 path 加对应那条 transform，**别手调单个点**。
 *
 * ⚠️ **这一颗破了本文件顶上那条「描边式」语法**：它是 `fill="currentColor"` 的实心
 * 剪影，不是 1.5 的描边。**这是作者给的图，不是疏忽**——别「顺手统一成描边」。
 * 跟 `currentColor` 那一条照守：悬浮变强调色、禁用变淡，图标自动跟着走。
 *
 * ⚠️ **也是这一排里唯一随状态换样子的图标，别照抄给别的按钮。** 别处的状态走
 * `aria-pressed` 加「那块屏幕自己在不在」，图标不动。这儿之所以要动：侧栏收起之后
 * **那一栏整个从屏幕上消失**，光看顶栏认不出是哪边收着。
 *
 * 走过一段弯路，记下来别再走：最早那份 `wings.svg` 的羽毛是很细的月牙，跟 Codex
 * 同时给出的效果图对不上，20px 下只剩几道看不见的划痕。中间我把它加厚过一版，
 * 后来作者照效果图重生成了 `wings-reference.svg`，那一版加厚整段撤了——
 * **别再去动羽毛的坐标，那是图的问题，回去重生成图。**
 */
// 展开：wings-reference.svg 里的 `#reference-wing-shape`（原 960×640 视框坐标，一个字没改）
const SPREAD_WING = [
  "M330.5 613 C320.3 610.8 306.1 603.3 306.9 600.5 C307.1 599.9 310.5 598.4 314.4 597 C324.8 593.3 334.7 586.2 350 571.4 C370 552 382.3 544.2 395.4 542.4 C401.7 541.5 400.1 540 393 540 C378.8 540 365.7 544.8 347.3 556.7 C322.1 572.9 308.4 577.4 284 577.4 L266.5 577.4 L255.2 573.6 C237.5 567.6 221.3 555.7 214.1 543.5 L212 539.9 L220.3 540.6 C238.6 542.1 254.5 538.2 292.1 523.1 C344 502.4 373.5 505 414.5 534 C431.2 545.7 434.5 547.1 425.1 538.4 C405.7 520.5 395.1 498 384.5 452.5 C367.9 381.4 327.8 339.2 237.4 297.5 C186.7 274.1 171.3 266.2 151.5 253.2 C103.3 221.8 70.5 181.2 58 137.7 C48.4 104 48.8 66.6 59.2 34 C64.1 18.7 63.6 18.5 69.5 36.6 C89.6 97.9 129.6 142.3 229.4 214.1 C264.5 239.3 269.5 243.1 282.7 253.5 C356.2 311.7 384.4 353.8 390.4 414.1 C394.1 450.1 406.6 475.7 431.9 498.9 C469.8 533.4 477.3 545.8 478.6 575.5 L479.2 587.5 L476.4 584 C466.4 571.5 458.9 567.5 445 567.5 C430.6 567.5 428.1 568.8 398.6 591.9 C375.6 609.8 350.9 617.5 330.5 613 Z",
  "M216.6 521 C175.8 516.4 140.5 490.8 122.5 452.8 C117.6 442.5 117.7 442.4 131.5 448.6 C159.3 460.9 190.1 465.4 236 463.9 C317.2 461.1 343.4 467.2 374.8 496.5 C383.5 504.6 383.5 504.7 373 502 C346.6 495.3 322.7 497.7 283.7 511 C258.3 519.7 235.5 523.1 216.6 521 Z",
  "M359.3 470.5 C326.4 453.8 299.1 448.3 237 446 C183.4 444 157.8 439 129.6 424.8 C95.9 407.9 70.2 378.1 58.9 343 C56.1 334.4 52.6 319.1 53.2 318.4 C53.4 318.3 57.8 322 63 326.6 C97.1 356.8 129.5 370.1 208.5 386.5 C302.7 406 345.1 429.1 372.4 475.8 C373.5 477.6 372.7 477.3 359.3 470.5 Z",
  "M358.3 436.4 C326.4 404.7 292.9 388.7 219.5 370 C150.8 352.6 121.8 340.4 90.1 316 C43.3 279.8 16.3 218.8 24.4 167.6 L25.2 162.7 L27.4 166.7 C43.8 195.6 73.8 226.4 107.1 248.8 C135.2 267.6 162.6 281.5 211 301.5 C249.6 317.5 268.9 327 286 338.4 C319 360.3 342.9 386.9 358.1 418.5 C363.1 429.1 368.9 444.8 367.9 445.3 C367.7 445.4 363.4 441.4 358.3 436.4 Z",
];

// 收起：wings-folded.svg 里那半只（原 256 视框坐标，一个字没改）
const FOLDED_WING = [
  "M119 65C107 45 85 40 70 55C45 81 55 141 80 183C88 197 100 209 113 217C95 183 80 144 76 113C72 85 82 61 99 59C106 58 113 60 119 65Z",
  "M111 77C97 71 85 88 87 112C89 142 101 170 118 191C108 158 101 128 101 106C101 91 105 82 111 77Z",
  "M115 93C105 95 104 111 108 130C111 146 116 160 122 170C118 148 116 130 117 116C117 106 118 99 115 93Z",
  "M122 76C113 80 115 95 123 108C121 96 126 85 122 76Z",
];

// bbox → 24 视框正中。数字是量出来的（Chrome `getBBox`），不是手调的。
const SPREAD_FIT = "translate(3.510 1.230) scale(0.033812)";
const FOLDED_FIT = "translate(1.508 -3.421) scale(0.117150)";

export function WingIcon({ side, collapsed }: { side: "left" | "right"; collapsed: boolean }) {
  const wing = collapsed ? FOLDED_WING : SPREAD_WING;
  return (
    <svg className="icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false" fill="currentColor">
      {/* 外层只管镜像（右栏那颗），内层只管「把原图摆进视框」——两件事别揉一个 transform */}
      <g transform={side === "right" ? "translate(24 0) scale(-1 1)" : undefined}>
        <g transform={collapsed ? FOLDED_FIT : SPREAD_FIT}>
          {wing.map((d) => (
            <path key={d} d={d} />
          ))}
        </g>
      </g>
    </svg>
  );
}

/**
 * 怀表（「历史」按钮）。作者 2026-09-09 给的设计，按这份文件开头那套语法折算过来：
 * 他给的是 64 视框、`stroke-width` 2.5，这儿是 24 视框、1.5，比例一致（描边占视框
 * 的 3.9% → 6.25%，小图标要更粗才吃得住）。
 *
 * 构图**整体居中在 y=12**：吊环顶 1.2、表壳底 22.8。不是「每个圆都画在 (12,12)」——
 * 怀表本来就是上小下大的一串，逐件居中会把它挤成一坨。
 *
 * ⚠️ **原设计里的「内圈表盘」和「四向刻度」这儿没有，是量过之后砍的，不是漏了。**
 * 原图外圈 r=21、内圈 r=17，折成 24 视框是 8.2 和 6.6——两条线相距 1.6 个单位，
 * 而描边自己就 1.5 个单位宽；渲染到 17px 时那道缝只剩 0.4 像素，两个圈糊成一条粗线。
 * 刻度更短（1.8 个单位 ≈ 1.2 像素，比线还窄），落地是四个灰点。
 * 这跟 `SnowflakeIcon` 那次「枝杈糊在一起」是同一条经验：**小尺寸下砍笔画不是偷懒，
 * 是让剩下的笔画看得清**。要恢复双圈，得先把这颗图标放到 28px 往上，那时它在这条
 * 顶栏上会比旁边的字高出一截。
 */
export function PocketWatchIcon() {
  return (
    <svg
      className="icon"
      viewBox="0 0 24 24"
      aria-hidden="true"
      focusable="false"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      {/* 吊环 + 表冠：一串接住，别留缝——留缝在小尺寸下会读成「圆环飘在表上面」 */}
      <circle cx="12" cy="3.1" r="1.9" />
      <rect x="10.5" y="5" width="3" height="1.7" rx="0.5" />
      {/* 表壳。cy 14.6 + r 8.2 ⇒ 底 22.8，和吊环顶 1.2 对称 */}
      <circle cx="12" cy="14.6" r="8.2" />
      {/* 指针：原设计那个「L 形」的两笔，等比缩到 r=8.2 的表盘里 */}
      <path d="M9.3 11.9L12 14.6l3.5-3.5" />
      <circle cx="12" cy="14.6" r="0.9" fill="currentColor" stroke="none" />
    </svg>
  );
}
