# Novel Harness · 接口级数据流明细

> 依据**实时生成的 OpenAPI**（`app.openapi()`，57 个路径 / 67 个操作）与 `frontend/src/__fixtures__/api.json`（从真 app dump 的 74 份真实出参快照，源码注释称其为当前真相源之一）逐条核对。
> 本文件为**只读审计产物**，未改动任何代码。前端手写类型权威文件：`frontend/src/api/types.ts` 与 `api/schema.ts`。

## 通用约定

- **路径参数**：`project_id`（ULID，可含 `:`）、`chapter`（全书顺序位置 1-based，**不是正文印的章号**）、`chat_id`、`node_id`、`draft_id`、`run_id`、`event_id`、`proposal_id`、`evidence_id`、`snapshot_id`。
- **错误形状两种**（前端 `client.ts` 都认）：`HTTPException` → 详情裹在 `.detail`；自定义异常处理器（UnknownName/AmbiguousName/AmbiguousQuote/WrongLabel/QuoteNotFound/…）→ 详情在**顶层**，带 `error` 码 + `message` + 可选 `candidates`。
- **章号铁律**：写路由的入参一律**没有** `chapter` 输入字段（`valid_from` 只由证据决定，约束 10；`tests/test_no_chapter_input.py` 逐个点名）。
- **422**：Pydantic 校验失败 / 业务 `ValueError`；**404**：项目/章/节点/证据查无此物；**501**：能力未实现（稳定，非 404）。

_下表「真实响应样例」里的键 = `__fixtures__/api.json` 的键。_

---

## 根

### `GET /`

- **概要**：Index
- **语义/数据走向**：SPA 入口。构建产物在就发 React 工作台，否则降级到 static/ 的原生原型（只读也能用）。
- **请求参数**：
  - （无）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---

## 设置与模型窗(model_windows)

### `GET /api/settings`

- **概要**：Get Settings
- **语义/数据走向**：读 AI 设置（BYOK）。不含完整 key。
- **涉及存储**：本机 ~/.config/novel-harness/settings.json（0600，非 SQLite）
- **请求参数**：
  - （无）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/settings`，共 1 个变体）:

```json
{
  "api_key_preview": "",
  "api_key_set": false,
  "auto_update_model_windows": false,
  "base_url": "",
  "context_window": null,
  "model": ""
}
```
---
### `PUT /api/settings`

- **概要**：Put Settings
- **语义/数据走向**：写 AI 设置到本机（`~/.config/novel-harness/settings.json`，0600）。
- **涉及存储**：本机 ~/.config/novel-harness/settings.json（0600，非 SQLite）
- **请求参数**：
  - （无）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `SettingsBody`（组件模型）
    - `base_url` string（可选）
    - `model` string（可选）
    - `api_key` string（可选）
    - `context_window` integer | null（可选）
    - `auto_update_model_windows` boolean | null（可选）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/settingsSaved`，共 1 个变体）:

```json
{
  "api_key_preview": "***********-key",
  "api_key_set": true,
  "auto_update_model_windows": false,
  "base_url": "https://api.deepseek.com",
  "context_window": 128000,
  "model": "deepseek-v4-flash"
}
```
---
### `POST /api/settings/model-windows/refresh`

- **概要**：Refresh Model Windows
- **语义/数据走向**：作者点一下，去拉一次公开的模型窗口表（ADR 0025 那一层的数据源）。
- **涉及存储**：拉取 litellm 官方价格窗表 → 用户级缓存 ~/model_windows.json（包内 model_windows.json 兜底）
- **请求参数**：
  - （无）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---

## 书架与项目生命周期

### `GET /api/projects`

- **概要**：Projects
- **涉及存储**：project 表（书库列表）
- **请求参数**：
  - （无）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/projects`，共 2 个变体）:

```json
[
  {
    "canon_version": 6,
    "id": "project:ID1",
    "name": "青云记",
    "root_path": "<root>/book"
  }
]
```
---
### `POST /api/projects`

- **概要**：Create Project
- **语义/数据走向**：新建一本书。root_path 由服务器在 books_root 下派生（§ADR 0007：那目录就是稿子）。
- **涉及存储**：project 表（书库列表）
- **请求参数**：
  - （无）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `CreateProject`（组件模型）
    - `name` string（必填）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `POST /api/projects/bootstrap`

- **概要**：Bootstrap Project
- **语义/数据走向**：原子创建新书：项目、首章、快照与导入报告一起成功或一起消失。
- **涉及存储**：project/node/chapter/chapter_snapshot + 磁盘 root_path
- **请求参数**：
  - （无）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  （inline schema: None）
- **响应**：
  `BootstrapView`（组件模型）
    - `project` Project（必填）
    - `initial_chapter` integer（必填）
    - `import_report` #/components/schemas/ImportReport | null（可选）
    - `summary` #/components/schemas/ImportSummary | null（可选）
- **真实响应样例**（`__fixtures__/api.json#/bootstrapPreamble`，共 2 个变体）:

```json
{
  "import_report": {
    "chapter_count": 1,
    "preamble_chars": 1104,
    "synced": {
      "added": [
        {
          "created": true,
          "id": "chapter:ID62",
          "number": 1,
          "path": "chapters/0001.md",
          "project_id": "project:ID61",
          "snapshot_created": true,
          "snapshot_id": "snapshot:ID63",
          "text_sha256": "<sha>",
          "title": "起"
        }
      ],
      "ignored_files": [],
      "refreshed": [],
      "unchanged_count": 0
    },
    "unchanged": [],
    "written": [
      "chapters/0001.md"
    ]
  },
  "initial_chapter": 1,
  "project": {
    "canon_version": 0,
    "id": "project:ID61",
    "name": "序章样书",
    "root_path": "<root>/books/序章样书"
  },
  "summary": {
    "chapter_count": 1,
    "headline": "《序章样书》切成了 1 章，已经建好了。",
    "ignored_files": [],
    "landed_count": 1,
    "lines": [
  …（截断，共 49 行）
```
---

## 单个项目：导入/同步/详情

### `GET /api/projects/{project_id}`

- **概要**：Project Detail
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `POST /api/projects/{project_id}/import`

- **概要**：Import Book
- **语义/数据走向**：把一本 TXT 切章 → 写成 {root}/chapters/NNNN.md → 落库。一次性播种，日常回路用 sync。
- **涉及存储**：importer → chapter/chapter_snapshot/project
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `ImportText`（组件模型）
    - `text` string（必填）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/bootstrapImport`，共 1 个变体）:

```json
{
  "import_report": {
    "chapter_count": 1,
    "preamble_chars": 0,
    "synced": {
      "added": [
        {
          "created": true,
          "id": "chapter:ID3",
          "number": 1,
          "path": "chapters/0001.md",
          "project_id": "project:ID2",
          "snapshot_created": true,
          "snapshot_id": "snapshot:ID4",
          "text_sha256": "<sha>",
          "title": "契约"
        }
      ],
      "ignored_files": [],
      "refreshed": [],
      "unchanged_count": 0
    },
    "unchanged": [],
    "written": [
      "chapters/0001.md"
    ]
  },
  "initial_chapter": 1,
  "project": {
    "canon_version": 0,
    "id": "project:ID2",
    "name": "契约样书",
    "root_path": "<root>/books/契约样书"
  },
  "summary": {
    "chapter_count": 1,
    "headline": "《契约样书》切成了 1 章，已经建好了。",
    "ignored_files": [],
    "landed_count": 1,
    "lines": [
  …（截断，共 49 行）
```
---
### `POST /api/projects/{project_id}/reconcile`

- **概要**：Reconcile Manuscript
- **语义/数据走向**：把这本书和磁盘对一遍。**不花钱**（一次模型调用都没有）。
- **涉及存储**：磁盘 md ↔ chapter 对齐（库为证）
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `deep`（query，可选，`boolean`，默认 `False`）忽略 stat 快路，每一章都重新读+算哈希（开书那一次用）。
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `ReconcileOutcome`（组件模型）
    - `checked` integer（必填）
    - `reread` array（可选）
    - `refreshed` array（可选）
    - `refused` array（可选）
---
### `POST /api/projects/{project_id}/sync`

- **概要**：Sync Project
- **语义/数据走向**：把 {root}/chapters/*.md 的现状读进库（作者在别的软件里改了稿之后走这条）。
- **涉及存储**：importer.sync → chapter(disk_mtime_ns/disk_size)/chapter_snapshot + 证据 revalidate
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `SyncOutcome`（组件模型）
    - `added_chapters` array（可选）
    - `updated_chapters` array（可选）
    - `unchanged_count` integer（可选）
    - `chapter_count` integer（可选）
    - `ignored_files` array（可选）
    - `headline` string（必填）
    - `notes` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/sync`，共 2 个变体）:

```json
{
  "added_chapters": [],
  "chapter_count": 3,
  "headline": "读回来了：1 章有新内容（这本书现在共 3 章）。",
  "ignored_files": [
    "chapters/大纲.md"
  ],
  "notes": [
    "内容变了：第 2 章。之前那一版还在「历史」里。",
    "这些文件不是章节，没有动它们：chapters/大纲.md。"
  ],
  "unchanged_count": 2,
  "updated_chapters": [
    2
  ]
}
```
---

## 章节（列表/增删/历史/快照/自检）

### `GET /api/projects/{project_id}/chapters`

- **概要**：Chapters
- **语义/数据走向**：列章：直接扫磁盘 {root}/chapters/NNNN.md（磁盘是正文真相源）。title = 首个非空行。
- **涉及存储**：chapter（每章=1 节点）+ chapter_snapshot
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/chapters`，共 1 个变体）:

```json
[
  {
    "number": 1,
    "title": "第一章 血脉"
  },
  {
    "number": 2,
    "title": "第二章 试探"
  },
  {
    "number": 3,
    "title": "第三章 对峙"
  }
]
```
---
### `POST /api/projects/{project_id}/chapters`

- **概要**：Create Chapter
- **语义/数据走向**：在这本书末尾新起一章（空的）。**没有请求体，也没有章号入参。**
- **涉及存储**：chapter（每章=1 节点）+ chapter_snapshot
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `DELETE /api/projects/{project_id}/chapters/{chapter}`

- **概要**：Delete Chapter
- **语义/数据走向**：删掉一章。**引擎在它上面记过东西就拒绝**（409 `chapter_in_use`，带明细）。
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `GET /api/projects/{project_id}/chapters/{chapter}/autopilot`

- **概要**：Autopilot Status
- **语义/数据走向**：这一章的后台活做到哪了（前端起草前的兜底）。**不派活、不花钱、不建 run。**
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `AutopilotStatus`（组件模型）
    - `chapter` integer（必填）
    - `summary_ready` boolean（必填）
    - `extraction_ready` boolean（必填）
    - `running` boolean（必填）
    - `summary_state` Readiness（必填）
    - `extraction_state` Readiness（必填）
    - `errors` array（可选）
---
### `POST /api/projects/{project_id}/chapters/{chapter}/autopilot`

- **概要**：Run Autopilot
- **语义/数据走向**：这一章「写完了」（= 作者切走了），把该在后台做的都做掉。幂等。
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `GET /api/projects/{project_id}/chapters/{chapter}/history`

- **概要**：Chapter History
- **语义/数据走向**：这一章的历史快照（版本对比的料）。**内容去重、非全量版本史**——同内容只存一次。
- **涉及存储**：chapter_snapshot（历史回看）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/chapterHistory`，共 2 个变体）:

```json
[
  {
    "created_at": "<ts>",
    "is_current": true,
    "snapshot_id": "snapshot:ID11",
    "text": "第一章 血脉\n\n萧决在青云城主府第一次听说了血脉秘密的真相。\n李管家什么也没说。\n",
    "text_sha256": "<sha>"
  }
]
```
---
### `DELETE /api/projects/{project_id}/chapters/{chapter}/snapshots/{snapshot_id}`

- **概要**：Delete Chapter Snapshot
- **语义/数据走向**：删掉一条历史版本。**只删真的没人引的那种**（图层的两条不变式，见
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `snapshot_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---

## 正文读写（磁盘）

### `GET /api/projects/{project_id}/chapters/{chapter}/text`

- **概要**：Chapter Text
- **语义/数据走向**：读磁盘 md（不是 DB 快照——那是给证据锚用的冻结版，不是可编辑的正文）。
- **涉及存储**：磁盘 chapters/N.md 读/写 + chapter_snapshot(sha 去重)
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/chapterText`，共 1 个变体）:

```json
{
  "markdown": "第一章 血脉\n\n萧决在青云城主府第一次听说了血脉秘密的真相。\n李管家什么也没说。\n",
  "number": 1
}
```
---
### `PUT /api/projects/{project_id}/chapters/{chapter}/text`

- **概要**：Save Chapter
- **语义/数据走向**：保存：先写磁盘（文件是作者的），再 importer.sync 落快照。磁盘先、DB 跟。
- **涉及存储**：磁盘 chapters/N.md 读/写 + chapter_snapshot(sha 去重)
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `ChapterSave`（组件模型）
    - `markdown` string（必填）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/chapterSaved`，共 1 个变体）:

```json
{
  "added": [
    {
      "created": true,
      "id": "chapter:ID47",
      "number": 3,
      "path": "chapters/0003.md",
      "project_id": "project:ID1",
      "snapshot_created": true,
      "snapshot_id": "snapshot:ID48",
      "text_sha256": "<sha>",
      "title": "对峙"
    }
  ],
  "ignored_files": [],
  "refreshed": [
    {
      "created": false,
      "id": "chapter:ID49",
      "number": 2,
      "path": "chapters/0002.md",
      "project_id": "project:ID1",
      "snapshot_created": true,
      "snapshot_id": "snapshot:ID50",
      "text_sha256": "<sha>",
      "title": "试探"
    }
  ],
  "unchanged_count": 1
}
```
---

## 认知面板（矩阵/约束/状态/在场）

### `GET /api/projects/{project_id}/chapters/{chapter}/constraints`

- **概要**：Constraints
- **语义/数据走向**：场景约束盒：scene_constraints 收原始称呼、内部自解析、fail-closed。
- **涉及存储**：edge(RELATED_TO/LOCATED_AT/HAS_STATE)+alias → 约束转译
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
  - `cast`（query，可选，`string`）
  - `include`（query，可选，`string`）一定要出现在这份在场里的称呼原文（跳转坐标用）。**只加不减**，不替换推导。
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/constraints`，共 1 个变体）:

```json
{
  "chapter": 2,
  "forbidden_entities": [
    {
      "first_appears_chapter": 200,
      "node": {
        "id": "character:ID6",
        "label": "Character",
        "name": "未来大能"
      },
      "surfaces": [
        "未来大能"
      ]
    }
  ],
  "must_not_reveal": [
    {
      "id": "secret:ID7",
      "label": "Secret",
      "name": "血脉秘密"
    },
    {
      "id": "secret:ID13",
      "label": "Secret",
      "name": "玄铁令下落"
    }
  ],
  "unresolved_cast": []
}
```
---
### `GET /api/projects/{project_id}/chapters/{chapter}/matrix`

- **概要**：Matrix
- **语义/数据走向**：★ 认知边界矩阵（头牌）。必须经 resolve_cast，才挂得上 unresolved_cast（store 自己不知道）。
- **涉及存储**：edge(KNOWS/BELIEVES)/secret/node —— 集合差集
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
  - `cast`（query，可选，`string`）在场称呼原文，逗号/顿号分隔；留空 = 由本章正文推
  - `include`（query，可选，`string`）一定要出现在这份在场里的称呼原文（跳转坐标用）。**只加不减**，不替换推导。
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/matrix`，共 1 个变体）:

```json
{
  "cells": [
    {
      "believed_value": null,
      "character_id": "character:ID10",
      "evidence_id": "evidence:ID15",
      "secret_id": "secret:ID7",
      "since_chapter": 1,
      "state": "KNOWS"
    },
    {
      "believed_value": null,
      "character_id": "character:ID10",
      "evidence_id": null,
      "secret_id": "secret:ID13",
      "since_chapter": null,
      "state": "UNKNOWN"
    },
    {
      "believed_value": null,
      "character_id": "character:ID8",
      "evidence_id": null,
      "secret_id": "secret:ID7",
      "since_chapter": null,
      "state": "UNKNOWN"
    },
    {
      "believed_value": null,
      "character_id": "character:ID8",
      "evidence_id": null,
      "secret_id": "secret:ID13",
      "since_chapter": null,
      "state": "UNKNOWN"
    }
  ],
  "chapter": 2,
  "characters": [
    {
      "id": "character:ID10",
      "label": "Character",
  …（截断，共 69 行）
```
---
### `GET /api/projects/{project_id}/chapters/{chapter}/mentioned`

- **概要**：Mentioned
- **语义/数据走向**：本章正文提到了花名册里的哪些称呼。**这不是「在场」**——见 `mentioned.py` 的模块说明。
- **涉及存储**：alias → text/mentions（本名字面出现）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/mentioned`，共 2 个变体）:

```json
{
  "chapter": 1,
  "has_text": true,
  "surfaces": [
    "萧决",
    "李管家"
  ]
}
```
---
### `GET /api/projects/{project_id}/chapters/{chapter}/state`

- **概要**：State
- **语义/数据走向**：当前状态卡：cast_states 收已解析 node_id，所以先 resolve_cast。
- **涉及存储**：edge(node/location/states) → 状态卡
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
  - `cast`（query，可选，`string`）
  - `include`（query，可选，`string`）一定要出现在这份在场里的称呼原文（跳转坐标用）。**只加不减**，不替换推导。
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/states`，共 1 个变体）:

```json
[
  {
    "chapter": 2,
    "edges": [
      {
        "confidence": 1.0,
        "dst": "secret:ID7",
        "evidence_id": "evidence:ID15",
        "evidence_status": "FRESH",
        "id": "edge:ID16",
        "information_scope": "CANON",
        "project_id": "project:ID1",
        "props": {
          "believed_value": null,
          "value": null,
          "value_key": null
        },
        "source": "author",
        "src": "character:ID10",
        "status": "ACTIVE",
        "type": "KNOWS",
        "valid_from_chapter": 1,
        "valid_to_chapter": null
      }
    ],
    "is_dead": false,
    "location": null,
    "node": {
      "id": "character:ID10",
      "label": "Character",
      "name": "萧决",
      "project_id": "project:ID1",
      "props": {
        "background": null,
        "character_notes": null,
        "dim_key": null,
        "first_appears_chapter": null,
        "gender": null,
        "main_character": null,
        "personality": null
  …（截断，共 69 行）
```
---
### `GET /api/projects/{project_id}/characters/{node_id}/state`

- **概要**：Character State Endpoint
- **语义/数据走向**：单个人物在第 chapter 章的状态快照。node_id 不在本项目 → NodeNotFound → 404。
- **涉及存储**：state_at(character) → 状态卡单角色
- **请求参数**：
  - `node_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
  - `chapter`（query，必填，`integer`）AS OF 第几章（查询参数，不写进任何数据）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/characterState`，共 1 个变体）:

```json
{
  "chapter": 2,
  "edges": [],
  "is_dead": false,
  "location": null,
  "node": {
    "id": "character:ID10",
    "label": "Character",
    "name": "萧决",
    "project_id": "project:ID1",
    "props": {
      "background": null,
      "character_notes": null,
      "dim_key": null,
      "first_appears_chapter": null,
      "gender": null,
      "main_character": null,
      "personality": null
    }
  },
  "scope": "CANON",
  "states": []
}
```
---
### `GET /api/projects/{project_id}/evidence/{evidence_id}`

- **概要**：Evidence
- **语义/数据走向**：Tab3 确定性证据：把矩阵格/状态边的 evidence_id 还原成「来源章 + 当年那句原文」。
- **涉及存储**：evidence + chapter_snapshot（双指针锚）
- **请求参数**：
  - `evidence_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `GET /api/projects/{project_id}/nodes/{node_id}/summary-mentions`

- **概要**：Node Summary Mentions
- **语义/数据走向**：**还有哪几章的总结提到它**，按章号升序。一次 SQL，不调模型、不花钱。
- **涉及存储**：summary_mention（按节点倒查）
- **请求参数**：
  - `node_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `GET /api/projects/{project_id}/resolve`

- **概要**：Resolve
- **语义/数据走向**：称呼 → 候选节点（消歧下拉 / 选区查人物的数据源）。
- **涉及存储**：alias 消歧（usable_for_rules）
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `surface`（query，必填，`string`）一个称呼原文，查它指向哪些节点
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/resolve`，共 2 个变体）:

```json
{
  "ambiguous": false,
  "hits": [
    {
      "kind": "canonical",
      "node": {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      },
      "usable_for_rules": true
    }
  ],
  "surface": "萧决",
  "unique_id": "character:ID10"
}
```
---
### `GET /api/projects/{project_id}/roster`

- **概要**：Roster
- **语义/数据走向**：左栏花名册：全项目节点，收窄成 {id,label,name}（不整体序列化 Node.props，防秘密泄漏）。
- **涉及存储**：node(Character)+alias → 头像矩阵行
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/roster`，共 1 个变体）:

```json
[
  {
    "id": "location:ID5",
    "label": "Location",
    "name": "青云城主府"
  },
  {
    "id": "character:ID6",
    "label": "Character",
    "name": "未来大能"
  },
  {
    "id": "secret:ID7",
    "label": "Secret",
    "name": "血脉秘密"
  },
  {
    "id": "character:ID8",
    "label": "Character",
    "name": "李管家"
  },
  {
    "id": "location:ID9",
    "label": "Location",
    "name": "北荒"
  },
  {
    "id": "character:ID10",
    "label": "Character",
    "name": "萧决"
  }
]
```
---
### `GET /api/projects/{project_id}/subgraph`

- **概要**：Subgraph
- **语义/数据走向**：局部关系图（Tab2）。hops>2 由引擎抛 ValueError → 422；center 不存在 → 404。
- **涉及存储**：edge/node 子图（本章视角收窄）
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `center`（query，必填，`string`）中心节点 node_id（已解析）
  - `chapter`（query，必填，`integer`）AS OF 第几章
  - `hops`（query，可选，`integer`，默认 `1`）展开跳数，硬上限 2（3 跳数学上坏）
  - `edge_types`（query，可选，`string`）逗号分隔的 EdgeType，空=默认展开
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/subgraph`，共 1 个变体）:

```json
{
  "center": {
    "id": "character:ID10",
    "label": "Character",
    "name": "萧决",
    "project_id": "project:ID1",
    "props": {
      "background": null,
      "character_notes": null,
      "dim_key": null,
      "first_appears_chapter": null,
      "gender": null,
      "main_character": null,
      "personality": null
    }
  },
  "chapter": 2,
  "edges": [],
  "hops": 1,
  "nodes": [
    {
      "id": "character:ID10",
      "label": "Character",
      "name": "萧决",
      "project_id": "project:ID1",
      "props": {
        "background": null,
        "character_notes": null,
        "dim_key": null,
        "first_appears_chapter": null,
        "gender": null,
        "main_character": null,
        "personality": null
      }
    }
  ],
  "scope": "CANON",
  "truncated": false
}
```
---

## 声明与认知写入

### `POST /api/projects/{project_id}/canon/events/{event_id}/cast`

- **概要**：Edit Canon Event Cast
- **语义/数据走向**：改一条已生效事件的知情 / 在场名单。**删掉的那一行留着（RETRACTED）。**
- **涉及存储**：event_participant(cast 纠正) + decision_log
- **请求参数**：
  - `event_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `EventCastEditRequest`（组件模型）
    - `knower_ids` array | null（可选）
    - `participant_ids` array | null（可选）
    - `expected_canon_version` integer（必填）
- **响应**：
  `EventCastCorrection`（组件模型）
    - `project_id` string（必填）
    - `canon_version` integer（必填）
    - `decision_id` string（必填）
    - `event` EventView（必填）
    - `knowers_added` array（可选）
    - `knowers_removed` array（可选）
    - `participants_added` array（可选）
    - `participants_removed` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/canonEventCast`，共 1 个变体）:

```json
{
  "canon_version": 14,
  "decision_id": "decision:ID40",
  "event": {
    "event": {
      "chapter_number": 1,
      "confidence": 0.6,
      "derived_from_event_id": "event:ID17",
      "evidence_id": "evidence:ID24",
      "evidence_status": "FRESH",
      "id": "event:ID28",
      "information_scope": "CANON",
      "project_id": "project:ID1",
      "source": "extractor",
      "status": "ACTIVE",
      "summary": "萧决得知血脉秘密。"
    },
    "knowers": [
      {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      }
    ],
    "participants": [
      {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      },
      {
        "id": "character:ID8",
        "label": "Character",
        "name": "李管家"
      }
    ],
    "revealed_facts": []
  },
  "knowers_added": [],
  "knowers_removed": [
  …（截断，共 50 行）
```
---
### `POST /api/projects/{project_id}/canon/knowledge`

- **概要**：Edit Canon Knowledge
- **语义/数据走向**：「他知道 X」改成「他以为 X」，或者反过来。**旧的那条留着（RETRACTED）。**
- **涉及存储**：edge(KNOWS/BELIEVES) + evidence + decision_log
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `KnowledgeEditRequest`（组件模型）
    - `character_id` string（必填）
    - `secret_id` string（必填）
    - `to_type` string（必填）
    - `believed_value` string | null（可选）
    - `expected_canon_version` integer（必填）
- **响应**：
  `KnowledgeCorrection`（组件模型）
    - `project_id` string（必填）
    - `canon_version` integer（必填）
    - `decision_id` string（必填）
    - `character` NodeRef（必填）
    - `secret` NodeRef（必填）
    - `from_type` EdgeType（必填）
    - `to_type` EdgeType（必填）
    - `believed_value` string | null（可选）
    - `since_chapter` integer（必填）
    - `edge_id` string（必填）
    - `retracted_edge_id` string（必填）
    - `closed_edge_ids` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/canonKnowledgeAdded`，共 1 个变体）:

```json
{
  "believed_value": "以为那是老爷编出来的",
  "canon_version": 15,
  "character": {
    "id": "character:ID8",
    "label": "Character",
    "name": "李管家"
  },
  "closed_edge_ids": [],
  "decision_id": "decision:ID64",
  "edge_id": "edge:ID65",
  "project_id": "project:ID1",
  "secret": {
    "id": "secret:ID7",
    "label": "Secret",
    "name": "血脉秘密"
  },
  "since_chapter": 2,
  "type": "BELIEVES"
}
```
---
### `POST /api/projects/{project_id}/chapters/{chapter}/canon/knowledge`

- **概要**：Add Canon Knowledge
- **语义/数据走向**：在一格「不知道」上补一条「他知道 / 他以为」。**这一格已经有内容时 409。**
- **涉及存储**：edge(KNOWS/BELIEVES) + evidence + decision_log
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `KnowledgeAddRequest`（组件模型）
    - `character_id` string（必填）
    - `secret_id` string（必填）
    - `type` string（必填）
    - `believed_value` string | null（可选）
    - `expected_canon_version` integer（必填）
- **响应**：
  `KnowledgeAddition`（组件模型）
    - `project_id` string（必填）
    - `canon_version` integer（必填）
    - `decision_id` string（必填）
    - `character` NodeRef（必填）
    - `secret` NodeRef（必填）
    - `type` EdgeType（必填）
    - `believed_value` string | null（可选）
    - `since_chapter` integer（必填）
    - `edge_id` string（必填）
    - `closed_edge_ids` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/canonKnowledgeAdded`，共 1 个变体）:

```json
{
  "believed_value": "以为那是老爷编出来的",
  "canon_version": 15,
  "character": {
    "id": "character:ID8",
    "label": "Character",
    "name": "李管家"
  },
  "closed_edge_ids": [],
  "decision_id": "decision:ID64",
  "edge_id": "edge:ID65",
  "project_id": "project:ID1",
  "secret": {
    "id": "secret:ID7",
    "label": "Secret",
    "name": "血脉秘密"
  },
  "since_chapter": 2,
  "type": "BELIEVES"
}
```
---
### `POST /api/projects/{project_id}/declare/death`

- **概要**：Declare Dead
- **语义/数据走向**：「他在这段原文里死了」。**R3 DEAD_SPEAKS 的生产写入方之一**（另一个是抽取器的
- **涉及存储**：node(status)+edge(HAS_STATE/死亡)+decision_log
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `DeclareDeadBody`（组件模型）
    - `who` string（必填）
    - `quote` string（必填）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `POST /api/projects/{project_id}/declare/first-appearance`

- **概要**：Declare First Appearance
- **语义/数据走向**：「他/它在这段原文里头一回露面」→ 首现章。**R2 FUTURE_LEAK 的作者入口。**
- **涉及存储**：node(first_appearance)+edge
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `DeclareFirstAppearanceBody`（组件模型）
    - `of` string（必填）
    - `quote` string（必填）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---

## 节点与别名（8 类节点 / 索引表）

### `POST /api/projects/{project_id}/aliases`

- **概要**：Declare Alias
- **语义/数据走向**：给已有节点加一个称呼。别名永不合并实体（ADR 0004）。
- **涉及存储**：alias（索引表，非事实）
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `DeclareAliasBody`（组件模型）
    - `of` string（必填）
    - `surface` string（必填）
    - `kind` AliasKind（可选）
    - `usable_for_rules` boolean（可选）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/createAlias`，共 2 个变体）:

```json
{
  "id": "alias:ID14",
  "kind": "alias",
  "node_id": "character:ID12",
  "project_id": "project:ID1",
  "surface": "顾姑娘",
  "usable_for_rules": true
}
```
---
### `POST /api/projects/{project_id}/nodes`

- **概要**：Declare Node
- **语义/数据走向**：声明一个节点（8 类 label）。幂等（键 = name）。
- **涉及存储**：node（8 类）
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `DeclareNodeBody`（组件模型）
    - `label` NodeLabel（必填）
    - `name` string（必填）
    - `aliases` array（可选）
    - `description` string（可选）
    - `sub_of` string | null（可选）
    - `first_appears_chapter` integer | null（可选）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/createNode`，共 2 个变体）:

```json
{
  "id": "character:ID12",
  "label": "Character",
  "name": "顾清音",
  "project_id": "project:ID1",
  "props": {
    "background": null,
    "character_notes": null,
    "dim_key": null,
    "first_appears_chapter": null,
    "gender": null,
    "main_character": null,
    "personality": null
  }
}
```
---

## 一致性检查（R2/R3）

### `POST /api/projects/{project_id}/chapters/{chapter}/check`

- **概要**：Check
- **语义/数据走向**：对第 chapter 章的磁盘正文跑一致性规则（今天 = R2 / R3；R4 和 R5 都已砍）。
- **涉及存储**：checks R2/R3（不落 validation_report，返 issues）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/check`，共 1 个变体）:

```json
{
  "chapter": 3,
  "issues": [],
  "rules_run": [
    "future_leak",
    "dead_speaks"
  ]
}
```
---

## 章节总结

### `GET /api/projects/{project_id}/chapters/{chapter}/summaries`

- **概要**：Chapter Summaries
- **语义/数据走向**：起草第 chapter 章时，滚动总结那一层覆盖的区间里每一章的状态。
- **涉及存储**：chapter_summary
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/summaries`，共 1 个变体）:

```json
{
  "chapter": 12,
  "chapters": [
    {
      "author_written": false,
      "chapter_number": 1,
      "created_at": "<ts>",
      "has_text": true,
      "retracted": false,
      "summary": "萧决在青云城主府听说了血脉秘密。"
    },
    {
      "author_written": false,
      "chapter_number": 2,
      "created_at": null,
      "has_text": true,
      "retracted": false,
      "summary": null
    },
    {
      "author_written": false,
      "chapter_number": 3,
      "created_at": null,
      "has_text": false,
      "retracted": false,
      "summary": null
    },
    {
      "author_written": false,
      "chapter_number": 4,
      "created_at": null,
      "has_text": false,
      "retracted": false,
      "summary": null
    },
    {
      "author_written": false,
      "chapter_number": 5,
      "created_at": null,
      "has_text": false,
  …（截断，共 99 行）
```
---
### `GET /api/projects/{project_id}/chapters/{chapter}/summary`

- **概要**：Chapter Summary
- **语义/数据走向**：这一章现在的总结。**没有的时候不许只回一个 null**（§10 约束 8）。
- **涉及存储**：chapter_summary（迁移 013 source/status）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/summaryChapter`，共 1 个变体）:

```json
{
  "author_written": false,
  "chapter_number": 1,
  "created_at": "<ts>",
  "has_text": true,
  "retracted": false,
  "summary": "萧决在青云城主府听说了血脉秘密。"
}
```
---
### `POST /api/projects/{project_id}/chapters/{chapter}/summary`

- **概要**：Generate Chapter Summary
- **语义/数据走向**：**显式**为第 chapter 章生成滚动总结（会调模型、会花钱）。
- **涉及存储**：chapter_summary（迁移 013 source/status）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/summaryGenerated`，共 1 个变体）:

```json
{
  "author_written": false,
  "chapter_number": 1,
  "created_at": "<ts>",
  "has_text": true,
  "retracted": false,
  "summary": "萧决在青云城主府听说了血脉秘密。"
}
```
---
### `PATCH /api/projects/{project_id}/chapters/{chapter}/summary`

- **概要**：Edit Chapter Summary
- **语义/数据走向**：把这一章的总结换成作者自己写的这一段。**不花钱**，模型写的那一行留在库里。
- **涉及存储**：chapter_summary（迁移 013 source/status）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `SummaryEdit`（组件模型）
    - `summary` string（必填）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/summaryEdited`，共 1 个变体）:

```json
{
  "author_written": true,
  "chapter_number": 2,
  "created_at": "<ts>",
  "has_text": true,
  "retracted": false,
  "summary": "萧决把玄铁令收进袖中，谁也没告诉。"
}
```
---
### `DELETE /api/projects/{project_id}/chapters/{chapter}/summary`

- **概要**：Retract Chapter Summary
- **语义/数据走向**：撤回这一章的总结。**库里一行都不少**（迁移 013：追加一行标记撤回）。
- **涉及存储**：chapter_summary（迁移 013 source/status）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/summaryRetracted`，共 1 个变体）:

```json
{
  "author_written": false,
  "chapter_number": 2,
  "created_at": null,
  "has_text": true,
  "retracted": true,
  "summary": null
}
```
---
### `GET /api/projects/{project_id}/chapters/{chapter}/summary/mentions`

- **概要**：Chapter Summary Mentions
- **语义/数据走向**：这一章**现在算数**的那段总结里，花名册的哪些东西被提到了。
- **涉及存储**：summary_mention / summary_index
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/summaryMentions`，共 2 个变体）:

```json
{
  "chapter": 1,
  "mentions": [
    {
      "node": {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      },
      "surfaces": [
        "萧决"
      ]
    },
    {
      "node": {
        "id": "location:ID5",
        "label": "Location",
        "name": "青云城主府"
      },
      "surfaces": [
        "青云城主府"
      ]
    },
    {
      "node": {
        "id": "secret:ID7",
        "label": "Secret",
        "name": "血脉秘密"
      },
      "surfaces": [
        "血脉秘密"
      ]
    }
  ]
}
```
---

## 候选稿起草

### `POST /api/projects/{project_id}/chapters/{chapter}/draft`

- **概要**：Draft
- **语义/数据走向**：AI 起草第 N 章（**实验状态**，修正案 7）。
- **涉及存储**：draft/context + LLM → draft_candidate
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `DraftRequest`（组件模型）
    - `mode` string（可选）
    - `goal` string（可选）
    - `cast` array（可选）
    - `length` LengthSpec（必填）
    - `form` string（可选）
    - `previous_tail` string（可选）
    - `write_rule` string（可选）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `POST /api/projects/{project_id}/chapters/{chapter}/plan`

- **概要**：Plan Stub
- **语义/数据走向**：AI 规划第 N 章的场景骨架（M2）。v1 的替代是作者手拖手填场景块（§2 表）。
- **请求参数**：
  - （无）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
---
### `GET /api/projects/{project_id}/drafts`

- **概要**：List Drafts
- **语义/数据走向**：助手写过的那几稿，最近的在前（ADR 0022）。
- **涉及存储**：draft_candidate（按章）
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `chapter`（query，可选，`?`）
  - `limit`（query，可选，`integer`，默认 `20`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `ChapterDrafts`（组件模型）
    - `drafts` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/drafts`，共 1 个变体）:

```json
{
  "drafts": [
    {
      "chapter": 2,
      "created_at": "<ts>",
      "id": "draft:ID42",
      "landed": false,
      "note": "这一版更冷，收在他没抬头。",
      "ordinal": 1,
      "preview": "风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他……",
      "stopped_reason": "",
      "units": 2800
    }
  ]
}
```
---
### `GET /api/projects/{project_id}/drafts/{draft_id}`

- **概要**：Read Draft
- **语义/数据走向**：摊开某一稿的全文。**界面上「推荐那一版摊开」读的就是它**（ADR 0022 的入口形态）。
- **涉及存储**：draft_candidate（单条不可变）
- **请求参数**：
  - `draft_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `DraftCandidateDetail`（组件模型）
    - `id` string（必填）
    - `chapter` integer（必填）
    - `ordinal` integer（必填）
    - `units` integer（必填）
    - `note` string（可选）
    - `preview` string（可选）
    - `created_at` string（可选）
    - `landed` boolean（可选）
    - `stopped_reason` string（可选）
    - `text` string（可选）
- **真实响应样例**（`__fixtures__/api.json#/draftDetail`，共 1 个变体）:

```json
{
  "chapter": 2,
  "created_at": "<ts>",
  "id": "draft:ID42",
  "landed": false,
  "note": "这一版更冷，收在他没抬头。",
  "ordinal": 1,
  "preview": "风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他……",
  "stopped_reason": "",
  "text": "风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。",
  "units": 2800
}
```
---

## 机器抽取（M4）与提案审阅

### `GET /api/projects/{project_id}/chapters/{chapter}/events`

- **概要**：Chapter Events
- **涉及存储**：story_event/event_participant/event_knower/event_reveal
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
  - `scope`（query，必填，`?`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/eventsCanon`，共 2 个变体）:

```json
[
  {
    "event": {
      "chapter_number": 1,
      "confidence": 0.6,
      "derived_from_event_id": "event:ID17",
      "evidence_id": "evidence:ID24",
      "evidence_status": "FRESH",
      "id": "event:ID28",
      "information_scope": "CANON",
      "project_id": "project:ID1",
      "source": "extractor",
      "status": "ACTIVE",
      "summary": "萧决得知血脉秘密。"
    },
    "knowers": [
      {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      },
      {
        "id": "character:ID8",
        "label": "Character",
        "name": "李管家"
      }
    ],
    "participants": [
      {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      },
      {
        "id": "character:ID8",
        "label": "Character",
        "name": "李管家"
      }
    ],
    "revealed_facts": []
  …（截断，共 82 行）
```
---
### `POST /api/projects/{project_id}/chapters/{chapter}/extract`

- **概要**：Extract Chapter
- **涉及存储**：extract/runner + LLM → extraction_run/story_event/proposal_set/evidence
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
  - `force`（query，可选，`boolean`，默认 `False`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/extractionRun`，共 1 个变体）:

```json
{
  "chapter_number": 1,
  "created_at": "<ts>",
  "discarded_event_count": 1,
  "errors": [],
  "finished_at": "<ts>",
  "id": "extraction_run:ID36",
  "model_call_id": "call:ID37",
  "project_id": "project:ID1",
  "prompt_hash": "prompt:1",
  "proposal_count": 2,
  "schema_version": "m4.analysis.v1",
  "snapshot_id": "snapshot:ID11",
  "started_at": "<ts>",
  "status": "SUCCEEDED",
  "valid_event_count": 3
}
```
---
### `GET /api/projects/{project_id}/chapters/{chapter}/proposals`

- **概要**：Chapter Proposals
- **语义/数据走向**：待审队列。出参**带着 `items` 里那些 id 的显示名**（`node_refs`）。
- **涉及存储**：proposal_set（待审队列，按章）
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
  - `status`（query，可选，`?`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/proposals`，共 1 个变体）:

```json
[
  {
    "audit_envelope": null,
    "base_canon_version": 10,
    "chapter_number": 1,
    "confidence": null,
    "created_at": "<ts>",
    "decision_log_id": null,
    "edge_ids": [],
    "event_ids": [
      "event:ID17"
    ],
    "id": "proposal:ID18",
    "item_count": 1,
    "items": [
      {
        "confidence": 0.6,
        "event_id": "event:ID17",
        "quote": "萧决在青云城主府第一次听说了血脉秘密的真相。",
        "source_kind": "event",
        "summary": "萧决得知血脉秘密。"
      }
    ],
    "kind": "low_confidence_main",
    "node_refs": [],
    "project_id": "project:ID1",
    "prompt_hash": "prompt:api-test",
    "resolution_action": null,
    "resolved_at": null,
    "resolved_canon_version": null,
    "schema_version": "m4.analysis.v1",
    "snapshot_id": "snapshot:ID11",
    "status": "PENDING",
    "summary": ""
  },
  {
    "audit_envelope": null,
    "base_canon_version": 10,
    "chapter_number": 1,
    "confidence": null,
  …（截断，共 132 行）
```
---
### `POST /api/projects/{project_id}/chapters/{chapter}/provisional/confirm`

- **概要**：Confirm Provisional
- **涉及存储**：provisional(未产生 proposal_set) 直落 PROVISIONAL 确认
- **请求参数**：
  - `chapter`（path，必填，`integer`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `ProvisionalConfirmRequest`（组件模型）
    - `fact_kind` string（必填）
    - `fact_ids` array（必填）
    - `expected_canon_version` integer（必填）
- **响应**：
  `ProvisionalConfirmation`（组件模型）
    - `confirmation_id` string（必填）
    - `project_id` string（必填）
    - `canon_version` integer（必填）
    - `decision_id` string（必填）
    - `events` array（可选）
    - `edges` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/provisionalConfirm`，共 1 个变体）:

```json
{
  "canon_version": 12,
  "confirmation_id": "proposal:ID31",
  "decision_id": "decision:ID32",
  "edges": [],
  "events": [
    {
      "event": {
        "chapter_number": 1,
        "confidence": 0.6,
        "derived_from_event_id": "event:ID25",
        "evidence_id": "evidence:ID26",
        "evidence_status": "FRESH",
        "id": "event:ID33",
        "information_scope": "CANON",
        "project_id": "project:ID1",
        "source": "extractor",
        "status": "ACTIVE",
        "summary": "萧决得知血脉秘密。"
      },
      "knowers": [
        {
          "id": "character:ID10",
          "label": "Character",
          "name": "萧决"
        },
        {
          "id": "character:ID8",
          "label": "Character",
          "name": "李管家"
        }
      ],
      "participants": [
        {
          "id": "character:ID10",
          "label": "Character",
          "name": "萧决"
        },
        {
          "id": "character:ID8",
  …（截断，共 49 行）
```
---
### `GET /api/projects/{project_id}/extractions/{run_id}`

- **概要**：Extraction Status
- **涉及存储**：extraction_run（轮询终态）
- **请求参数**：
  - `run_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `ExtractionRunView`（组件模型）
    - `id` string（必填）
    - `project_id` string（必填）
    - `chapter_number` integer（必填）
    - `snapshot_id` string（必填）
    - `status` ExtractionRunStatus（必填）
    - `errors` array（可选）
    - `valid_event_count` integer（必填）
    - `discarded_event_count` integer（必填）
    - `proposal_count` integer（必填）
    - `model_call_id` string | null（可选）
    - `schema_version` string（必填）
    - `prompt_hash` string（必填）
    - `created_at` string（必填）
    - `started_at` string | null（可选）
    - `finished_at` string | null（可选）
- **真实响应样例**（`__fixtures__/api.json#/extractionRun`，共 2 个变体）:

```json
{
  "chapter_number": 1,
  "created_at": "<ts>",
  "discarded_event_count": 1,
  "errors": [],
  "finished_at": "<ts>",
  "id": "extraction_run:ID36",
  "model_call_id": "call:ID37",
  "project_id": "project:ID1",
  "prompt_hash": "prompt:1",
  "proposal_count": 2,
  "schema_version": "m4.analysis.v1",
  "snapshot_id": "snapshot:ID11",
  "started_at": "<ts>",
  "status": "SUCCEEDED",
  "valid_event_count": 3
}
```
---
### `POST /api/projects/{project_id}/proposals/{proposal_id}/accept`

- **概要**：Accept Proposal
- **涉及存储**：proposal_set→ACCEPTED + decision_log + edge/node CANON
- **请求参数**：
  - `proposal_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `ProposalAcceptRequest`（组件模型）
    - `expected_canon_version` integer（必填）
- **响应**：
  `ProposalResolution`（组件模型）
    - `proposal_id` string（必填）
    - `status` string（必填）
    - `canon_version` integer（必填）
    - `decision_id` string（必填）
    - `event` #/components/schemas/EventView | null（可选）
    - `character` #/components/schemas/NodeRef | null（可选）
    - `events` array（可选）
    - `edges` array（可选）
    - `characters` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/proposalAccept`，共 1 个变体）:

```json
{
  "canon_version": 11,
  "character": null,
  "characters": [],
  "decision_id": "decision:ID27",
  "edges": [],
  "event": {
    "event": {
      "chapter_number": 1,
      "confidence": 0.6,
      "derived_from_event_id": "event:ID17",
      "evidence_id": "evidence:ID24",
      "evidence_status": "FRESH",
      "id": "event:ID28",
      "information_scope": "CANON",
      "project_id": "project:ID1",
      "source": "extractor",
      "status": "ACTIVE",
      "summary": "萧决得知血脉秘密。"
    },
    "knowers": [
      {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      },
      {
        "id": "character:ID8",
        "label": "Character",
        "name": "李管家"
      }
    ],
    "participants": [
      {
        "id": "character:ID10",
        "label": "Character",
        "name": "萧决"
      },
      {
        "id": "character:ID8",
  …（截断，共 91 行）
```
---
### `POST /api/projects/{project_id}/proposals/{proposal_id}/edit`

- **概要**：Edit Proposal
- **语义/数据走向**：接受这条提案，但按作者改过的样子落进 CANON。
- **涉及存储**：proposal_set→EDITED + decision_log + write-back
- **请求参数**：
  - `proposal_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `ProposalEditRequest`（组件模型）
    - `edited_summary` string | null（可选）
    - `knower_ids` array | null（可选）
    - `participant_ids` array | null（可选）
    - `expected_canon_version` integer（必填）
- **响应**：
  `ProposalResolution`（组件模型）
    - `proposal_id` string（必填）
    - `status` string（必填）
    - `canon_version` integer（必填）
    - `decision_id` string（必填）
    - `event` #/components/schemas/EventView | null（可选）
    - `character` #/components/schemas/NodeRef | null（可选）
    - `events` array（可选）
    - `edges` array（可选）
    - `characters` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/proposalEdit`，共 1 个变体）:

```json

```
---
### `POST /api/projects/{project_id}/proposals/{proposal_id}/reject`

- **概要**：Reject Proposal
- **涉及存储**：proposal_set→REJECTED + decision_log
- **请求参数**：
  - `proposal_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `ProposalRejectRequest`（组件模型）
    - `action` string（必填）
    - `expected_canon_version` integer（必填）
- **响应**：
  `ProposalResolution`（组件模型）
    - `proposal_id` string（必填）
    - `status` string（必填）
    - `canon_version` integer（必填）
    - `decision_id` string（必填）
    - `event` #/components/schemas/EventView | null（可选）
    - `character` #/components/schemas/NodeRef | null（可选）
    - `events` array（可选）
    - `edges` array（可选）
    - `characters` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/proposalReject`，共 1 个变体）:

```json
{
  "canon_version": 11,
  "character": null,
  "characters": [],
  "decision_id": "decision:ID30",
  "edges": [],
  "event": null,
  "events": [],
  "proposal_id": "proposal:ID29",
  "status": "REJECTED"
}
```
---

## 写作助手对话

### `GET /api/projects/{project_id}/chats`

- **概要**：List Chats
- **语义/数据走向**：侧栏那一列，最近说过话的在前。
- **涉及存储**：chat_session
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `limit`（query，可选，`integer`，默认 `50`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/chats`，共 1 个变体）:

```json
[
  {
    "created_at": "<ts>",
    "id": "chat_session:ID41",
    "message_count": 9,
    "pending_lookups": 0,
    "running": false,
    "title": "第 2 章能说破血脉的事吗？",
    "updated_at": "<ts>"
  }
]
```
---
### `POST /api/projects/{project_id}/chats`

- **概要**：Create Chat
- **语义/数据走向**：开一段新的对话。**作者可以同时开好几段**，每一段各自 resume。
- **涉及存储**：chat_session
- **请求参数**：
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `NewChat`（组件模型）
    - `title` string（可选）
    - `write_rule` string（可选）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/chatCreated`，共 1 个变体）:

```json
{
  "created_at": "<ts>",
  "id": "chat_session:ID41",
  "message_count": 0,
  "pending_lookups": 0,
  "running": false,
  "title": "",
  "updated_at": "<ts>"
}
```
---
### `GET /api/projects/{project_id}/chats/{chat_id}`

- **概要**：Read Chat
- **语义/数据走向**：看一段对话。404 = 这一段不在（或不属于这本书），和 501 分得开。
- **涉及存储**：chat_session + chat_message
- **请求参数**：
  - `chat_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `ChatDetail`（组件模型）
    - `session` ChatSessionView（必填）
    - `messages` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/chatDetail`，共 2 个变体）:

```json
{
  "messages": [
    {
      "seq": 0,
      "speaker": "author",
      "text": "第 2 章能说破血脉的事吗？"
    },
    {
      "seq": 8,
      "speaker": "assistant",
      "text": "第 2 章这一场，血脉那条先别说破。我写了一稿，你看看要不要。"
    }
  ],
  "session": {
    "created_at": "<ts>",
    "id": "chat_session:ID41",
    "message_count": 9,
    "pending_lookups": 0,
    "running": false,
    "title": "第 2 章能说破血脉的事吗？",
    "updated_at": "<ts>"
  }
}
```
---
### `DELETE /api/projects/{project_id}/chats/{chat_id}`

- **概要**：Delete Chat
- **语义/数据走向**：删掉一段对话。**这不会动到任何一行正文**——正文在磁盘上，这两张表里没有它。
- **涉及存储**：chat_session + chat_message
- **请求参数**：
  - `chat_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `ChatDeleted`（组件模型）
    - `chat_id` string（必填）
    - `deleted` boolean（必填）
- **真实响应样例**（`__fixtures__/api.json#/chatDeleted`，共 1 个变体）:

```json
{
  "chat_id": "chat_session:ID45",
  "deleted": true
}
```
---
### `POST /api/projects/{project_id}/chats/{chat_id}/stop`

- **概要**：Stop Chat
- **语义/数据走向**：按下「停」。**它不等这一轮跑完**——信号交给正在跑的那一轮，跑一轮的那个请求
- **涉及存储**：中断回合（候选停止原因）
- **请求参数**：
  - `chat_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（可选请求体）：
  （inline schema: None）
- **响应**：
  `ChatStopped`（组件模型）
    - `chat_id` string（必填）
    - `stopped` boolean（必填）
    - `message` string（必填）
- **真实响应样例**（`__fixtures__/api.json#/chatStopped`，共 1 个变体）:

```json
{
  "chat_id": "chat_session:ID41",
  "message": "这段对话这会儿没在跑，不用停。",
  "stopped": false
}
```
---
### `POST /api/projects/{project_id}/chats/{chat_id}/turn`

- **概要**：Run Chat
- **语义/数据走向**：跑一轮，**跑完才回**（十一种停法）。
- **涉及存储**：agent/loop + LLM + 白名单工具 → chat_message + draft_candidate/summary/story_event（副作用）
- **请求参数**：
  - `chat_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `TurnBody`（组件模型）
    - `chapter` integer（必填）
    - `said` string（可选）
    - `run_id` string（可选）
- **响应**：
  `TurnReceipt`（组件模型）
    - `session` ChatSessionView（必填）
    - `chapter` integer（必填）
    - `reason` StopReason（必填）
    - `message` string（必填）
    - `reply` string（可选）
    - `messages` array（可选）
    - `steps` integer（可选）
    - `lookups` integer（可选）
    - `tokens_reported` integer（可选）
    - `calls_without_usage` integer（可选）
    - `context` ContextReceipt（可选）
    - `asked` #/components/schemas/AuthorQuestion | null（可选）
    - `drafts` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/chatTurn`，共 1 个变体）:

```json
{
  "asked": null,
  "calls_without_usage": 3,
  "chapter": 2,
  "context": {
    "compressed_blocks": 0,
    "dropped_lookups": 0,
    "dropped_reasoning": 0,
    "full": false,
    "lost_lookups": 0,
    "off_chapter": 0,
    "stale_lookups": 0,
    "trimmed_results": 0
  },
  "drafts": [
    {
      "chapter": 2,
      "created_at": "<ts>",
      "id": "draft:ID42",
      "landed": false,
      "note": "这一版更冷，收在他没抬头。",
      "ordinal": 1,
      "preview": "风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他……",
      "stopped_reason": "",
      "units": 2800
    }
  ],
  "lookups": 4,
  "message": "说完了。",
  "messages": [
    {
      "seq": 8,
      "speaker": "assistant",
      "text": "第 2 章这一场，血脉那条先别说破。我写了一稿，你看看要不要。"
    }
  ],
  "reason": "done",
  "reply": "第 2 章这一场，血脉那条先别说破。我写了一稿，你看看要不要。",
  "session": {
    "created_at": "<ts>",
  …（截断，共 50 行）
```
---
### `POST /api/projects/{project_id}/chats/{chat_id}/turn/events`

- **概要**：Stream Chat
- **语义/数据走向**：跑一轮，**边跑边说**（ADR 0024 决策一 / 三）。
- **涉及存储**：回合事件流（SSE 管道）
- **请求参数**：
  - `chat_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - Content-Type `application/json`（必填请求体）：
  `TurnBody`（组件模型）
    - `chapter` integer（必填）
    - `said` string（可选）
    - `run_id` string（可选）
- **响应**：
  - `200` 响应**无 response_model**（收窄 dict / 由前端手写类型定义形状）。
- **真实响应样例**（`__fixtures__/api.json#/chatTurnEvents`，共 1 个变体）:

```json
[
  "event: turn\ndata: {\"kind\":\"tool_started\",\"said_to_author\":\"正在写前校准。\",\"text\":\"\",\"tool\":\"calibrate_scene\",\"ok\":null,\"chapter\":null,\"index\":1,\"total\":1,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"tool_finished\",\"said_to_author\":\"写前校准（第 2 章），好了。\",\"text\":\"\",\"tool\":\"calibrate_scene\",\"ok\":true,\"chapter\":2,\"index\":1,\"total\":1,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"tool_started\",\"said_to_author\":\"正在封存写作简报。\",\"text\":\"\",\"tool\":\"seal_scene_brief\",\"ok\":null,\"chapter\":null,\"index\":1,\"total\":1,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"tool_finished\",\"said_to_author\":\"封存写作简报（第 2 章），好了。\",\"text\":\"\",\"tool\":\"seal_scene_brief\",\"ok\":true,\"chapter\":2,\"index\":1,\"total\":1,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"tool_started\",\"said_to_author\":\"正在查这一章不许说破什么（这一批 2 件里的第 1 件）。\",\"text\":\"\",\"tool\":\"scene_constraints\",\"ok\":null,\"chapter\":null,\"index\":1,\"total\":2,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"tool_finished\",\"said_to_author\":\"查这一章不许说破什么（第 2 章），好了。\",\"text\":\"\",\"tool\":\"scene_constraints\",\"ok\":true,\"chapter\":2,\"index\":1,\"total\":2,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"tool_started\",\"said_to_author\":\"正在写一稿（这一批 2 件里的第 2 件）。\",\"text\":\"\",\"tool\":\"draft_chapter\",\"ok\":null,\"chapter\":null,\"index\":2,\"total\":2,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"draft_started\",\"said_to_author\":\"正在写第 2 章的一稿。\",\"text\":\"\",\"tool\":\"\",\"ok\":null,\"chapter\":2,\"index\":0,\"total\":0,\"stream\":1,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"draft_kept\",\"said_to_author\":\"第 2 章的第 2 稿写好了，2800 字。\",\"text\":\"\",\"tool\":\"\",\"ok\":null,\"chapter\":2,\"index\":0,\"total\":0,\"stream\":1,\"ordinal\":2,\"units\":2800,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"tool_finished\",\"said_to_author\":\"写一稿（第 2 章），好了。\",\"text\":\"\",\"tool\":\"draft_chapter\",\"ok\":true,\"chapter\":2,\"index\":2,\"total\":2,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"reply_text\",\"said_to_author\":\"\",\"text\":\"第 2 章这一场，血脉那条先别说破。我写了一稿，你看看要不要。\",\"tool\":\"\",\"ok\":null,\"chapter\":null,\"index\":0,\"total\":0,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":null,\"asked\":null}\n\n",
  "event: turn\ndata: {\"kind\":\"turn_stopped\",\"said_to_author\":\"说完了。\",\"text\":\"\",\"tool\":\"\",\"ok\":null,\"chapter\":null,\"index\":0,\"total\":0,\"stream\":0,\"ordinal\":0,\"units\":0,\"reason\":\"done\",\"asked\":null}\n\n",
  "event: receipt\ndata: {\"session\":{\"id\":\"chat_session:ID43\",\"title\":\"长连接那一轮\",\"created_at\":\"<ts>\",\"updated_at\":\"<ts>\",\"message_count\":9,\"running\":false,\"pending_lookups\":0},\"chapter\":2,\"reason\":\"done\",\"message\":\"说完了。\",\"reply\":\"第 2 章这一场，血脉那条先别说破。我写了一稿，你看看要不要。\",\"messages\":[{\"seq\":8,\"speaker\":\"assistant\",\"text\":\"第 2 章这一场，血脉那条先别说破。我写了一稿，你看看要不要。\"}],\"steps\":4,\"lookups\":4,\"tokens_reported\":5664,\"calls_without_usage\":3,\"context\":{\"off_chapter\":0,\"stale_lookups\":0,\"trimmed_results\":0,\"dropped_lookups\":0,\"dropped_reasoning\":0,\"lost_lookups\":0,\"full\":false,\"compressed_blocks\":0},\"asked\":null,\"drafts\":[{\"id\":\"draft:ID44\",\"chapter\":2,\"ordinal\":2,\"units\":2800,\"note\":\"这一版更冷，收在他没抬头。\",\"preview\":\"风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他终于抬起头。风雪落在肩上，他……\",\"created_at\":\"<ts>\",\"landed\":false,\"stopped_reason\":\"\"}]}\n\n"
]
```
---

## 活动流与运行记账

### `GET /api/projects/{project_id}/activity`

- **概要**：Activity
- **语义/数据走向**：日志页的折叠行，新的在前。
- **涉及存储**：activity 事件流（合并通道）
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `actor`（query，可选，`?`）按 actor 过滤，今天是 author / system
  - `limit`（query，可选，`integer`，默认 `50`）
  - `cursor`（query，可选，`?`）上一页返回的 next_cursor，原样回传
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `ActivityPage`（组件模型）
    - `entries` array（可选）
    - `next_cursor` string | null（可选）
    - `actors` array（可选）
- **真实响应样例**（`__fixtures__/api.json#/activity`，共 3 个变体）:

```json
{
  "actors": [
    {
      "actor": "author",
      "count": 14
    },
    {
      "actor": "system",
      "count": 3
    }
  ],
  "entries": [
    {
      "actor": "system",
      "chapter_number": 1,
      "id": "extraction_run:ID36",
      "jump": {
        "cast": [],
        "chapter_number": 1,
        "character_id": null,
        "endpoints": [],
        "event_id": null,
        "label": "第 1 章还有 2 条待审",
        "proposal_id": null,
        "secret_id": null,
        "target": "proposal"
      },
      "source": "extraction",
      "status": "succeeded",
      "subtitle": "有效事件 3 条 · 丢弃 1 条 · 待审提案 2 条",
      "title": "第 1 章抽取",
      "ts": "<ts>"
    },
    {
      "actor": "system",
      "chapter_number": 1,
      "id": "call:ID37",
      "jump": {
        "cast": [],
        "chapter_number": 1,
  …（截断，共 197 行）
```
---
### `GET /api/projects/{project_id}/activity/{entry_id}`

- **概要**：Activity Detail
- **语义/数据走向**：展开某一条：跑了什么、结果是什么、花了多少。
- **涉及存储**：单条活动详情（decision/model_call/run/summary）
- **请求参数**：
  - `entry_id`（path，必填，`string`）
  - `project_id`（path，必填，`string`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `ActivityDetail`（组件模型）
    - `entry` ActivityEntry（必填）
    - `rows` array（可选）
    - `cost` #/components/schemas/ActivityCost | null（可选）
    - `errors` array（可选）
    - `payload` object | null（可选）
- **真实响应样例**（`__fixtures__/api.json#/activityCallDetail`，共 5 个变体）:

```json
{
  "cost": {
    "call_id": "call:ID37",
    "capability": "extractor",
    "cost": null,
    "model": "deepseek-v4",
    "ms": 900,
    "tokens_in": 1200,
    "tokens_out": 400
  },
  "entry": {
    "actor": "system",
    "chapter_number": 1,
    "id": "call:ID37",
    "jump": {
      "cast": [],
      "chapter_number": 1,
      "character_id": null,
      "endpoints": [],
      "event_id": null,
      "label": "去第 1 章",
      "proposal_id": null,
      "secret_id": null,
      "target": "chapter"
    },
    "source": "model_call",
    "status": "succeeded",
    "subtitle": "deepseek-v4 · 入 1200 / 出 400 token · 900 ms",
    "title": "模型调用 · 抽取",
    "ts": "<ts>"
  },
  "errors": [],
  "payload": null,
  "rows": [
    {
      "label": "能力",
      "value": "抽取"
    },
    {
      "label": "模型",
  …（截断，共 68 行）
```
---
### `GET /api/projects/{project_id}/rules`

- **概要**：Recorded Rules
- **语义/数据走向**：作者交代过的每一条规矩，**按他说话时在写第几章排**。
- **涉及存储**：chat_message.rule_until / 有据可查的“记录为规则”
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `limit`（query，可选，`integer`，默认 `200`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `RecordedRules`（组件模型）
    - `rules` array（可选）
    - `scanned_chats` integer（可选）
- **真实响应样例**（`__fixtures__/api.json#/recordedRules`，共 1 个变体）:

```json
{
  "rules": [
    {
      "chapter": 2,
      "chat_id": "chat_session:ID46",
      "chat_title": "沙地那一段",
      "text": "男主在这片沙地不杀人",
      "until": "男主走出这片沙地为止"
    },
    {
      "chapter": 2,
      "chat_id": "chat_session:ID46",
      "chat_title": "沙地那一段",
      "text": "冷一点",
      "until": "这一场写完"
    }
  ],
  "scanned_chats": 3
}
```
---
### `GET /api/projects/{project_id}/runs`

- **概要**：Runs
- **语义/数据走向**：底栏「最近运行 / Token / 成本」。
- **涉及存储**：model_call 汇总（tokens/成本，按 run 归组）
- **请求参数**：
  - `project_id`（path，必填，`string`）
  - `limit`（query，可选，`integer`，默认 `20`）
- **请求体**：
  - （无请求体 GET/DELETE）
- **响应**：
  `RunsPanel`（组件模型）
    - `entries` array（可选）
    - `run_count` integer（可选）
    - `totals` CostTotals（必填）
- **真实响应样例**（`__fixtures__/api.json#/runs`，共 4 个变体）:

```json
{
  "entries": [
    {
      "actor": "system",
      "chapter_number": 1,
      "id": "extraction_run:ID36",
      "jump": {
        "cast": [],
        "chapter_number": 1,
        "character_id": null,
        "endpoints": [],
        "event_id": null,
        "label": "第 1 章还有 2 条待审",
        "proposal_id": null,
        "secret_id": null,
        "target": "proposal"
      },
      "source": "extraction",
      "status": "succeeded",
      "subtitle": "有效事件 3 条 · 丢弃 1 条 · 待审提案 2 条",
      "title": "第 1 章抽取",
      "ts": "<ts>"
    }
  ],
  "run_count": 1,
  "totals": {
    "calls": 2,
    "cost": null,
    "metered_calls": 1,
    "ms": 900,
    "priced_calls": 0,
    "tokens_in": 1200,
    "tokens_out": 400
  }
}
```
---