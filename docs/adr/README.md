# 架构决策记录（ADR）

每份 ADR 回答四件事：**决策是什么、推翻了原需求文档的哪一条、论证、若此决策错误修复成本是什么**。

⚠️ **这张表 2026-09-06 补过一次：它停在 0039，而 0040–0043 已经落地了。**
一张停在半路的索引比没有索引更糟——它让人以为后面没有了。**加 ADR 的同时加这一行。**

v1 的完整实施计划见 [`../PLAN.md`](../PLAN.md)。

| # | 决策 | 推翻了原文档的 | 状态 |
|---|---|---|---|
| [0001](0001-no-neo4j-in-v1.md) | 故事图谱进 SQLite，不用 Neo4j | 原则 12、第 9 轮表态 | 已接受（含加回触发条件） |
| [0002](0002-no-vector-retrieval-in-v1.md) | v1 不做向量检索 | 原则 12、第 9/10 节 | 已接受（含 v1.1 选型与触发条件） |
| [0003](0003-stable-ulid-ids.md) | 业务 ID 用 ULID，不用 slug | 第 9 轮 ID 方案 | 已接受（Day 2 必须落地） |
| [0004](0004-declaration-over-extraction.md) | 声明优于抽取 | 第 22 节第 2 项、第 17 节 | 已接受（最重要的单点改动） |
| [0005](0005-set-judgment-only.md) | 只做集合判断，不做语义判断 | 第 13 节 Validator | 已接受（**R5 覆盖率待 Day 1 实测填入**） |
| [0006](0006-evidence-double-pointer.md) | Evidence 双指针 + STALE 不进队列 | 第 7/14 节的 offset 方案 | 已接受（Day 2 契约） |
| [0007](0007-manuscript-lives-on-disk.md) | 正文在磁盘上，v1 不做编辑器 | 第 4/17 节、第 22 节第 12 项 | 已接受（**2026-07-19 时机松动**：应用内 CM6 进 v1，正文真相源仍在磁盘） |
| [0008](0008-related-to-is-undirected.md) | `RELATED_TO` 无向，存储时规范化 | —（补 PLAN **从没回答过**的问题） | 已接受（M1 写关系边之前必须定死） |
| [0009](0009-m2-verdict.md) | M2 kill-gate 的裁决 | —（协议 §8 的时序被维护者裁定显式改变） | **已写（2026-08-03，维护者裁定）**——**非数据裁决**：泄漏统计未产出，「图谱约束有效」未证明；仪器保留可补跑（那份修正案的原话是「`nh gate` 保留为可跑仪器」；入口 2026-08-20 改成 `python -m novel_harness.gate`，见 [ADR 0034](0034-no-command-line-surface.md)） |
| [0010](0010-writer-boundary.md) | Writer 边界：`assemble()` 能看见什么 | —（补 `EVAL_PROTOCOL.md` §8 点名要的那份） | 已接受（**`draft/assemble.py` 存在之前必须定死**：第 4 道守卫明说拦不住「完整 PLANNED 进 prompt」，那一格只有它和 review 守得住） |
| [0011](0011-bilingual-draft-length.md) | 双语长度契约与通用模型调用计划 | 固定中文短输出 / Claude 原生调用假设 | 已接受（修正案 5 的产品边界；先于第一份 `runs/*.jsonl`） |
| [0012](0012-book-owns-its-db.md) | 库跟着书走：没有全局默认位置 | —（补 `__main__.py` 一直空着的那个决定） | 已接受（2026-08-02；`novel-harness` 不带参数 = 向上找最近的 `book.db`） |
| [0013](0013-draft-length-is-a-request-parameter.md) | 起草长度是请求参数，不是后端设置 | —（补 ADR 0011 没定的「选择权归谁、存在哪」） | 已接受（2026-08-02；作者随 `/draft` 发 `LengthSpec`，后端只验硬上限） |
| [0014](0014-r5-cut-by-quote-coverage.md) | R5 判据改为引语口径，实测 8.2% → 砍掉 R5 | —（补 ADR 0005 判据分母和探针分母不一致的洞） | 已接受（2026-08-02；M3 规则集 = R2/R3/R4） |
| [0015](0015-inline-continuation-is-a-short-draft.md) | 行内续写是 `/draft` 的一个短档；**不知道谁在场就全禁，不问作者**；D7（2026-09-10 补）：**novel-agent 模式下默认不续写**，设置里可放行（`continuation_in_agent_mode`） | —（补「作者停手就提示下一段」这个新能力该挂在哪） | 已接受（2026-08-06；**不知道谁在场就全禁，不问作者、不推衍**） |
| ~~0016~~ | ~~LLM 验证只对机器输出开火~~ | ~~部分推翻 ADR 0005~~ | **已撤回（2026-08-06，维护者裁定）**：那份论证的全部力量来自「验证器在隔离沙盒里跑、不跟作者说话」；不在沙盒里跑就撑不住。**ADR 0005 的「LLM Validator 砍到 v2」原判不动。** 号不复用 |
| [0017](0017-memory-is-an-export-not-a-source-of-truth.md) | 小说记忆导出成 md，但图仍是真相源 | —（补 agent 记忆该住哪、能不能存事实） | 已接受（2026-08-06） |
| [0018](0018-cast-is-derived-not-declared.md) | 在场从正文推，作者不填 | **本仓库自己**：顶栏「出场人物」框 + 「写作前简报」那套心智 | 已接受（2026-08-06；推导是超集 = 多禁 = fail-closed，**但界面上不许说「在场」**） |
| [0019](0019-agent-loop-not-graph.md) | 模式二走单 Agent 工具循环，**工具表就是权限边界**；**上下文投影按章号参数化** | 桌面那份编排综述 §5 的落地图（照 Neo4j + Qdrant 画的）；通行的「按时间近切上下文」 | 已接受（2026-08-06；循环归模型不归代码，不上 LangGraph/LangChain；六条边界里**边界一写错不可回收**，**边界五写错不会报错**） |
| [0020](0020-clean-extraction-auto-canon.md) | 干净的抽取结果**自动升 CANON**（`actor='system'`），三个例外 bucket 照旧进队列 | **本仓库自己**：ARCHITECTURE §10 约束 3 的后半句「Agent 不得污染 Canon」（前半句保留） | 已接受（2026-08-10；退路是**可查 + 可改**而非事前批准，**编辑能力必须先落地**；今天「不烦你」= 「也不用你」= 白抽） （**2026-08-25 两份补记**：① `new_character` 从例外表里拿掉——认不出就建，不再问，因为那条规矩在空角色册上的实际效果是「不工作而且没人告诉作者」，且它和 [ADR 0018](0018-cast-is-derived-not-declared.md) 同时成立不了；配套是角色册能删（自动建 + 不能删 = 单向阀）。② 累计信息量：**算出来了，那道「够不够、问不问」的闸没接**——阈值要几本书的分布才定得下来，而「问了没人答」正是 8-15 那次死锁的形状。**「不答默认建进去」是硬约束，真接闸那天不许省**） |
| [0021](0021-agent-writes-drafts-without-asking.md) | 起草**直接落盘**，退路是版本历史；唯一的闸是「拒绝覆盖作者更晚的改动」 | **本仓库自己**：ADR 0019 边界一最后一句「写正文必须作者确认」（同节其余三条保留） | ~~已接受~~ **2026-09-12 由 [ADR 0048](0048-drafting-writes-the-chapter.md) 收回**（落盘交回作者：稿子以未保存的样子进编辑器，他按保存才写盘；机器不落盘）。原：已接受（2026-08-11；同 ADR 0020 的理由链；**新建一章仍是作者的动作**——标题是切章的锚） |
| [0022](0022-drafting-is-a-proposal-not-a-write.md) | 起草是**提议**不是落盘：生成 / 落盘拆成两个动作，候选既不进正文也不进对话 | **本仓库自己**：ADR 0021 的**机制**（落盘是起草的副作用）；其**精神**保留（落盘仍不问作者） | ~~已接受~~ **入口形态 2026-09-12 被 [ADR 0048](0048-drafting-writes-the-chapter.md) 换掉**（候选表 / 读回 / 并排页保留）。原：已接受（2026-08-12；旧机制下一批三稿会自相残杀——第一稿落盘后另两稿被 sha 闸拒，而「别处」是它自己） |
| [0023](0023-context-is-pruned-by-rebuildability.md) | 上下文按「**可不可重建**」剪，不按「新不新」；**作者的规矩带章号区间**，取窄 + 重复放宽 | 实现 ADR 0019 边界四那张一直没落地的表；把边界五的剪枝顺序重述成按可重建性 | 已接受（2026-08-12；**作者偏好过期要 fail-open，跟 `must_not_reveal` 方向相反**；厂商的服务端清理是第二层不是替代） |
| [0024](0024-a-turn-is-a-conversation-not-a-black-box.md) | 编排**边跑边发事件**（引擎不认识传输）；只换「跑一轮」那条传输；加一个**「问作者」**的工具 | **本仓库自己**：一个从 M1.5 起没人写下来过的隐含前提「工作台 = HTTP 请求/响应」；以及 ADR 0019 第四问那条理由（结论不变，换理由） | 已接受（2026-08-12；**实测 HTTP 只值 1 毫秒 —— 不是性能问题，是请求/响应不能「推」**；没有它，刚做完的「可中断」只能闭眼按） |
| [0025](0025-unregistered-routes-ask-the-endpoint.md) | 没登记的路由**去问端点自己**（只对把能力发布成 API 的家）；数字取**所有上游的下确界**；读不懂就整份作废 | **本仓库自己**：一个没写下来过的隐含前提「能力只能由维护者事先审计」——`resolve_capabilities` 的 `metadata` 那一档留了口子却零调用方 | 已接受（2026-08-13；作者自选端点时 `unknown` 会同时掐掉**停按钮 / 边写边看 / 账**三样，而 ADR 0024 刚把它们做出来） |
| [0026](0026-the-table-only-holds-what-cannot-be-learned.md) | 能力表**只装学不会的**：协议保证的删掉（`supports_streaming` / `supports_stream_usage`）、公开的外包给一份可一键更新的快照（窗口 + 单价）、测不起的才留 | **本仓库自己**：ADR 0025 落地一天后发现它解决错了问题；以及「路由的每一位能力都该由一张表来答」这条没写下来的前提 | 已接受（2026-08-13；**旧默认让自选端点的「停」和「边写边看」一起哑掉且不报错**；对照过 Cursor 根本不存这一位、Aider 直接用 litellm 那份公共表） |
| [0027](0027-scene-blocks-cut.md) | **场景块和 R4 一起砍**：那套 `## 场景 N` + `<!-- nh: … -->` 标记语法要作者手写，真书上覆盖率 **0%**，于是一条「零误报」的规则一次都没开过火；屏幕上那句「这一章还没有场景信息」不是空态是常驻文案 | **本仓库自己**：PLAN §5「改 3」、`text/scenes.py`、R4、两条路由、中栏场景条、底栏「场景顺序」列 | 已接受（2026-08-14；作者问「为什么会有场景信息」；同 ADR 0014 砍 R5 的判据，只是数字从 8.2% 变成 0%；顺手发现右栏「暂时无法进行内容检查」自 R2/R3 进表起说谎 12 天） |
| [0028](0028-rules-expire-by-situation.md) | **作者的规矩按情境失效，由模型判**：引擎只做去重 / 撤销 / 条数上限，规矩带着「作者写第几章时说的；情境不在了就不必守」进 prompt；不上屏 | **本仓库自己**：ADR 0023 决策二的后半段（章号区间 + 说两遍升章级）和它的界面形态（那颗按钮 + 两条路由） | 已接受（2026-08-14；作者：「男主出了沙地这个规则就失效了，这个要由 llm 智能判断」；维护者提出「拿掉界面 = 拿掉 ADR 0023 的退路一半」，作者驳回——**稿子本身就是审查面**，规矩判错直接写在字上） |
| [0029](0029-save-triggers-snapshot-refresh.md) | **保存键触发快照刷新**：不可变 `ChapterCommitToken` + 单调 generation + CAS 收口晚到结果；固定章节刷新 DAG（验证 → 总结∥抽取），不做通用 Workflow Runtime | **本仓库自己**：「只在离章时 autopilot、禁止挂保存键」的决定 | 已接受（2026-08-17；保存是作者唯一稳定的「我写完这一版了」信号；两秒合并 + 幂等 coverage 让成本可控） |
| [0030](0030-versioned-summaries-and-advisory-reconciliation.md) | 三类总结 **append-only 版本 + 显式 head**、作者优先 CAS；核对只写系统通知，**不阻断**总结/Canon | **本仓库自己**：「最新 = 按 rowid 猜」的隐式做法；[ADR 0005](0005-set-judgment-only.md) 的窄例外（只告警的语义核对） | 已接受（2026-08-17；「总结可能和正文不一致」与「正文触发 R2/R3」不共表、不共 UI、不共闸门） |
| [0031](0031-auto-alias-with-reversible-correction.md) | **无歧义别名自动入档**（`source=extractor` + 证据 + 阈值），歧义隔离进聚类提案；修改/撤回/改归属 + 持久 replay outbox | [ADR 0004](0004-declaration-over-extraction.md) 中「别名聚类必须先确认」的**一部分**（其余原样保留） | 已接受（2026-08-17；别名是索引不是事实，失败方向轻；**先纠错后自动**的启用顺序） |
| [0032](0032-reversible-auto-canon-edges.md) | `LOCATED_AT/HAS_STATE/RELATED_TO` 三类边 **先可逆、后自动**：稳定 ID 读取/修改/软撤回/改归属/审计入口齐了才进 auto-Canon allowlist | 不推翻新决策——**执行 ADR 0020 已满足的推翻条件**（那三类缺纠错路由的缺口） | 已接受（2026-08-17；append-only `canon_edge_override` + 唯一语义槽；**无纠错入口不得 auto-Canon**） |
| [0033](0033-pre-draft-calibration-scene-brief.md) | 模式二**写前校准 + 不可变 `SceneBrief`**：作者意图 / 作者 Canon / 机器抽取 / 机器总结 / 未知**分开不混**，Writer 拿到的是类型化、可反查的执行简报 | —（补 `docs_dev/2026-08-17` 定下的模式二写前数据链：空白新章不该因 `UnknownCast` 把正向记忆一起关掉） | ~~已接受~~ **已退役**（2026-09-12，[ADR 0047](0047-drafting-is-assistant-curated-not-calibrated.md)）。原：已接受（2026-08-17；同批定下**迁移编号约定**——它的迁移一度错命名成 `024_`，跳号让 `_migrations()` 直接抛 `MigrationError`，2026-08-19 改名 `017_`，并行任务的 017–023 将来整体后移一档） |
| [0034](0034-no-command-line-surface.md) | **命令行面整个删掉**：17 个子命令 + `cli.py` + `__main__.py` + `[project.scripts]` + typer 全删（−2984 行）；产品线 = 桌面壳，调试线 = Web 工作台，**没有第三块表面**；`serve` 搬成 `api/launch.py::launch()`、`gate` 留一个薄入口 | **本仓库自己**：ADR 0012 的入口叙事（`__main__.py` 存在的全部理由）、ADR 0007 里「uvx 一条命令」那一截的落地形态、`PLAN.md` 通篇按命令行写的作者面 | 已接受（2026-08-20；作者定的三条线「桌面=产品 / Web=调试 / CLI=不留」；**留着它就是维护第二块没有用户的作者面**——而那块面已经骗过人两次：心跳卡在早被删掉的子命令上、以及红了四天没人发现） |
| [0035](0035-autopilot-cut.md) | **换章 autopilot 整块砍掉**：`POST/GET …/autopilot` 两条端点 + 前端类型 + 契约 fixture 全删（端点 92→89）；它写下过的五条纪律逐条搬家 | **本仓库自己**：`api/autopilot.py` 那套「换章即后台」设计，和 UI_ARCHITECTURE 里它的成本论证 | 已接受（2026-08-20；并行任务 Task 16 就把前端那一半摘了，后端留成一块**零调用方**的付费入口——合并那天才发现。**删的时候查出一个真 bug**：「撤回过的章不许被自动买回来」这条纪律在新的保存触发链上是破的，而它唯一的测试走的正是这条已没人调用的端点，所以一直绿着） |
| [0036](0036-scheduling-allocates-slots-not-permission.md) | **调度只分名额，不决定做不做**：权重只排一轮的名额（要干活的章全部进池子，唯一豁免是作者当前打开的那一章）；「缺不缺」和「旧不旧」拆成两个独立判据、收敛到 `summary_alignment` 一个出口；协调器按 `missing_branch_mask` 决定跑哪几支；覆写单真去重买、自动覆盖；保存算出的「失去依据」清单落进 `chapter_refresh_run` | **本仓库自己**：`summary_schedule` 把排队分数当准入门槛、`chapter_refresh` 把三件事挤进一份「缺哪几样」的清单、协调器根本不读那份清单 | 已接受（2026-08-23；三处实测：作者焦点在第 2 章时后面 156 章权重全 0.000；扫描器报 `queued_overwrite` 而单里没有总结那一项；`skip_summary=True` 排出 mask=0b101 但总结 adapter 照调 1 次。**同一个病的三层：一个数在两个地方各有一份含义，没有任何测试站在缝上**） |
| [0037](0037-widen-the-cast-instead-of-giving-up.md) | **拿不准就往里加人，不整个放弃**：歧义称呼把所有候选的正式名都算在场；模型主动查认知矩阵时给一格不给整张表（答案带「截至第 N 章」）；续写 prompt 删掉「【在场】未知」那一块 | **本仓库自己**：[ADR 0018](0018-cast-is-derived-not-declared.md) §2「歧义称呼由 `rules_only=True` 天然出局」——那只在 cast 因此变空时成立（正文一字未动，加了带日期的补记） | 已接受（2026-08-23；判据「多算一个人 = 多一批禁令 = 安全，少算一个人 = 漏」方向可证明。**全禁也安全，但它安全得没用**——AI 面对一份「什么都别碰」的清单写不出能用的东西，而空章起草必然触发它。矩阵不整张出去的真理由是**寿命**不是 props：起草 prompt 每次重建，工具结果永久留在对话里） |
| [0038](0038-track-guardrails-and-split-contexts.md) | **改老章的轨道**：不是最新章才启用；锚定按相关性不按位置（前两级零模型调用）；**写的和验的分上下文**（Writer 从没见过轨道）；评语形状限死为「第几句 ↔ 第几章 + 冲突类型」；落一条不阻断的通知 | **本仓库自己**：一个写在三处代码里、没写在任何文档里的前提「作者永远在书的最前沿写」；并给 [ADR 0019](0019-agent-loop-not-graph.md) 六问表补一格（第五问那个「全是纯函数」的理由在这儿不成立） | 已接受（2026-08-23；**信息隔离是地基不是优化**——后面章节的总结里写着这一章读者还不该知道的事，给 Writer 看等于把伏笔亲手告诉它。机制是形状不是提示词：`TrackClash` 没有能装下轨道原文的字段。**今天的真实缺口是缺一根线，不是缺一套检索**——索引在跑、反查函数在，写作那条路一次都不调） |
| [0039](0039-secrets-offline.md) | **秘密整套下线**：`Secret` 节点 / `KNOWS`·`BELIEVES` 边 / 认知边界矩阵 / `SecretDetail` / 三条声明·改正路径 / M2 那套仪器 / `revealed_facts` 那条限肢，连同 `secret`·`event_reveal` 两张表一起删（028） | **这个项目的第一句话**：`CLAUDE.md` 开头那句「唯一一个知道『谁在第几章还不该知道什么』的引擎」、README 第一行、PLAN §3.2 的头牌面板 | 已接受（2026-08-25；**产品裁定不是数据裁决**——依据是「作者今天要的是上下文」，不是「真书上这一格是空的」。**四处塌方**：`SAFETY_FACT_TYPES` 成空集、[ADR 0018](0018-cast-is-derived-not-declared.md) §3 的单调性证明失去对象、[ADR 0002](0002-no-vector-retrieval-in-v1.md) (b) 整节失效（「不欠一个 RAG 对照组」跟着塌）、`UnknownCastConstraints` 不再是安全类型——**四处的代码都留着，理由都改写了**。**计划里两条前提是错的**：`event_reveal` 有一整条限肢在引它；M3 那张卷子有 5 个 Secret 节点，第一刀把 25 题变成 12 题而测试绿了三天——因为 `gate.db` 不进 git） |
| [0040](0040-future-leak-cut.md) | **R2 `FUTURE_LEAK` 砍掉**：`checks/future_leak.py` + `SYSTEM_RULES` 里那条 `RuleSpec` 一起删，`ALL_CHECKS` 2 → 1；`first_appears_chapter` 字段和 `declare_first_appearance()` 都留着 | **本仓库自己**：[ADR 0005](0005-set-judgment-only.md) 那张表里的 R2 一行（正文不改，变成历史记录） | 已接受（2026-08-27，维护者裁定）——**它的输入从来没有入口能填**：`first_appears_chapter` 说的是「这东西我打算第 200 章才让它出场」，那是作者的计划、物理上不在已写文本里（ADR 0004），而浏览器上从来没有那个框。不是「作者嫌麻烦不写」，是**没地方写** |
| [0041](0041-forbidden-entities-cut.md) | **`forbidden_entities`（未登场实体禁写清单）整个删掉**：右栏「写作提醒」、产品起草的约束装配、模式二两个工具里的全部读取一起摘 | **本仓库自己**：「未登场实体不许提前出现，系统该主动挡一道」这条设计 | 已接受（2026-08-31，**维护者裁定不是数据裁决**）。它和 R2 是同一个病的两面：那份清单的唯一输入也是 `first_appears_chapter` |
| [0042](0042-dead-speaks-cut.md) | **R3 `DEAD_SPEAKS` 砍掉，系统规则清零**（`ALL_CHECKS` 1 → **0**）；整套规则机制、检验发生的位置、作者自定义的 `forbidden_literal` 全部保留 | **本仓库自己**：「引擎自带一套连续性硬规则」这条设计 | 已接受（2026-09-05，维护者裁定）。**结构性理由**：`is_dead` 只有「标死」一个写入方，抽取的封闭枚举里没有「活过来」——于是死而复生的书上一个人被标死之后**永远是死的**，而 R3 `blocks_downstream=True`，卡住的是那一章的总结和抽取。作者唯一的出路是去删掉一条正确的事实 |
| [0043](0043-facts-store-a-start-not-an-interval.md) | **事实只记起点，区间在读的时候算**：`edge.valid_to_chapter` 停用（迁移 036 清空、不删列），排他类型改成「同一语义槽里取 `valid_from` 不晚于本章的最后一条」（`CURRENT_EDGE_CTE`）；`SupersedeConflict` / `close_edge` / `UpsertResult.closed` 一起删 | **本仓库自己**：「闭开区间 `[valid_from, valid_to)` 是事实的存储形态」这条设计（§5.9「只进不退」那条**纪律**不推翻，换的是实现） | 已接受（2026-09-06，维护者审过方案后裁定）。**`valid_to` 从来是派生值**（全仓唯一写入方永远把它写成「下一条事实的起点」），而提前存派生值要求事实按章顺序到达；补全队列按「离作者正在写的那一章多近」倒着跑（那是对的，ADR 0036），于是每条更早的事实都抛「乱序」、整章回滚——真书 **62 章分析失败、全书只有 11 章有事件**。改完真书上第 4/5 章立刻各出 10/7 条事件 |
| [0044](0044-structured-output-on-the-wire.md) | **要 JSON 就在线上要**：`StructuredCallPlan` 自动带 `response_format={"type":"json_object"}`，端点不认就退掉并记住路由（`_NO_JSON_MODE`）；剥 ```json 围栏收敛成一份（`provider.unfenced`）；解析失败带上 pydantic 指到字段的那一条 | **本仓库自己**：「需要结构化输出的调用靠 prompt 里一句话求模型配合」这个做法，和 `parse_analysis` 三个「不」里的**「不剥壳」** | 已接受（2026-09-06）。**键在类型上不加参数**：调用方没有忘记的余地，散文那两条路一个字节不变。真书 73 次抽取失败里 23 次死在这儿，且**不是内容问题**（同章重跑就好、`finish_reason` 全是 `stop`、失败与成功的输出长度完全重叠）。改完「整份不是对象」那一类消失，**但不是清零**——`json_object` 不保证语法合法 |
| [0045](0045-conflicts-only-when-the-machine-overrules-the-author.md) | **只有「机器要推翻作者」才做提案卡**：`edge_conflict` 的判据从「和当前 Canon 不一样」换成「那一格是作者亲手改过的」（查 `canon_edge_override`）；其余直接自动升 CANON。配套：`settings.review_card_edits` 开关（默认关，改完卡片叫核对模型验一遍）+ 人物卡上「你改过」的标记 | **[ADR 0020](0020-clean-extraction-auto-canon.md)** 三个例外桶里的 `edge_conflict` 那一个（另两个未动） | 已接受（2026-09-06，维护者裁定）。真书回填之后队列 **100 张卡 / 298 条 ≈ 75 分钟纯点击**，而不点的时候那些事实既不进人物卡也不进写作上下文——抽取读对了却等于白跑。**这一刀的前提是 ADR 0043**：人物卡那几格印全部历史，所以自动生效不丢东西，旧值只是不再排第一 |
| [0046](0046-mid-turn-messages-queue-and-merge.md) | **一轮跑着的时候还能说：排队，在步的边界并入，不打断**：`POST …/say` 把中途那句放进正在跑的那一轮的信箱（`agent/loop.py::Mailbox`），loop 在下一次模型调用之前把它按正常的作者消息并进对话；模型说完了而信箱里有话就接着跑；收场时剩的一句不丢（`TurnReceipt.unanswered`）。界面：「停」挪到发送那颗圆钮上，输入框跑着的时候不再灰 | `api/chat.py::_Running` 那条对作者可见的行为（跑着时再发 = 409、输入框灰着）；闸本身未动 | 已接受（2026-09-12，作者提出：「像 codex 那样，新的消息可以直接发出去，模型可以读，并且不会耽误正在做的」）。**不是并发**：模型一次只读一份上下文，能做到的是最早的下一个边界；对话的写入仍然只有 loop 那一条线 |
| [0047](0047-drafting-is-assistant-curated-not-calibrated.md) | **模式二起草改成「助手分析意图 → 由粗到细检索 → 对照规矩（检验规则 / 有时限的规矩 / 查到的矛盾）→ `draft_chapter(chapter, brief, materials)`」**：写手固定装配那六格不动，多两格由助手给：「要写什么」（它自己的话）和「补的资料」（远章总结 / 事件 / 原文节选，按预算封顶，跟着稿子存进候选表让作者看得见）。校准 / 封存两个工具、`calibration/` 整个包、五个封闭指令码一起砍 | **[ADR 0033](0033-pre-draft-calibration-scene-brief.md)** 整条写前数据链；ADR 0019 边界二「draft 只收章号」那一半 | 已接受（2026-09-12，作者定的）。旧链在真书上从没跑通（校准 6 份全是失败那几轮留下的、封存 0、模式二稿子 0 篇），封闭码装不下「重写结局」，报告 232 KB 把助手上下文撑爆；它服务的秘密 / 未登场 / R3 都已下线 |
| [0048](0048-drafting-writes-the-chapter.md) | **稿子直接进左边的编辑器**：写的过程看得到（同一条流，匀速露字；贴着底跟着走、翻上去留在原地 + 「滑到最下方」），写完以**未保存**的样子留着，作者按「保存」才写进书（`PUT …/text` 带 `draft_id`）、按「放弃这一稿」就丢；**没保存的改动都有痕迹、不分谁改的**（对着上一次保存的那一版：减去的段红、增加的行绿，git diff 的样子，保存即消失）；`save_draft` / `ChapterDesk.land` 删，**模型手上没有落盘这个动作**；对话里每稿只剩一行、字一个都不画 | **[ADR 0022](0022-drafting-is-a-proposal-not-a-write.md)** 的入口形态；**[ADR 0021](0021-agent-writes-drafts-without-asking.md)** 的「落盘不问作者」**收回**（不是机器不问，是机器不落盘）；ADR 0019 边界一「写正文必须作者确认」又成立 | 已接受（2026-09-12，作者提出；同日改了四版，第一版是「写完直接落盘」，第三版把落盘交回作者，终稿把稿子钉在左边、痕迹改成 diff，记录在 ADR 内） |

## 为什么这些 ADR 是公开的

一个仓库里有明确的、公开的、**会让作者自己停下来的 kill-gate 和触发条件**，是最强的可信度信号——比任何架构图都更能招来认真的贡献者。

## bench/

ADR 0001 的实测证据。可复现：

```bash
cd docs/adr/bench
python scale_bench.py    # 构造 500 章 fixture（novel.db, ~24MB）并打印 5 类查询的延迟表
python fanout.py         # 打印 1/2/3 跳的扇出对比
```

`novel.db` 是生成物，不要提交进 git。

**fixture 的边界**：它建模的是**原需求文档**的 17 节点 / 20 关系 schema，不是本计划最终的 8 节点 / 9 关系 schema。在最终 schema 下图会显著更小——这只会让 ADR 0001 的结论更强。
