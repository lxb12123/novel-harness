# 架构决策记录（ADR）

每份 ADR 回答四件事：**决策是什么、推翻了原需求文档的哪一条、论证、若此决策错误修复成本是什么**。

v1 的完整实施计划见 [`../PLAN.md`](../PLAN.md)。

| # | 决策 | 推翻了原文档的 | 状态 |
|---|---|---|---|
| [0001](0001-no-neo4j-in-v1.md) | 故事图谱进 SQLite，不用 Neo4j | 原则 12、第 9 轮表态 | 已接受（含加回触发条件） |
| [0002](0002-no-vector-retrieval-in-v1.md) | v1 不做向量检索 | 原则 12、第 9/10 节 | 已接受（含 v1.1 选型与触发条件） |
| [0003](0003-stable-ulid-ids.md) | 业务 ID 用 ULID，不用 slug | 第 9 轮 ID 方案 | 已接受（Day 2 必须落地） |
| [0004](0004-declaration-over-extraction.md) | 声明优于抽取 | 第 22 节第 2 项、第 17 节 | 已接受（最重要的单点改动） |
| [0005](0005-set-judgment-only.md) | 只做集合判断，不做语义判断 | 第 13 节 Validator | 已接受（**R5 覆盖率待 Day 1 实测填入**） |
| [0006](0006-evidence-double-pointer.md) | Evidence 双指针 + STALE 不进队列 | 第 7/14 节的 offset 方案 | 已接受（Day 2 契约） |
| [0007](0007-manuscript-lives-on-disk.md) | 正文在磁盘上，v1 不做编辑器 | 第 4/17 节、第 22 节第 12 项 | 已接受（**2026-07-19 时机松动**：应用内 CM6 进 v1，正文真相源仍在磁盘） |
| [0008](0008-related-to-is-undirected.md) | `RELATED_TO` 无向，存储时规范化 | —（补 PLAN **从没回答过**的问题） | 已接受（M1 写关系边之前必须定死） |
| [0009](0009-m2-verdict.md) | M2 kill-gate 的裁决 | —（协议 §8 的时序被维护者裁定显式改变） | **已写（2026-08-03，维护者裁定）**——**非数据裁决**：泄漏统计未产出，「图谱约束有效」未证明；`nh gate` 保留可补跑 |
| [0010](0010-writer-boundary.md) | Writer 边界：`assemble()` 能看见什么 | —（补 `EVAL_PROTOCOL.md` §8 点名要的那份） | 已接受（**`draft/assemble.py` 存在之前必须定死**：第 4 道守卫明说拦不住「完整 PLANNED 进 prompt」，那一格只有它和 review 守得住） |
| [0011](0011-bilingual-draft-length.md) | 双语长度契约与通用模型调用计划 | 固定中文短输出 / Claude 原生调用假设 | 已接受（修正案 5 的产品边界；先于第一份 `runs/*.jsonl`） |
| [0012](0012-book-owns-its-db.md) | 库跟着书走：没有全局默认位置 | —（补 `__main__.py` 一直空着的那个决定） | 已接受（2026-08-02；`novel-harness` 不带参数 = 向上找最近的 `book.db`） |
| [0013](0013-draft-length-is-a-request-parameter.md) | 起草长度是请求参数，不是后端设置 | —（补 ADR 0011 没定的「选择权归谁、存在哪」） | 已接受（2026-08-02；作者随 `/draft` 发 `LengthSpec`，后端只验硬上限） |
| [0014](0014-r5-cut-by-quote-coverage.md) | R5 判据改为引语口径，实测 8.2% → 砍掉 R5 | —（补 ADR 0005 判据分母和探针分母不一致的洞） | 已接受（2026-08-02；M3 规则集 = R2/R3/R4） |
| [0015](0015-inline-continuation-is-a-short-draft.md) | 行内续写是 `/draft` 的一个短档；**不知道谁在场就全禁，不问作者** | —（补「作者停手就提示下一段」这个新能力该挂在哪） | 已接受（2026-08-06；**不知道谁在场就全禁，不问作者、不推衍**） |
| ~~0016~~ | ~~LLM 验证只对机器输出开火~~ | ~~部分推翻 ADR 0005~~ | **已撤回（2026-08-06，维护者裁定）**：那份论证的全部力量来自「验证器在隔离沙盒里跑、不跟作者说话」；不在沙盒里跑就撑不住。**ADR 0005 的「LLM Validator 砍到 v2」原判不动。** 号不复用 |
| [0017](0017-memory-is-an-export-not-a-source-of-truth.md) | 小说记忆导出成 md，但图仍是真相源 | —（补 agent 记忆该住哪、能不能存事实） | 已接受（2026-08-06） |
| [0018](0018-cast-is-derived-not-declared.md) | 在场从正文推，作者不填 | **本仓库自己**：顶栏「出场人物」框 + 「写作前简报」那套心智 | 已接受（2026-08-06；推导是超集 = 多禁 = fail-closed，**但界面上不许说「在场」**） |
| [0019](0019-agent-loop-not-graph.md) | 模式二走单 Agent 工具循环，**工具表就是权限边界**；**上下文投影按章号参数化** | 桌面那份编排综述 §5 的落地图（照 Neo4j + Qdrant 画的）；通行的「按时间近切上下文」 | 已接受（2026-08-06；循环归模型不归代码，不上 LangGraph/LangChain；六条边界里**边界一写错不可回收**，**边界五写错不会报错**） |
| [0020](0020-clean-extraction-auto-canon.md) | 干净的抽取结果**自动升 CANON**（`actor='system'`），三个例外 bucket 照旧进队列 | **本仓库自己**：ARCHITECTURE §10 约束 3 的后半句「Agent 不得污染 Canon」（前半句保留） | 已接受（2026-08-10；退路是**可查 + 可改**而非事前批准，**编辑能力必须先落地**；今天「不烦你」= 「也不用你」= 白抽） |
| [0021](0021-agent-writes-drafts-without-asking.md) | 起草**直接落盘**，退路是版本历史；唯一的闸是「拒绝覆盖作者更晚的改动」 | **本仓库自己**：ADR 0019 边界一最后一句「写正文必须作者确认」（同节其余三条保留） | 已接受（2026-08-11；同 ADR 0020 的理由链；**新建一章仍是作者的动作**——标题是切章的锚） |
| [0022](0022-drafting-is-a-proposal-not-a-write.md) | 起草是**提议**不是落盘：生成 / 落盘拆成两个动作，候选既不进正文也不进对话 | **本仓库自己**：ADR 0021 的**机制**（落盘是起草的副作用）；其**精神**保留（落盘仍不问作者） | 已接受（2026-08-12；旧机制下一批三稿会自相残杀——第一稿落盘后另两稿被 sha 闸拒，而「别处」是它自己） |
| [0023](0023-context-is-pruned-by-rebuildability.md) | 上下文按「**可不可重建**」剪，不按「新不新」；**作者的规矩带章号区间**，取窄 + 重复放宽 | 实现 ADR 0019 边界四那张一直没落地的表；把边界五的剪枝顺序重述成按可重建性 | 已接受（2026-08-12；**作者偏好过期要 fail-open，跟 `must_not_reveal` 方向相反**；厂商的服务端清理是第二层不是替代） |

## 为什么这些 ADR 是公开的

一个仓库里有明确的、公开的、**会让作者自己停下来的 kill-gate 和触发条件**，是最强的可信度信号——比任何架构图都更能招来认真的贡献者。

## bench/

ADR 0001 的实测证据。可复现：

```bash
cd docs/adr/bench
python scale_bench.py    # 构造 500 章 fixture（novel.db, ~24MB）并打印 5 类查询的延迟表
python fanout.py         # 打印 1/2/3 跳的扇出对比
```

`novel.db` 是生成物，不要提交进 git。

**fixture 的边界**：它建模的是**原需求文档**的 17 节点 / 20 关系 schema，不是本计划最终的 8 节点 / 9 关系 schema。在最终 schema 下图会显著更小——这只会让 ADR 0001 的结论更强。
