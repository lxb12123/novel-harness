# ADR 0005：铁律——v1 的规则只做集合判断，不做语义判断

- 状态：已接受（R5 的覆盖率实测待 Day 1 下午补入）
- 日期：2026-07-16
- 推翻了什么：需求文档第 13 节的 Hard-rule Validator 清单（部分）、LLM Validator（全部）、第 7 节的 20 类关系（缩到 9 类）

## 铁律

> **任何需要回答「这句话是什么意思」的规则，一律不进 v1。**

## 背景：为什么划这条线

原设计的头牌规则是 `ADDRESS_CONFLICT`（称呼冲突）和 `KNOWLEDGE_VIOLATION`（人物知识越权）。**这两条都做不了，而且理由是同一个——它们隐含要求「说话人归属 + 受话人识别」，这是未解的 NLP 问题。**

- 真实中文网文大量对白不带说话人标签（`「师兄。」/ 她抬眼。`）
- 一章里可能有 8 个不同角色被称「师兄」
- 字符串匹配没有词边界（中文无空格）、没有指代消解
- `KNOWLEDGE_VIOLATION` 更狠：「这句台词是否泄露了秘密 S」是**语义判定**。退化成关键词匹配后：「他忽然想起龙纹的传说」（叙述非台词）**误报**；「你身上那道疤」（换说法指代 S）**漏报**

**这会在 M3 的「误报 < 1 条/章」生死线上以 5–20 条/章 的形态撞墙——项目会按自己的规则判死。**

## 解法：把认知边界从「事后检测」改成「事前面板」

见 ADR 0004。面板不读正文，零 LLM、零 NLP、零误报。

**预防的价值高于事后检测**：作者忘的不是「我写错了」，是「他这章还不知道」。在他敲第一个字之前告诉他，比在他写完 3000 字之后红字质疑他，**价值高一个量级，破坏力低一个量级**。

一个决定性的结构区分（来自 FANToM 等 ToM 研究）：

- 「列出所有知道 X 的人」= **list 型**问题（模型失败的那种）
- 「这句台词是否泄露了 X」= **binary/choice 型**问题（模型相对能做的那种）
- **而面板连 choice 都不是——它是数据库查询**

**抽取是开放式枚举，校验是封闭式判定，声明+查询是零判定。原文档把这三件事混为一谈了。**

## 铁律划完后，v1 剩下的规则

| # | 规则 | 判定方式 | 读正文？ | FP 风险 |
|---|---|---|---|---|
| **R1** | 认知边界面板 | 集合查询（cast × secret） | **否** | **零（不做断言）** |
| **R2** | `FUTURE_LEAK`：正文提到作者标记为「第 K 章才出现」的实体/秘密，而当前 N < K | 唯一专名精确匹配 | 是 | 极低（专名由作者声明） |
| **R3** | `DEAD_SPEAKS`：已死/未登场角色开口说话 | 正则 `(全名)(道\|说道\|冷笑道\|问道\|答道)` × 图上 status | 是 | **零歧义** |
| **R4** | `LOCATION_CONFLICT`：场景块声明的 `loc` 与人物当前所在地冲突 | 作者声明 vs 作者声明 | **否** | 零 |
| **R5** | `ADDRESS_CONFLICT` | 仅限显式说话人标签引语内 | 是 | **待实测，见下** |

**R1 和 R4 完全不读正文，而它们是零 FP 的核心。** R2/R3 读正文，但限定在**高信号位置**：R2 是作者亲自挑的唯一专名，R3 是说话人标签位置。

**没有一条需要指代消解，没有一条需要说话人归属推断。**

R3 值得单独说：`萧决道：「……」`，而萧决在第 89 章死了，现在是第 152 章。这不需要任何解释——**死人没有对话标签**。而「萧决当年……」（别人提到死者）不会触发，因为它不在标签位置。这就是「集合判断 vs 语义判断」这条线的价值。

## R5 的生死判据（Day 1 下午，2 小时）

`scripts/probe_speaker_tags.py`：

```python
SPEAKER = re.compile(r'(?P<name>[一-龥]{2,4})(?:道|说道|问道|答道|冷笑道|笑道|开口道)\s*[：:]?\s*[「“]')
```

拿 **3 章真实小说**，数两个数：
- 分母：全部称呼词实例
- 分子：落在 `SPEAKER` 模式内的实例

> **⚠️ 口径对齐（2026-07-30 记）—— 填下面那节「实测结果」之前必须先读这段。**
>
> **① 上面那份正则是说明性拷贝，已经和可执行副本漂移了。** 可执行的那份在
> `scripts/probe_speaker_tags.py:27-31`：动词多四个（`沉声道|低声道|喝道|叹道`），
> 引号类还认 ASCII `"`。**以脚本为准**，别照着这份 ADR 去「修」脚本——这里写的是当天的想法，
> 那里是真的会跑的东西。
>
> **② 两边的分母不是一回事，这条比正则漂移要命。** 本节写的分母是「全部称呼词实例」
> （以称呼为单位）；脚本量的分母是 `QUOTE_RE` 数出来的**引语条数**（以对白为单位，
> 见 `probe_speaker_tags.py:72-79`）。**直接拿脚本跑出来的百分比去触发下面那张 10% 的表，
> 是用 A 的数字触发 B 的阈值**——量的根本是两个不同的东西。
>
> 所以顺序是：先统一分母口径（改脚本、或另开一份 ADR 说明为什么改判据），再填「实测结果」。
> **本节的判据和阈值属于预注册，不在这份 ADR 里就地改**——就地改了，10% 这个数就不再是
> 「看到结果之前定下的」。

| 覆盖率 | 判定 |
|---|---|
| **≥ 10%** | R5 进 v1，**只在**显式说话人标签 + 受话人在场且唯一时开火，其余一律不报 |
| **< 10%** | **当场砍掉**，M3 验收标准相应改写 |

### 实测结果

> **2026-08-02 已填**（判据口径先由 [ADR 0014](0014-r5-cut-by-quote-coverage.md)
> 统一为「引语条数」，理由见该 ADR——ADDRESS_CONFLICT 只在显式标签内开火，
> 「标签覆盖了多少句对白」才是它该问的问题）。
>
> 分母 = 引语条数（3 章样本 30+35+32 = 97）
> 分子 = 带显式说话人标签的引语（2+1+5 = 8）
> 覆盖率 = **8.2%**，判定 = **< 10% → 当场砍掉 R5**，M3 验收标准改写为 R2/R3/R4。

**零依赖、2 小时、Day 1 就知道最高风险规则的答案。**

## LLM Validator：砍到 v2

- 慢（10–30s）、花钱、结论主观（文风、模板化抒情、说明书式叙述）——**作者会不同意，然后把它整个关掉**
- 更要命：**它的误报会连坐硬规则的可信度**，用户不会区分是哪个模块在吵
- 它的输出质量完全取决于 Prompt 迭代，而 Prompt 迭代需要真实草稿样本，**v1 阶段样本量为零**——此时写 LLM Validator 是在无数据的情况下猜规则

`ValidationIssue` 的 Pydantic Schema 和 `validation_report` 表 **v1 就建好**，`issue_type` 是开放字符串枚举，**直接采用 ConStory-Bench 的 5 类 19 子类分类法**，别自己发明文档第 23 节那 13 个指标。v2 直接往里填。

## 顺带永久删除的关系类型

- **`DOES_NOT_KNOW`**：组合爆炸炸弹。实体化后额外产生约 67,500 条边，总量冲到 84,000。改为**闭世界推导：不存在 KNOWS 边 ⇒ 不知道**。这是 O(已声明秘密) 而不是 O(角色 × 事实)。
- **`PARTIALLY_KNOWS`**：欠定义（部分知道「什么」？），无法标注也就无法评测。改用**秘密拆子事实 + 每子事实一条 KNOWS**——表达力完全相同，零新增边类型。
- **`CAUSES` / `RESULTS_IN` / `PRECEDES` 的自动抽取**：Conan 关系 F1 0.276，因果只会更低；而且**写作时没有任何 Validator 规则依赖它**——纯成本，零收益。

## Schema 增长规则

> **只有当某条面板分区或某条规则真的要查它时，才允许加一个节点/边类型。**

这把 schema 设计从猜测变成需求驱动。从文档的 17 节点 / 20 关系缩到 **8 节点 / 9 关系**：

- 节点：`Character` / `Location` / `Faction` / `Secret` / `Foreshadow` / `Object` / `StateDim` / `Chapter`
- 关系：`LOCATED_AT` / `MEMBER_OF` / `RELATED_TO` / `KNOWS` / `BELIEVES` / `HAS_STATE` / `OWNS` / `PLANTED_IN` / `RESOLVED_IN`

`Volume` / `Scene` / `Skill` / `Event` / `Fact` / `State` / `RelationshipState` / `EvidenceRef` 在 v1 **没有任何消费者**——没有一条规则、没有一个面板分区会读它们。

## 若此决策错误，修复成本是什么

**低。** 规则是纯函数 `check(ctx: CheckContext) -> list[Issue]`。加一条语义规则 = 加一个文件。真正的成本是反向的：**先加语义规则，误报会在 M3 撞墙，而那时你分不清是规则错了还是图谱错了。**
